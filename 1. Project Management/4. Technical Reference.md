# Technical Reference

> **Scope note:** This file is for Claude/developer reference — architecture, conventions, gotchas, decisions. It is not user-facing.
> A separate **in-app site wiki for league members/admins** (e.g. "how is my handicap calculated," "what does Diff Net mean") is a future feature to design — not yet started. When that gets built, user-facing explanations (like the "Absolute Net vs. Differential Net" content below) should be adapted/duplicated there in plain-language form, not replace what's here.

-----

## Tech Stack

| Layer       | Technology              | Notes |
|-------------|-------------------------|-------|
| Backend     | Python / Flask 3.1      | Blueprint-based; one blueprint per feature area |
| Database    | PostgreSQL (Supabase)   | Connection via pooler port 6543 (not 5432 — IPv6-only on Render) |
| DB Driver   | psycopg2-binary         | RealDictCursor for dict-style row access |
| Hosting     | Render                  | Auto-deploys from `main` branch |
| Dev DB      | SQLite (local fallback) | `database.py` branches on `DATABASE_URL` env var |
| Version Ctl | Git / GitHub            | Active branch: `main` — commit all work directly to `main`, no feature branches |
| Domain      | Cloudflare Registrar    | Not yet pointed to Render |
| Payments    | Stripe                  | Future — not yet integrated |

## Architecture

Multi-tenant Flask app. Each league is fully isolated by `league_id`. No data crosses league boundaries.

```
app/
  app.py              # Flask app factory, blueprint registration, context processors
  config.py           # DATABASE_URL, SECRET_KEY env var loading
  database.py         # get_db() — branches SQLite vs Postgres; _PgWrapper + _PgCursorWrapper
  init_db.py          # DB init — Postgres: runs schema_postgres.sql; SQLite: run_migrations()
  schema_postgres.sql # Full 51-table Postgres schema (source of truth for Postgres)
  routes/             # One blueprint per feature: admin, auth, players, scores, standings, etc.
  templates/          # Jinja2 templates mirroring routes structure
  static/css/main.css
  static/js/main.js
```

Legacy SQLite migration scripts (`migrate_*.py`) live in project root — not used under Postgres.

### Key Files for Orchestration (Planner/Executor sessions)

| File | Purpose |
|---|---|
| `app/routes/scores.py` | Scoring engine, `_recalc_single_round`, handicap recalc |
| `app/routes/schedule.py` | Schedule, scorecards, detailed score sheet |
| `app/routes/players.py` | Player management, handicap detail |
| `app/routes/debug_scores.py` | Hcp before/after audit page |
| `app/routes/email_config.py` | Weekly recap email |
| `app/templates/base.html` | Nav, layout |
| `app/templates/scores/enter_week.html` | Score entry UI |

Verification pattern for handoffs (no live DB in a fresh session): `py_compile` on touched `.py` files, Jinja2 template-parse **through the real Flask app context** (`app.jinja_env`, not a bare `jinja2.Environment()` — the app registers custom filters like `enumerate`/`zip` that a stripped-down environment will falsely flag as missing) on touched `.html` files.

## Conventions

- All queries use `%s` placeholders (psycopg2), never `?` (sqlite3)
- Row access: `row['column_name']` via RealDictCursor — never positional index
- INSERTs that need the new PK use `RETURNING <pk>` — never `cursor.lastrowid` (returns None in psycopg2)
- Dialect branching where needed: `database.is_postgres()` guards SQLite-only code paths
- `enumerate` registered as both Jinja2 global and filter in app.py
- **Admin League Settings numbering** (`admin/settings.html`, added 2026-07-02): every section header and field label is prefixed with an `X.XX` reference number via `<span class="setting-num">`, e.g. `2.05` = Handicap Percent. First digit = section (1 Scoring, 2 Handicap, 3 Max Score Per Hole, 4 Playoffs, 5 Skins Defaults, 6 Self-Reporting, 7 Season Segments, 8 Tiebreakers), two-digit suffix = field order within that section. When adding a new setting, number it as the next `X.XX` in its section (or start a new section number at the end if it doesn't fit an existing category) — don't renumber existing fields, since these numbers are meant to be stable references (e.g. in support conversations, docs).
- `login_required` / `admin_required` decorators in auth.py — all protected routes use these
- **Wiki/tooltip voice** (per @user, 2026-07-15, see `setting_help.py`'s docstring): new copy is written from @user's own first-person point of view as the person who built this site. An occasional first-person aside is welcome where it genuinely helps explain the "why" behind a setting — don't overdo it, most entries stay plain third-person explanation.

## Key Dependencies

| Package          | Purpose |
|------------------|---------|
| Flask 3.1        | Web framework |
| psycopg2-binary  | Postgres driver |
| Werkzeug         | Password hashing |
| Jinja2           | Templating (bundled with Flask) |
| Supabase         | Managed Postgres host |
| Render           | App hosting + auto-deploy |
| Stripe           | Billing (recurring annual subscription, web-only — see Billing section below) |

**Local dev/test tooling (not app runtime deps — only needed to assemble a no-root ephemeral Postgres in a sandboxed session, see "Local Ephemeral Postgres" below for the full recipe):**

| Package (apt path, confirmed working 2026-08-09 on Ubuntu "resolute") | Purpose |
|---|---|
| `postgresql-18` | The server itself (`postgres`, `initdb`, `pg_ctl`) |
| `postgresql-client-18` | `psql` and client tools |
| `postgresql-common`, `postgresql-client-common` | Shared version-management scripts both of the above depend on |
| `libpq5` | Postgres client library |
| `ssl-cert` | Pulled in as a dependency; not otherwise used for a plain-trust local instance |
| `libicu78` | Transitive — `initdb`/`postgres` link against ICU for collation; not resolved automatically by `apt-get download` (unlike `apt-get install`, it doesn't pull the full dependency tree) |
| `libnuma1` | Transitive, same reason |
| `liburing2` | Transitive, same reason |

Re-confirm this exact list with `ldd <prefix>/usr/lib/postgresql/*/bin/postgres \| grep 'not found'` after extracting — it's specific to Postgres 18 on this distro/version and may differ elsewhere.

## Environment & Setup

**Production (Render):**
- Branch: `main` — Render auto-deploys on push
- Env vars set in Render dashboard: `DATABASE_URL` (Supabase pooler, port 6543), `SECRET_KEY`
- Live URL: https://bettergolfleague.com (custom domain, went live 2026-07-15 via Cloudflare Registrar — Render's own `https://bettergolfleaguetracker-oceb.onrender.com` still resolves as a fallback). No app-code changes were needed for the domain switch: no CORS/cookie-domain/hardcoded-base-URL config exists anywhere in the codebase (`grep -rn "onrender\.com"` across `app/` came back empty — only PM docs referenced it), and every `url_for(..., _external=True)` call (e.g. `billing.py`'s Stripe redirect URLs) derives its host from the incoming request, not a hardcoded value, so it automatically reflects whichever domain the request came in on.

**Local dev (Postgres — the only path that actually works):**

*(Corrected 2026-07-03 — this section previously documented a SQLite fallback as a working local-dev option. It never actually worked: every route uses `%s` placeholders (the Postgres convention), and `database.py`'s SQLite branch returns a raw `sqlite3.Connection` with no `%s`→`?` translation, so the first real query in any route raises `sqlite3.OperationalError: near "%": syntax error`. The SQLite path only ever succeeded at schema creation (`init_db.py`'s own internal queries use `?` directly). This was never caught because local dev had apparently never been exercised end-to-end before — only `py_compile`/Jinja-parse static validation, against production Supabase for anything live.)*

```bash
# One-time setup (or automatic via .claude/hooks/session-start.sh in a
# Claude Code on the web session):
python3 -m venv .venv && .venv/bin/pip install -r app/requirements.txt
sudo -u postgres psql -c "CREATE USER golf_dev WITH PASSWORD 'golf_dev_local' SUPERUSER;"
sudo -u postgres psql -c "CREATE DATABASE golf_league_dev OWNER golf_dev;"

export DATABASE_URL="postgresql://golf_dev:golf_dev_local@localhost:5432/golf_league_dev"
cd app && ../.venv/bin/python app.py   # → http://127.0.0.1:5000, init_db() auto-creates schema + demo league on first run
```

**Seeding realistic played rounds:** `app/seed_dev_db.py` (run with `DATABASE_URL` set to the local dev DB — refuses to run against anything else as a safety rail) drives the real `/scores/enter` route via Flask's test client to add 5 matchups on top of the built-in "Shankapotamus Golf League" demo league, covering the edge cases this project's scoring/handicap engine keeps needing debugged: a normal round, an absent player with no sub (ghost score), a brand-new sub's pre-eligibility round, that same sub's eligibility-crossing round, and a manual playing-handicap override. Idempotent — safe to re-run.

**Bugs found and fixed getting local dev working for the first time (2026-07-03)** — all four were latent, never caught because the app had apparently never been run against a genuinely fresh Postgres DB before (only ever exercised against production Supabase, which already had years of accumulated manual/ad-hoc state):
1. `init_db.py _seed_if_empty()`: used the raw SQLite connection object instead of a cursor (`cur.fetchone()` on a `Connection` has no such method) — crashed on the very first empty-DB check.
2. `schema_postgres.sql`: `courses.default_tee_id` had an inline `FOREIGN KEY ... REFERENCES tees(tee_id)`, but `tees` isn't defined until after `courses` in the file — a genuine circular dependency (`tees.course_id` also references `courses`) that only breaks on a from-scratch DB. Fixed by moving that one FK to a post-creation `ALTER TABLE ... ADD CONSTRAINT` (idempotency-guarded via `pg_constraint` check, since this file re-runs on every app startup).
3. Two `DATE_TRUNC('month', requested_at)` indexes (one in `schema_postgres.sql`, one in the now-redundant `migrations/add_course_api_cache.sql`) failed with "functions in index expression must be marked IMMUTABLE" — `DATE_TRUNC` on a `TIMESTAMPTZ` column is STABLE, not IMMUTABLE, since it depends on the session timezone. Removed both (the underlying rate-limit query still works without them, just without that specific optimization on a low-volume internal table).
4. `_apply_additive_migrations_postgres()`'s migration loop ran every file against the same cursor/transaction with no per-file isolation — one failing migration (the DATE_TRUNC one above) poisoned every migration after it in the list ("current transaction is aborted, commands ignored until end of transaction block"), silently skipping all of them. Fixed with a `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` around each migration so a single failure can't cascade.
5. **Registered 5 migrations that existed as files but were never in the additive list** — the exact "written but never registered" failure class already documented below, just newly discovered instances of it: `add_is_sub.sql` (`players.is_sub` — missing entirely broke score entry with `UndefinedColumn`), `add_absence_sub_name.sql`, `add_round_reflections.sql`, `add_scorecard_hcp_override.sql` (**`scorecards.hcp_manually_overridden`** — the column this entire session's absence/override work depends on, missing from a fresh DB), `nullable_absence_round_id.sql`. The other 9 unregistered migration files were checked and found genuinely redundant (their column/table already exists in `schema_postgres.sql` from being folded in later) — left unregistered with an explanatory comment rather than blindly re-added.
6. **`rebuild_league_handicaps_and_scores()`'s Phase 2 silently skipped every round in any season with no `league_settings` row** (`get_league_settings()` returns raw `None`, and the Phase 2 loop treated that as `continue` instead of falling back to the same defaults every other call site already uses). Real production risk, not just a seed-script artifact: `seasons.create()` never inserts a `league_settings` row — only the first Admin Settings save does — so any brand-new season nobody has visited Settings for yet would have this exact "Rebuild Handicap Timeline silently does nothing" gap. Fixed by falling back to the standard defaults (90.0/18.0/'match_play'/'excused_only') instead of skipping. Caught by seeding a fresh season and finding a pre-eligibility round's playing handicap stayed at its naive initial-save value (0) instead of correcting to the temp handicap (18) after the rebuild ran — `rounds_changed: 0` even on a from-scratch rebuild was the tell.

**Two more downstream instances of the same `seasons.create()`/`league_settings` root gap found and fixed 2026-07-03** (see `Audits/2026-07-03-league-creation-new-season-audit.md`): (a) `get_league_settings(db, season_id, league_id)` (`scores.py:146`) was called with the two ID args **swapped** at `admin.py:163` (`panel()`) and `schedule.py:2022` (`week_scorecards()`) — only "worked" by coincidence for a league's first season (`league_id == season_id`), silently returned `None`/hardcoded defaults for any season after that, with no other call site affected (all ~15 others already correctly ordered). (b) `schedule.py:_get_single_course()` treated a missing `league_settings` row the same as `multi_course=1`, so generating a schedule before the admin's first Settings save gave every matchup `NULL` course_id/tee_id even for a genuinely single-course league — fixed to treat a missing row as `multi_course=0` (its actual schema default), matching the Phase-2 fallback precedent above. The root gap itself (`seasons.create()` never writing a `league_settings` row) was deliberately left unfixed — whether to auto-write defaults there, or ship a "copy previous season's settings/teams" feature instead, is a real product decision, not a bug; see the audit doc's Recommendations.

## Manual Points Override

**Built 2026-08-09.** Lets an admin manually adjust a player's `total_points` for a specific matchup, for special-rule situations the scoring engine doesn't model (bonuses, penalties, one-off rulings). Full design rationale in `Plans/2026-08-09-points-override-technical-spec.md` — this section is the "what actually shipped."

**Why a separate table, not a column on `match_results`**: that table is deleted and fully re-inserted, not updated in place, by **9 real write sites across 8 functions in 6 files**: `scores.py`'s `_process_scores()` (normal score entry), `_recalc_single_round()` (Recalc Points), `compute_classical_stroke_play_points()` (weekly bulk pass), and `cancel_edit()`'s session-backup restore; `admin.py`'s `_save_edited_scores()`; `api.py`'s `api_submit_scores()` and `api_admin_approve()` (iOS API paths); `self_report.py`'s `approve()`; `score_import.py`'s `process_upload()`. A column-based override would need to be hand-preserved at every one of those — exactly the shape of GLT's own documented flaw (re-recording points silently wipes a manual override, per the `recording-points` how-to article). The original technical spec's grep (DELETE-only) found 6/7 of these; a fuller grep (by INSERT) during implementation found the other 3 — a real illustration of why this design centralizes on one function rather than trusting every call site to remember.

**`point_overrides` table** (append-only, mirrors `handicap_history`'s existing override pattern): `matchup_id`, `player_id`, `team_id` (snapshot), `field` (`'total_points'` or `'overall_point_won'` — only `total_points` is exposed in the UI for v1), `original_value` (computed value at override time), `override_value`, `reason` (**required** — GLT's own flow has no reason field at all), `created_by_user_id`/`created_at`, `active`, `cleared_by_user_id`/`cleared_at`/`cleared_reason`. Re-overriding never edits a row in place — the prior active row is marked `active=0` ("superseded by a new override") and a fresh row inserted, so the full history of every override ever set on a matchup survives forever, visible in the same page. A partial unique index (`WHERE active = 1`) enforces at most one active override per `(matchup_id, player_id, field)`.

**`scores.apply_point_overrides(db, matchup_id)`** is the one choke point — call it once, right after any `INSERT INTO match_results` for a matchup. Looks up active overrides for that matchup and `UPDATE`s the corresponding `match_results` row(s). Idempotent, cheap no-op when there are no active overrides. Wired into all 9 write sites above **except** `cancel_edit()`'s restore-from-session-backup — that path already restores whatever `match_results` held at reopen time (which itself reflected any override active then), so calling it there would be redundant in the normal case. **Known accepted edge case**: if an override is cleared *while* the matchup is reopened, `cancel_edit` can restore a now-stale value with no active `point_overrides` row behind it (reopen → clear override → cancel-edit instead of resaving) — narrow, not fixed, documented in a code comment at the call site.

**Routes** (`scores.py`, admin-only, `block_if_locked()`-gated like every other points/handicap-rewriting route):
- `GET/POST /scores/override-points/<matchup_id>` — shows every `match_results` row for the matchup with an editable "new value" field (blank = unchanged) and a required reason textarea covering the whole submission. Linked from the Scoring Debug page (`debug/scoring.html`, "Override Points →" per matchup).
- `POST /scores/override-points/<matchup_id>/clear/<player_id>` — clears the active override and re-runs `_recalc_single_round()` (the same function `admin.py`'s existing "Recalc Points" action uses) so the value actually reverts to computed — an explicit admin action, not something that happens as a side effect the way it does in GLT.

**UI marker**: an "Overridden" badge (reusing the existing `.status-badge--in_progress` amber pill, same visual language as other in-progress/pending chips) appears next to any overridden `total_points` value on the Scoring Debug page and on the override page itself — GLT's own Detailed Points Scoresheet doesn't show overridden points anywhere, this is the direct fix for that.

**Open, deliberately not built (per @user's go-ahead on the spec's own recommendations)**: `overall_point_won` (drives W-L-T record) is schema/helper-ready but not exposed in the UI — fast-follow only if a real need shows up. No auto-sync between `total_points` and `overall_point_won` if both are ever overridden — they can diverge, trusted to the admin. No per-league setting to gate the feature on/off.

**Validated against a real local Postgres 18 instance** (2026-08-09) — no system Postgres was available in-environment, so one was assembled from Ubuntu's `.deb` packages extracted with `dpkg-deb -x` (no root needed) into a scratch prefix and run directly via `initdb`/`pg_ctl` against TCP loopback, entirely disposable, torn down after. Confirmed via the app's own real Postgres init path (`create_app()` → `_init_db_postgres()`), not simulated: the `add_point_overrides` migration actually creates the table with the right columns and both indexes, and the partial unique index (`WHERE active = 1`) is genuinely enforced by Postgres — a direct duplicate-active-row insert raised a real `UniqueViolation`. Override persists to `match_results` immediately, survives a simulated recompute (delete+reinsert + `apply_point_overrides()`) unchanged, re-overriding supersedes rather than overwrites (full 2-row history confirmed), clearing marks the row inactive with `cleared_by`/`cleared_at`/`cleared_reason` set, a locked season blocks the override page. An earlier pass had only validated this against sqlite (with shims for `%s` placeholders and the billing lockout hook, since this codebase's sqlite fallback doesn't actually work standalone — see the `oldest_score_date` Session Log entry) — that sqlite run is superseded by this one. **Still not exercised**: the clear route's live `_recalc_single_round()` recompute branch for a truly `completed` matchup with full round/scorecard fixture data — that branch calls the same already-shipped function `admin.py`'s Recalc Points action uses identically, so it's exercising proven code, but wasn't independently re-verified end-to-end in either pass.

## Billing (Stripe) — Recurring Annual Subscription, Web-Only

**Built 2026-07-15.** One Stripe subscription per league (`subscriptions` table, `league_id` UNIQUE), $99/year via Stripe Checkout, 14-day trial (`STRIPE_TRIAL_DAYS`). `subscriptions` mirrors Stripe's own state via webhooks — it doesn't compute status independently, so it can never drift into a state Stripe itself doesn't agree with.

**Why no StoreKit / Apple In-App Purchase**: verified directly against Apple's current App Store Review Guidelines (3.1.3(b), fetched live 2026-07-15, not from training-data memory) — an app may let users access a subscription purchased on the web, as long as the app itself has no purchase button or external checkout link. BGLT's iOS app never sells anything (league creation/billing is web-only), so this applies cleanly — it's the same pattern Netflix/Kindle use, not a guideline workaround. This resolves the "cross-platform entitlement" open question from 2026-07-03: there's only ever one entitlement source (Stripe), so nothing to reconcile across platforms.

**Files**: `routes/billing.py` (blueprint, `/billing` prefix), `templates/billing/`, `migrations/add_subscriptions.sql` (registered in `init_db.py`'s additive list + folded into `schema_postgres.sql` directly). Config in `config.py`: `STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID_ANNUAL`, `STRIPE_TRIAL_DAYS` — all unset in dev by design, `billing.py` degrades to a "not configured" message rather than erroring.

**Routes**: `/billing` (status + Subscribe/Manage buttons, `admin_required`), `/billing/checkout` (creates a Stripe Checkout Session, redirects), `/billing/portal` (Stripe Customer Portal for self-service cancel/manage), `/billing/webhook` (signature-verified via `STRIPE_WEBHOOK_SECRET`, CSRF-exempted like `api_bp` — handles `checkout.session.completed`, `customer.subscription.updated/created/deleted`, `invoice.payment_failed`).

**Enforcement built 2026-07-15 (Option B — usage-based free trial, per @user)**: no Stripe-side calendar trial (`trial_period_days` removed entirely, `STRIPE_TRIAL_DAYS` config var deleted). Instead, a new league gets `FREE_ROUNDS` (6, `billing.py`) rounds of full access — counted as a lifetime total across all seasons via `_league_round_count()` (`rounds.season_id` → `seasons.league_id`, direct join). Once a league is past that with no `active`/`trialing`/`comped` subscription, `get_lockout_status()` returns `locked: True`.

**Lockout behavior**: enforced by a new `@app.before_request` hook in `app.py` (`_enforce_lockout`) — an allowlist of endpoint names (`ALWAYS_ALLOWED_ENDPOINTS`: auth, billing, static/health/PWA, public marketing) always passes through; a locked league additionally gets `scores.enter`/`enter_week`/`enter_week_current` (the actual entry flow), `scores.tees_json` (the entry page's AJAX tee-lookup — found via grep that it's a hard dependency, would have silently broken tee-switching if missed), and `scores.reopen_scores`/`cancel_edit` (correcting an already-entered score — a judgment call, flag it if stricter enforcement is wanted). Everything else redirects to score entry with a flash message. A persistent banner (`base.html`, driven by `inject_nav_context`'s new `lockout_status`) renders on every page while locked, including the still-allowed ones — deliberately covers both "lockout just started" and "is ongoing" with one mechanism, rather than hooking the ~4 different code paths that can create a `rounds` row (direct entry, self-report approval, CSV import, migration import) to catch the exact instant.

**Metrics**: new append-only `subscription_events` table (`league_id`, `event_type` ∈ `lockout_started`/`converted`/`canceled`, `created_at`) — `subscriptions` itself only holds current state (overwritten by every webhook), so this is the only place trial-start/conversion/cancellation history survives. `record_lockout_started()` is idempotent (a league only "starts" its trial-ended state once); `_upsert_subscription()` logs a `converted` event whenever status newly becomes `active`/`trialing` from something that wasn't (covers first subscribe, and also a later resubscribe after `past_due`/`canceled` — verified this doesn't fire twice for a mere renewal). Site Admin Dashboard's new "Free Trial Funnel" card shows currently-in-trial count, locked-and-unconverted count, and a real trial→paid conversion rate (leagues that ever hit `lockout_started` with a later `converted` event, divided into leagues that hit `lockout_started` at all).

**Production state verified 2026-07-15**: Root Beer League (`league_id=2`) already has 21 real rounds recorded — well past the 6-round threshold — but stays unlocked since its `comped` status short-circuits `get_lockout_status()` before round count is even checked. Confirmed via direct query against prod that this feature doesn't unexpectedly lock out either real production league.

**Still queued — Stripe product rename** (per @user, 2026-07-15): the Stripe MCP connector wasn't reachable this session to apply it. Product `prod_UtM87YigGvpA4s` (price `price_1TtZPx8IU40lPzTwfHa3TTNT`) needs: `name` → "Annual League Access" (was "BetterGolfLeagueTracker — Annual League Subscription"), `description` → "One golf league, one season. All site features included. 0% goes to charity." (was "One golf league, one season year. $99/year, all features included, no paid add-ons."). Either via `stripe_api_write` `PostProductsProduct` (`{id: 'prod_UtM87YigGvpA4s', name: ..., description: ...}`) once the connector reconnects, or manually in the Stripe dashboard.

**Comped/free leagues** (added 2026-07-15): a `status='comped'` row is a manually-inserted, non-Stripe-backed subscription — `has_active_subscription()` treats it as entitled alongside `active`/`trialing`, but it's kept as its own distinct status (not reused `'active'`) so a comped league never inflates the Site Admin's real paying-subscriber count; it gets its own "Comped / Free" tile instead. **Production state**: the `add_subscriptions.sql` migration has been run against production Supabase (`zwycrzwunwsqeueqlrxg`), and Root Beer League (`league_id=2`) has a `comped` row (`stripe_customer_id='comped-no-stripe-customer'`, a sentinel value, not a real Stripe customer) — it stays free of charge whenever/if paywall enforcement is ever turned on. To comp another league: insert the same shape of row (`league_id`, a distinct sentinel `stripe_customer_id`, `status='comped'`) — no code change needed per-league.

**What's still needed before this can go live**: a real Stripe account (@user's to create — requires business/bank details Claude can't provide), the env vars above set on Render, and the migration run against production Supabase. Volume pricing ($79/league at 10+ leagues) has no attachment mechanism yet — it requires a customer/organization spanning multiple leagues, and multi-league user accounts are still explicitly deferred (Project Overview's Scope: Out) — single-league billing at $99/year is what's actually implemented.

## Visual Theme — "Sage & Terracotta" / Inter

**Current palette, set 2026-07-19.** All brand colors are CSS custom properties in `static/css/main.css`'s `:root` block — swapping the whole site's palette is a single-file edit, nothing else needs to change:

- `--green-dark: #26302a` (deep sage-ink) — nav, headings, primary text
- `--green-mid: #4c6b52` / `--green-light: #7a9c7e` — mid/light sage, secondary accents
- `--green-bg: #f6f3ea` (warm cream) — feature-icon backgrounds, subtle tints
- `--accent: #f3ded1` / `--accent-hover: #ecc9b3` (soft terracotta tint) — most accents: icons, checkmarks, badges
- `--accent-bright: #c1653f` / `--accent-bright-hover: #a8532f` (burnt terracotta) — small accents/hovers/underlines (tab underlines, icon-button hovers, feedback bubble) — **superseded as "the button color" by `--btn-primary` below, 2026-08-05**
- `--bg: #f8f6f0`, `--border`/`--border-dark`/`--text`/`--text-muted`/`--text-faint` all in the same warm-cream family
- `--teal` is set equal to `--accent-bright` for any legacy consumer, not otherwise meaningful under this palette

**Primary button color — `--btn-primary` (decided 2026-08-05):** every primary/CTA button sitewide uses `--btn-primary: #bcd6ab` (light sage) / `--btn-primary-hover: #aecb99` / `--btn-primary-text: var(--green-dark)`, not `--accent-bright`. This resolved a real drift: `.btn-primary` had been using this sage hex as a hardcoded literal for a while (the actual dominant "primary action" color across nearly every in-app form already), while `--accent-bright`'s doc comment still claimed to be "reserved for actionable buttons only" and a handful of components (`.score-entry-cta`, `.dash-card--cta`, the landing page's `.lp-btn-primary`, `.ap-checklist-banner .btn-primary`) had drifted onto dark-green or terracotta instead. All of those now reference `--btn-primary`/`--btn-primary-hover`/`--btn-primary-text` — **any new primary/CTA button should too, never a literal hex.** `--accent`/`--accent-bright` remain correct for their original non-button purposes (icons, checkmarks, badges, small hover accents) — this change only reassigns "what color is a button."

**Admin indicator color — `--admin-accent` (decided 2026-08-05):** anything whose whole job is signaling "this is an admin-only thing" (the nav drawer's flat "Admin" link, the `role-badge--league_admin` pill on the Dashboard, the "🛡️ Admin Panel" badge in the Admin Panel's own header) uses `--admin-accent: #f4c15a` (gold text) / `--admin-accent-bg: #fff3cd` / `--admin-accent-text: #856404` (pale-gold badge background + brown text). These were two independent hardcoded hex pairs before this change. **Deliberately does not include** the visually-similar pending/warning amber (`#fff3cd`/`#856404` — yes, the literal hex values are shared, but the semantic role isn't) used all over the app for in-progress/trial/pending status chips (`.status-badge--in_progress`, `.sr-chip--pending`, `.profile-badge--trial`, etc.) — those are staying separate on purpose since "this is pending" and "this is admin-only" are different facts that happen to have picked similar-looking colors historically; don't fold them into `--admin-accent` just because the hex matches.

**Nav drawer "Admin" is a flat link, not a nested group (decided 2026-08-05):** `templates/base.html` — `<a href="{{ url_for('admin.landing') }}" class="nav-drawer-group-btn nav-drawer-group-btn--admin nav-drawer-group-btn--link">Admin</a>`, same flat-link pattern as "Stats & Records" (no chevron, no `.nav-drawer-group-items` sibling, `navToggleGroup()` never called on it). It used to be an expandable group holding Score Entry, Print Scorecards, Submissions, Manage Users, Admin Panel, Dues, Hall of Fame Winners, and Billing — all of those now live inside the (tabbed, rebuilt 2026-08-03) Admin Panel itself: Score Entry/Print Scorecards/Submissions on the Weekly tab, Users & Access and League Configuration/Imports/Flight Management on League Settings, League Profile/Billing/Dues on League Info, Contests/Hall of Fame on Contests. **If you add a new admin-only page, give it a tile somewhere in `admin/season.html`'s tabs — don't add it back to the nav drawer as a new nested item**, that's the pattern being deliberately moved away from.

**History**: this replaced a fairway-green/lime palette (set 2026-07-15, referencing unknowngolf.com's shipped CSS) that @user felt over-used lime as more than an accent. Three replacement schemes were built and reviewed live on production in sequence — Coastal Slate & Teal, Sage & Terracotta, Fairway Gold (deep forest + mustard gold) — @user picked Sage & Terracotta. See Session Log 2026-07-19 for the full sequence and the specific hex values of the other two, in case revisiting them is useful later.

**Scheme-portability convention (added during this pass, follow it for any future swap)**: a plain `:root` variable swap doesn't catch colors that were hardcoded as literal hex/rgba *derived from* the old accent (e.g. a drop-shadow tinted to match the old lime, or a gradient glow at a fixed opacity of the old accent color). `templates/home.html`'s hero background/shadow effects now use `color-mix(in srgb, var(--accent-bright) 18%, transparent)`-style expressions instead of literal `rgba(...)` — derive future decorative color-mixes from the variables the same way, don't hardcode a color computed from a scheme that's about to change. Before considering a scheme swap "done," grep the touched templates for `#[0-9a-fA-F]{3,6}` / `rgba(` outside `var(--...)` — that's exactly how 3 leftover hardcoded lime values were caught after the first swap attempt.

**Font**: Inter, site-wide via `base.html` (replaced Nunito Sans 2026-07-16, itself a replacement of the original system-font stack from the 2026-07-15 pass — see WP4.1 Phase 1).

**Scope note** (still true): this system covers shared chrome + the pages that have been explicitly redesigned (landing `home.html`, `compare.html`, `dashboard.html`'s recap tiles). It has **not** been hand-passed over the thousands of page-specific one-off rules elsewhere in the ~8,700-line `main.css` — most of those are either neutral/semantic (status colors, category-differentiated gradients on stat cards, trophy-gold banners) and intentionally don't track the brand accent, but some may still carry an old hardcoded green that was never audited. Do the same grep-for-hardcoded-hex check before assuming a page you haven't touched will look right under a new scheme.

**`compare.html`** (public pricing comparison page) had **no CSS at all** for its pricing table before 2026-07-19 (`class="table"` isn't defined anywhere in `main.css` — pure unstyled browser table, looked broken on mobile). Fixed with a scoped, horizontally-scrollable table, fully variable-driven (no hardcoded colors, verified clean in the grep sweep above).

**Legacy variable aliases (added 2026-07-27):** pre-rename code referenced `var(--color-primary, ...)`, `var(--primary, ...)`, and `var(--green, ...)` in ~35 places across `main.css` — none of those three were ever defined in `:root`, so every one of them was silently falling through to its own stray hardcoded fallback hex (mostly `#212821`, a near-but-not-identical duplicate of `--green-dark`, plus a few `#2e7d32`s) — a few `border-left: 4px solid var(--color-primary)` rules with *no* fallback were rendering with no border at all. All three are now aliased to `--green-dark` in the "P2: Consolidate color variables" `:root` block near the end of `main.css` — new code should reference `--green-dark` directly rather than any of the three aliases, which exist only for the old call sites. If you find another undefined-variable-with-a-drifted-fallback pattern like this, alias it in that same block rather than hand-fixing each call site.

**`.data-table` cascade bug fixed (2026-08-08, UI overhaul Phase 4):** two conflicting `.data-table th`/`td` rule blocks existed in `main.css` — the canonical one (~line 544) and an unscoped override (~line 5277, inside an unrelated Manage Users section) that silently won the cascade and also carried leftover off-palette hex from before the 2026-07-19 swap (`#ddd`/`#f9f9f9`/`#666`/`#f0f0f0`). Merged into one block at the original location; rendered result unchanged (dark-green header via `--green-dark`, which was already winning), but the stray hex now reference `--card-bg`/`--border` per the scheme-portability convention above. If you find another duplicate/conflicting rule block like this elsewhere in `main.css`, merge it the same way — one canonical block, at the original/logical location, not a second override buried in an unrelated section.

**Theme decision: staying on Sage & Terracotta (2026-08-08).** The alternative-palette candidates explored as an interactive artifact — `Artifacts/2026-07-29-theme-options.html` ("Fairway Drench", Studio Amber, Fescue & Ink, Clubhouse Brass) — were reviewed by @user against the current shipped palette, who chose to keep it as-is. No swap. Artifact stays live as the historical record; see `Plans/2026-07-28-mobile-nav-and-theme-options.md` (Themes track now `Archived`) and Decisions Log 2026-08-08.

**Revisited 2026-08-12 (same day, continued) — the theme-options artifact gained a live interactive customizer** (start from any preset, edit background/header/button/accent/text per-location, save/export a new combination — see the artifact itself for the full mechanism, it's genuinely a small design tool now, not just a static comparison page). @user built "Fairway Drench 1st Alternate" with it (saved as `custom_fairway-drench-1st-alter_msqij9f2` in the artifact, now baked in as a permanent preset there) and asked to ship it — **mobile web only, desktop stays on Sage & Terracotta unchanged.** Implemented as a `@media (max-width: 768px)` block (the codebase's established mobile breakpoint, see `.feedback-bubble`) redefining `:root` tokens, placed directly after the base `:root` in `main.css`. This is a real production split: mobile visitors now see a dark two-tone scheme (near-black `#1c211d` background, `--green-dark`/`--btn-primary` unchanged since the alternate reuses the shipped green exactly, cream text), desktop is byte-for-byte unaffected.

**Scope of the mobile override — deliberately partial, not a full dark-mode pass:** only tokens explicitly covered by the customizer export were redefined (`--bg`, `--card-bg`, `--border`/`--border-dark`, `--text`/`--text-muted`/`--text-faint`, `--btn-primary` family, `--admin-accent-bg`/`--admin-accent-text`, `--shadow-sm`/`--shadow-md`). **Explicitly left unchanged** — would need their own design pass, not modeled by the customizer: `--accent`/`--accent-hover` (found to be multi-purpose during this work — used as both a solid badge background *and* plain text/icon color across ~25 call sites in `main.css`, so a blind swap risks breaking one of those usages without a visual check), `--green-bg`, `--btn-warning`/`--btn-warning-hover`, and the `--red`/`--orange`/`--yellow` status colors. If mobile dark mode gets extended further, audit those specifically (start with the `--accent` usage grep) before touching them — don't assume the same swap pattern applies.

**Bug found + fixed same day: the mobile scheme didn't visually apply on the homepage despite the tokens being correct.** @user tested live on an iPhone 16 Pro Max (Chrome, incognito — ruled out caching) and still saw the light scheme. A temporary CSS-only diagnostic probe added to the footer (`.viewport-probe--mobile`/`--desktop` in `main.css` + `base.html` — still there as of this writing, remove once @user confirms the fix) confirmed the `@media (max-width: 768px)` block *was* matching correctly — the bug was elsewhere: `home.html`'s `.lp-hero` has its own hardcoded background gradient fading to a literal `#ffffff` (independent of `--bg`), and `.lp-btn-ghost` had a literal `background: #fff`. Neither tracks the shared tokens at all, so no amount of `:root` overriding could reach them. Fixed with a matching `@media (max-width: 768px)` block added directly in `home.html`'s own `<style>` tag, overriding those two specific rules to reference `--lp-card`/`--bg`/`--lp-ink`/`--lp-line` instead. **Lesson for next time:** the `--accent`/`--green-bg`-etc. scope-boundary above assumed the only risk was *shared* `main.css` tokens being multi-purpose — didn't account for page-specific templates with their own hardcoded literals bypassing the token system entirely. Before declaring a token-based theme override "done," grep the specific page being tested for hardcoded hex/`white`/`#fff` too, not just check the token definitions — same lesson as the "scheme-portability convention" above, just missed on the first pass here.

**That first attempted fix (d3d0253) was itself broken by a second bug — CSS source order.** It placed the `@media (max-width: 768px) { .lp-hero {...} .lp-btn-ghost {...} }` override *before* the original unconditioned `.lp-hero`/`.lp-btn-ghost` rules in `home.html`'s `<style>` block. Equal selector specificity means the cascade tiebreaker is pure source order — a later unconditioned rule always beats an earlier media-query-gated one, *regardless of whether the media query's condition is true*. Confirmed dead on an actual device (iPhone 16 Pro Max) even though a diagnostic probe showed the breakpoint matching correctly — the override was firing, then immediately losing the cascade. Fixed (e4129c3) by moving the whole override block to *after* both original rules. **General lesson:** a same-specificity override via a later media query must be placed after the rule it's meant to override in source order — being "conditionally scoped" doesn't give it priority on its own. Also added a matching `.lp-btn-ghost:hover` override, missed on the first pass (would've kept the dark `--lp-green` border, invisible against the new dark card background).

**Also fixed in passing:** `home.html`'s `--lp-lime` was referenced (`.lp-strip-p a:hover`) but never defined anywhere — a dead reference to a variable from the old lime-accent palette (removed 2026-07-19). Now aliased to `--accent-bright-hover`. Unrelated to the mobile-theme work, just found while in the file.

**Real root cause of "tokens correct + `!important` deployed + still doesn't render" (found 2026-08-13, resolved same day):** the previous session's `!important` cascade-forcing (`8a8b1ab`) and Service Worker cache-version bump (`e7a55cf`) were both dead ends — the `:root` mobile override itself was correct and winning the cascade the whole time (confirmed live via direct `curl` of production `main.css` mid-investigation, byte-for-byte match with source). The actual bug was the **same class as the already-fixed `home.html` hero/ghost-button bug, just far more widespread**: `main.css` had **32 more components** hardcoding `background: #fff`/`white` instead of `var(--card-bg)` — cards, panels, banners, and form selects/inputs across nearly every admin/dashboard/records/subs/stats page (`.settings-section` — the Admin Panel's own League Settings tab — `.archive-card`, `.arc-stat-card`, `.arc-section`, `.sr-card`, `.activity-card`, `.report-card`, `.records-section`(`--half`), `.records-streak-card`, `.dash-ann-banner`, `.ann-card`, `.notif-item`, `.trend-chart-wrap`/`.trend-table-wrap`, `.league-card`, `.list-card`, `.matchup-row` (both definitions), `.card-form`, `.admin-widget`, `.unlock-summary`, `.profile-stat-card`, `.profile-sparkline-wrap`, `.team-ref-card`, `.weekly-score-row td`/`.weekly-pts-row td`, plus selects/inputs `.records-switcher-select`/`.role-select`/`.subs-select`/`.subs-input`/`.tb-select`/`.map-col-select`/`.sch-edit-select`/`.sch-edit-date`/`.form-group select`/`.filter-group select`/`.season-switcher`/`.setting-row__control select`/`.score-input`/`.hcp-override-input:focus`, and `.week-nav-btn`/`.empty-state` — the latter two also had hardcoded `color: #333`/`#222`/`#888` needing conversion to `var(--text)`/`var(--text-muted)`). All converted to `var(--card-bg)`/`var(--border)`/`var(--text)` as appropriate, so the mobile `!important` override (already correct) now actually reaches them.

**Exception — elements pairing a fixed (theme-independent) dark-green accent with a white fill:** `.sch-team-pill`, `.btn-secondary`, `.start-next-banner .btn-primary`, `.sch-edit-save-bar .btn:not(.btn-danger)` all pair `var(--green-dark)`/`var(--color-primary)` text or border (deliberately **not** touched by the mobile override — same-green-everywhere is intentional) with a white background. Converting those backgrounds to `var(--card-bg)` (which *does* go dark on mobile) would have made dark-green text sit on a dark card — near-zero contrast. Converted to `var(--green-bg)` instead (`#f6f3ea`, cream — also deliberately untouched by the mobile override, so it stays light in both themes), matching the pattern `.ap-tab:hover` already used. **If you add a new component pairing a fixed green accent color with a white/light fill, use `var(--green-bg)` for the fill, not `var(--card-bg)`** — same reasoning applies to any future one found.

**Secondary bug found + fixed in the same pass: `.ap-tab.active`** (Admin Panel's active tab pill) set `color: var(--green-dark)` against `background: var(--btn-primary)` — on mobile, `--btn-primary` is deliberately re-pointed to `var(--green-dark)` too (same reasoning as above, the shipped green stays the shipped green), which made the active tab's text and background resolve to the *same* color — invisible text, not merely "wrong". Fixed by changing the text color to `var(--btn-primary-text)`, the token already designed to pair correctly with `--btn-primary` in both themes (`var(--green-dark)` on desktop — no visible change there — `#f6f3ea` cream on mobile).

**Lesson for the *next* "tokens are right but it's not rendering" investigation:** don't reach for `!important`, Service Worker cache-busting, or "must be a browser rendering bug" as the first three moves — those all assume the override is failing to *win* the cascade. Check first whether the elements actually visible on screen *reference the token at all* — `grep -c "background:\s*#fff\b\|background:\s*white\b"` (and the `color:` equivalent) across the whole stylesheet, not just the specific selectors the override touches. A component using a literal hex never enters the cascade fight in the first place, so no amount of specificity-forcing on the *token* will ever reach it, and this is invisible to a "does the deployed CSS match source" check since the literal-hex rule is just as "correct" and "deployed" as everything else — it's simply a different, disconnected rule.

**The actual root cause of the whole saga (found 2026-08-13, later same day, via Chrome DevTools on desktop): a malformed CSS comment silently deleted the entire mobile `:root` override from every browser's parsed stylesheet.** The hardcoded-`#fff` fix above was real and worth keeping, but it wasn't what was blocking the *rest* of the mobile theme (`.ap-tile`, which had never had a hardcoded color, still rendered light too — that's what proved this was a second, deeper bug). The explanatory comment directly above the `@media (max-width: 768px) { :root {...} }` block (`main.css:73-87`) contained the substring `--red*/--orange` in its prose (documenting which tokens were deliberately left unchanged) — the `*` immediately followed by `/` accidentally forms `*/`, the CSS comment-close token, **in the middle of a sentence**. CSS comments don't nest and there's no escape sequence — the first `*/` a parser finds always closes the comment, regardless of author intent. So the big comment actually closed one line early, at that accidental token, and everything after it (the rest of that sentence, including the *real* intended `*/`) got parsed as invalid top-level CSS garbage sitting directly in front of `@media (max-width: 768px) {`. Different browsers/engines recover from top-level parse errors differently, but the practical effect here was uniform and severe: **the entire mobile override block was dropped from the parsed stylesheet before it was ever evaluated against any viewport** — not "present but losing the cascade," not "present but not matching the media query," just gone, on every browser tested (Safari iOS, Chrome iOS, Chrome desktop in device-emulation mode). This is why every previous check kept coming back clean: the deployed file was byte-correct (curl doesn't parse CSS), the cache was fresh (confirmed via a real Chrome DevTools Network-tab byte count, not just cache headers), no service worker was involved, the viewport genuinely matched the breakpoint (430×932 confirmed in DevTools) — and yet Chrome's own Elements→Styles panel for `<html>` never listed the block at all, matching or not, which was the actual tell: a rule that's present-but-not-matching still *shows up* in DevTools (confirmed by a neighboring `@media (max-width: 640px)` block rendering correctly in the same panel); a rule the parser never successfully recognized does not.

**Fixed** by rewriting the comment to spell out the token names in full instead of using an informal `*` "family" wildcard (`--btn-warning*` → `--btn-warning/--btn-warning-hover`, etc.) — removing every place a `*` sat immediately before a `/` anywhere in that comment. Verified fix with a real character-by-character comment tokenizer over the whole file (not just brace-counting, which can't detect this class of bug — a premature comment close and a later incidental `/*`/`*/` pair can net out to a numerically balanced brace count file-wide while still corrupting a specific region in between).

**General lesson, above and beyond the specific fix:** `grep`-based sanity checks on CSS (brace balance, `cat -A` for invisible characters, checking each custom property is defined in exactly N places) cannot catch a malformed comment — the file is 100% valid-looking ASCII, has zero encoding issues, and every property *name* still appears exactly where expected via plain-text search (the corrupted region is prose inside what was meant to be a comment, not a property assignment). The only checks that actually catch this: (1) a real comment-aware tokenizer walking the whole file character-by-character tracking `/*`/`*/` pairs (a five-line Python script — far cheaper than it sounds), or (2) what actually found it here — noticing that a rule confirmed byte-correct in the source was *completely absent*, not merely non-matching, from a real browser's parsed rule list in DevTools. If a `:root`/`@media` block goes missing from Chrome's Styles panel entirely (not shown at all, not even crossed out) despite the source clearly containing it, suspect a parse-time issue in that block or immediately before it — most commonly a malformed comment — before suspecting anything about caching, specificity, or the media condition itself. Worth adding a comment-tokenizer check as a habit anytime a `:root`-scoped or `@media`-scoped block seems to vanish without a trace.

**Follow-up polish pass, same day, after @user's real-device confirmation:** four more reported issues, all instances of the same two bug families already established above (hardcoded literals that never reach the token system):
1. **Text-color contrast**: `.admin-panel-title__league` and `.ap-tile__title` (and 48 other selectors — see below) set `color: var(--green-dark)` directly, with no background of their own — they rely on whatever page/card surface they're sitting on. `--green-dark` is deliberately fixed (never changes on mobile, matches the shipped header green), so on a now-dark card/page background this reads as barely-visible dark-on-dark text. **Systemic fix**, not a one-off: found every `color: var(--green-dark)` instance file-wide (76 total), mechanically classified each by whether its own rule block also sets a fixed light background (23 do — status badges, chips, pills with their own colored fill, e.g. `.status-badge--scheduled`, `.pts-win` — correctly left alone, same reasoning as the `--green-bg` exception above) versus not (52 — swapped to `color: var(--text)` instead). Safe because `--text` and `--green-dark` are the *same hex* (`#26302a`) at the base `:root` — zero visual change on desktop — but `--text` correctly goes light (`#f6f3ea`) under the mobile override while `--green-dark` doesn't, by design.
2. **Archive Settings tile still white**: `.admin-widget__header` (the "📚 Archive Settings" title strip) had `background: #f8faf8` — a near-white hex that the original `#fff`/`white` grep sweep didn't catch since it wasn't an exact match. Converted to `var(--bg)` rather than `var(--card-bg)` — `--bg`/`--card-bg` are two different (if close) values in both themes, so this preserves the original "slightly-tinted header strip within a plain card" two-tone look instead of flattening it.
3. **Feedback button spacing**: `.site-footer-feedback` had no `margin-bottom`, so it sat directly against `.site-footer-legal` (Terms/Privacy/Refund) below it with no breathing room. Added `margin-bottom: 14px`.
4. **Broader near-white sweep**: re-ran the hardcoded-background search without requiring an *exact* `#fff`/`white` match — found 200+ more light-hex backgrounds file-wide. Classified by RGB-channel spread: ~154 are genuinely tinted (red/amber/green/purple/blue status colors — `#fee2e2`, `#fef3c7`, `#f0fdf4`, etc.) and correctly stay fixed-color regardless of theme, same convention as every other status badge in the file. The remaining ~54 are neutral grays (near-equal R/G/B) — of those, ~14 are genuinely semantic gray badges/pills (`.archive-badge--hidden`, `.li-badge--gray`, `.chip-tie`, etc. — left alone) and **40 are structural surfaces mistakenly hardcoded**: table headers/rows (`.score-table thead th`, `.records-table th`, `.activity-standings-table th`, dozens of `.xxx-row td` variants), card-header strips (`.skins-round-card__header`, `.activity-card__header`, `.ann-card__header`, `.dash-ann-footer` — converted to `var(--bg)`, same two-tone pattern as `.admin-widget__header`), info/callout panels (`.users-invite-box`, `.playoff-seeding-preview`, `.bracket-result-form`), and a few interactive toggle buttons (`.archive-toggle-btn`, `.arc-nav-btn`). All 40 converted to `var(--card-bg)` (or `var(--bg)` for header strips), with any paired muted-gray text color (`#555`/`#666`/`#888`/`#999`/`#aaa`/`#6b7280`/etc.) converted to `var(--text-muted)` in the same pass — same reasoning as the `--text`/`--green-dark` swap above, since `--text-muted`'s base-theme value already matches these grays closely enough that the visual change on desktop is negligible.

**Known remaining gap, not fixed this pass:** ~9 instances of `border-bottom: 1px solid #f0f0f0` inside the same header-strip components just converted — a hardcoded light border color, lower severity than the background/text bugs above (a subtle line, not unreadable content or a jarring white block) but still not theme-adaptive. Convert to `var(--border)` if it's visually noticeable on mobile; same technique as everything above.

**For any future theme/color tweak, run these two checks before considering it done** (both demonstrated this session, both cheap):
1. A comment-tokenizer script (character-by-character `/*`/`*/` pair tracking over the whole file) — catches malformed comments that brace-counting can't.
2. `grep -noE "background(-color)?:\s*#[fF][0-9a-fA-F]{5}\b" main.css`, classify each distinct hex by RGB-channel spread (near-equal = neutral surface, likely a bug; spread = tinted semantic color, likely intentional) rather than only searching for exact `#fff`/`white` — the exact-match search missed `#f8faf8` and the whole near-white-gray family entirely.

## Season Context & Rollover

*(Added 2026-07-03, WP3.20)*

**Current-season model:** the app "lives" inside one season at a time. `get_current_season_id(db, league_id)` (`database.py`) is the single source of truth — it returns `session['current_season_id']` after validating that season belongs to the current league (guards stale ids after a league switch), else falls back to newest-by-`season_id` and writes the session key. `inject_nav_context()` (`app.py`) and every "current-season" page route use it; the nav drawer's "Switch Season" select (`/switch-season/<id>`) therefore moves the *whole app* into the chosen season, not just the dropdown. When adding a new route that needs "the current season," call this helper — do **not** write another `ORDER BY season_id DESC LIMIT 1`. Known deliberate exceptions: `admin.overview()` (has its own season-picker UI), `api.py`'s `_current_season()` (mobile JWT, no browser session), `public_view.py` (unauthenticated).

**Year-prefixed headers:** `inject_nav_context()` exposes `nav_season_year` (first 4 chars of the current season's `start_date` if `YYYY…`, else a 19xx/20xx token in `season_name`, else None) — rendered as `2026 · League Name` in the drawer header and dashboard page header. The "Start Another Season" wizard requires `start_date` partly to feed this.

**"Start Another Season" (client-facing verbiage — never "copy previous season"):** wizard at `/seasons/start-next` (`seasons.py`). Single-transaction create with opt-out bring-overs: teams & divisions (straight re-point to new `season_id`, inactive-player teams copied and flagged in the hub, not dropped), league settings (explicit clone column list `_LEAGUE_SETTINGS_CLONE_COLUMNS` — keep in sync when adding `league_settings` columns! — `dues_amount` kept, `dues_due_date` forced NULL), and starting-handicap seeding via the shared `_seed_starting_handicaps(db, league_id, season_id)` (`admin.py`, also used by the standalone seed route; no commit inside, caller owns the transaction). Players/courses/tees are league-scoped and always persist — nothing to copy. On success the session moves into the new season and redirects to the **Season Setup hub** (`/seasons/<id>/setup`): status cards for roster, player activation, settings, schedule, buy-ins (reuses `dues.py`'s paid/eligible derivation), and starting handicaps.

**Season-over signal:** `season_is_over(db, season_id)` (`seasons.py`) — `MAX(matchups.scheduled_date) < today` (ISO-string compare), falling back to `end_date` when no matchups. Drives the dashboard admin CTA card and the admin-panel banner; a "Continue season setup" link appears when a newer season still lacks matchups or settings. Note the import direction: `seasons.py` imports `_seed_starting_handicaps` from `admin.py` at module level, so `admin.py` imports `season_is_over` lazily inside `panel()` to avoid the cycle.

**Season locking (added 2026-07-27):** `archive_settings.locked` (default 1, set via the Archive Settings panel's 🔒/🔓 toggle — `archive.update_settings`, action `toggle_locked`) is enforced, not just displayed. `routes/archive.py` exposes `season_is_locked(db, season_id, league_id)`, `block_if_locked(db, season_id, league_id, redirect_endpoint, **redirect_kwargs)` (flashes + returns a redirect if locked, else `None` — caller does `blocked = block_if_locked(...); if blocked: return blocked`), and `locked_season_names(db, league_id)` for league-wide checks. Wired into every route that can rewrite a season's recorded points/handicaps: `scores.enter` (POST only)/`reopen_scores`/`cancel_edit`/`swap_side`/`clear_scores`, `admin.recalc_points`, `handicap.recalc_season`, `schedule.clear`. **`handicap.rebuild_timeline` has no `season_id` — it's a full league-wide chronological rebuild** — so it can't be gated per-season; instead its Apply/POST step blocks outright if `locked_season_names()` returns anything (GET preview still runs and rolls back, harmless). When adding a new route that mutates scores/handicaps/points for a specific season, call `block_if_locked` right after resolving `season_id` — don't assume this is covered by `@admin_required`, locking is a separate, additive check. Not gated by lock (deliberately, per @user's ask — "past points or handicaps" only): contests, dues, hall of fame admin routes.

## UX Design Patterns

### Score Entry — Input Advance Philosophy

Score inputs follow **row-major (player-centric) advance order**: all holes for player 1, then all holes for player 2, etc. This matches the natural DOM tab order (inputs are laid out as player rows × hole columns in the HTML).

**Rules:**
- Single digit 1–9 → auto-advance to the next input (same player, next hole). Uses the `input` event, not `keydown` — mobile numeric keyboards (iOS/Android) do not reliably fire `keydown` with `e.key` values.
- `Enter` key → same advance.
- Typing `0` or any non-single-digit value (e.g. "10") → stays in cell. Admin enables "Allow double-digit" for scores ≥ 10.
- Advance order is built in JS as a flat array (`_scoreAdvanceOrder`) from `players → holes` loop nesting (not `holes → players`).

**Mobile layout (unified 2026-07-03):** `scores/enter.html` and `scores/enter_week.html` now share one mobile scorecard treatment, per `Audits/2026-07-03-mobile-entry-unification.md`:
- **Part 1 — dead card view removed.** The old hole-by-hole card view (markup, controller JS — `loadHole()`/`syncAndCalc()`/`window.updateMobileView` — and the `.mobile-sc-section`/`.mob-*` CSS family) was fully deleted from `enter.html`, along with the `.mob-score-input` special case in `base.html`'s `spApplyAbsenceToScores`. It had been visually retired (`display: none !important`, no media query ever re-showed it) but was still shipping and executing against hidden elements — pure dead weight, zero user-visible change from removing it.
- **Part 2 — shared compact table.** Both pages now use the same `@media (max-width: 640px)` compact table (`main.css`, in the "COMPACT MOBILE SCORECARD TABLE" section) and the same `fit()` JS (moved from `enter_week.html` into `base.html` so it's shared — page-agnostic, operates on every `.scorecard-table` in the DOM via `document.querySelectorAll`, no-ops harmlessly above 640px or where there's no scorecard). `fit()` computes per-hole column widths via canvas `measureText` once fonts are ready, and on width-changing resizes; names are abbreviated to first-name-only on mobile via `.sc-name-first`/`.sc-name-last` (last name hidden) so `fit()` can measure a shorter string.
- **Completed-view Pts column:** the Tot/Hcp/Net/Pts summary columns are hidden during live entry and revealed via a shared `sc-completed-view` class (renamed from `enter_week.html`'s old `ew-completed-scorecard`) applied to the `.scorecard-wrap` div — `enter_week.html` applies it to its locked/completed matchup cards; `enter.html` applies it via `{% if matchup['status'] == 'completed' %}` since it always renders one live editable form regardless of status (no separate locked view).
- **Shared Hcp-marker overlay CSS** (`.hcp-cell-wrap`/`.hcp-provisional-mark` positioning, both the desktop base rule and the mobile override) was folded into `main.css`, replacing byte-identical copies that had drifted into both templates' local `<style>` blocks — moving only the *mobile override* to main.css while leaving the *base* rule duplicated locally would have silently broken the override (main.css loads before either template's local `<style>`, so an unconditional local rule of equal specificity would win at every width via cascade source order).
- Breakpoint reconciled to 640px as the primary mobile breakpoint (matching `enter_week.html`'s original, better-tested treatment); the remaining 641-768px `main.css` block now only sets `.scorecard-wrap` horizontal-scroll + `font-size: 16px` anti-zoom on `.score-input` (the old fixed-36px-width override was removed as redundant/conflicting with the new fixed-layout table).

**Input type:** Score inputs use `type="text" inputmode="numeric" pattern="[0-9]*"` — NOT `type="number"`. On iOS, `type="number"` can return `this.value = ""` during the `input` event before the browser commits the value, which breaks the auto-advance regex check. `type="text"` with `inputmode="numeric"` shows the numeric keyboard on iOS/Android and gives reliable `this.value` on every keystroke. Range validation (1–20) is enforced server-side.

**iOS focus — confirmed working approach:** `input` event + `requestAnimationFrame(() => next.focus())`. Both sync and rAF calls are made (sync for desktop/Android speed; rAF for iOS). Confirmed working on iOS Safari and Chrome iOS as of 2026-06-13.

**What was tried (in order):**
1. `keydown` + `e.key` — iOS virtual keyboards don't fire reliable `e.key` values
2. `input` + `type="number"` — `this.value` can be `""` during the event on iOS before browser commits
3. `input` + `type="text" inputmode="numeric"` — appeared broken; root cause was actually a JS syntax error (see below)
4. `input` + `type="text"` + `requestAnimationFrame(focus)` — **confirmed working** ✓

**Root cause of all iOS failures:** A missing `</script>` tag before `{% endif %}` in `enter.html` caused a JS syntax error in the holes script block. All holes JS (live calc, stroke dots, auto-advance) was silently dead. The `<script>` for `toggleAbsenceRow` appeared literally inside the unclosed holes `<script>`, and `<script>` is not valid JS syntax. Added `</script>` before `{% endif %}` in commit `37c8fb5`. The iOS focus restriction research was a red herring — `input` + rAF focus works fine once the script executes.

**Why player-centric, not hole-centric:**
Walking a golf hole, each player writes down their own score before moving to the next hole. A player-centric tab order (P1: H1→H2→H3…H9, then P2: H1→H2…) matches how a scorer mentally fills the card — one player at a time. Hole-centric order (H1: P1→P2→P3→P4, then H2…) adds 17+ unnecessary focus jumps across rows, which feels wrong even on desktop and becomes expensive on mobile.

**Related audit item:** P0-5 in `Audits/2026-06-12-score-entry-audit.md`

**Debug overlay recipe** (add temporarily when JS isn't running, remove after):
```html
<!-- inside {% if holes %}, before <script> -->
<div id="_dbg" style="position:fixed;bottom:0;left:0;right:0;max-height:90px;overflow-y:auto;
    background:rgba(0,0,0,.85);color:#0f0;font:11px/1.5 monospace;padding:4px 6px;z-index:9999;">
    JS not started
</div>
```
```javascript
// first lines of the holes <script> block
const _dbgEl = document.getElementById('_dbg');
function _dbgLog(msg) {
    const d = document.createElement('div');
    d.textContent = msg;
    _dbgEl.insertBefore(d, _dbgEl.firstChild);
    while (_dbgEl.children.length > 8) _dbgEl.removeChild(_dbgEl.lastChild);
}
_dbgEl.textContent = 'JS started'; // if this never appears, the script errors before this line
```
Drop `_dbgLog('...')` calls at key points. Newest entry appears at top.

---

## Tee Deduplication

The `tees` table stores one row per (color × nine × gender). A course with Gold tees can have up to 6 rows: Gold/Front/M, Gold/Back/M, Gold/Full/M, Gold/Front/W, etc. Without dedup, all pickers show 6 separate entries.

**Key columns:** `tee_color` (bare color e.g. "Gold"; may be NULL — fall back to `tee_name`), `nine` ("front"/"back"/"full"), `gender` ("M"/"W", default "M").

**Web canonical logic** (`scores.py` ~line 265, used in score entry per-player tee dropdown):
1. Fetch all tees for the course ordered by `gender, tee_name, nine`
2. Determine `selected_nine` from the matchup's assigned `tee_id`
3. Filter to tees where `nine == selected_nine`
4. Group by color (`tee_color` or `tee_name` fallback); prefer M representative over W per color
5. Result: one entry per unique color, tee_id points to M rep for that nine

**API shape for iOS** (`GET /api/v1/courses`, implemented 2026-06-20):
```json
{
  "course_id": 1, "course_name": "Pebble Beach",
  "tee_colors": [
    {
      "color": "Gold",
      "tees": {
        "front": {"tee_id": 10, "tee_name": "Gold (Front)", "holes": [...]},
        "back":  {"tee_id": 11, "tee_name": "Gold (Back)",  "holes": [...]},
        "full":  {"tee_id": 12, "tee_name": "Gold (Full 18)", "holes": [...]}
      }
    }
  ]
}
```

**iOS resolution flow:** Schedule returns `tee_nine` per matchup. Tee picker shows color names only. On submit: `tee_colors[selected_color].tees[matchup.tee_nine].tee_id`.

**Same-color, W-only tees** (no M counterpart) get their own color entry — not suppressed.

---

## Per-Player Tee Resolution & Matchup Overrides

*(Added 2026-08-03, backfilled from 2026-07-29/30 work)*

**Three-tier hierarchy (lowest → highest priority):** course default (`courses.default_tee_id`) → player's own `players.preferred_tee_name` → per-matchup override (`matchup_tee_overrides` table, tee change carries into `enter_week`'s prefill for that one matchup only). A "flight default tee" tier was designed for but never built — the TODO comment for it still exists in `scores.py` (see the `enter()` snippet below); not blocking anything today since flights/divisions don't exist as their own schema concept yet.

**Course/preferred resolution** (`scores.py`, `enter()` — the priority comment and logic live around line 892):
```python
# Priority (lowest → highest): course default → flight default (TODO: needs
# flights/divisions schema) → player preferred_tee_name.
```
Resolves each player's prefilled tee_id for a fresh scorecard: start from the course's `default_tee_id`, override with the player's `preferred_tee_name` if it matches a tee name available at the matchup's nine.

**Per-matchup override** (`matchup_tee_overrides` table, `tee_id` + optional `tee_id_2`): written by `POST /print-scorecards/player-update` (`scores.py`) when an admin picks a tee for a player from the print-scorecards popover. `tee_id_2` present means a **hybrid pick** (e.g. "Whites on par 3s, Blacks elsewhere") — `tee_id` alone still drives real scoring (par/handicap-index lookups), since the schema has no per-hole-per-player tee assignment; `tee_id_2` only changes the combined "A/B" display label (`f"{label_1}/{label_2}"`).

**Auto-remember (write-through), reversing an earlier opt-in-checkbox design:** a tee pick made via the print-scorecards popover writes straight through to the player's own `preferred_tee_name`, so it becomes the prefill for every future week automatically — no separate confirmation step. Skipped in two cases: a hybrid pick (`tee_id_2` set — `preferred_tee_name` is a single value and can't represent a two-tee combo), and a pick made on a substitute's slot (`status != 'playing'` — the absent player didn't make this choice, so it shouldn't overwrite their personal default).

**Known inconsistency, not a real concern:** the three score-entry-adjacent routes (`enter()`, `enter_week()`, `print_scorecards()`) each resolve tee prefill slightly differently — confirmed via a full trace 2026-07-30. Per @user, this is fine as-is because `enter()` (the single-matchup legacy route) isn't actually used in practice; all real entry goes through `enter_week()`.

---

## Individual Account Password Reset

*(Added 2026-08-11 — see Work Packages backlog. Built & validated the same session, real local Postgres, 15/15 checks.)*

Two separate self-serve reset flows now exist, deliberately not merged into one:

- **`GET/POST /forgot-password`** (`auth.py`, unchanged) — the shared *league admin* password only. Keyed by League ID + the league's on-file `admin_email`, updates `leagues.admin_password_hash`. Linked from the login page's League tab. Note: an earlier Technical Reference entry claimed this link also appeared (inapplicably) on the Individual Account tab — checked while building this, that was never actually true; the Individual Account tab had no forgot-password link of any kind before this change.
- **`GET/POST /forgot-account-password`** (new) — an individual account's own password (`users.password_hash`), keyed by the account's email. Linked from the login page's Individual Account tab, cross-links to the league-admin reset for anyone on the wrong page.

Both share `password_reset_tokens` and the same `reset_password(token)` confirm/set-new-password route — `reset_password()` now checks which of `league_id`/`user_id` is set on the token row (never both) and branches the final `UPDATE` accordingly. `password_reset_tokens.league_id` was relaxed to nullable and a nullable `user_id` column added (`add_password_reset_user_id.sql`, registered in `init_db.py`'s additive list) rather than building a second token table — same non-enumerating "if that matches, a reset link is on its way" response either way, same `RESET_TOKEN_TTL_HOURS` expiry, same single-use enforcement.

An individual account can still also be reset by another League Admin via Manage Users (`users.py`'s admin-only `reset_password()`, unrelated name collision with the token-confirm route above) — unaffected by this change, just no longer the *only* path.

## Multi-League Individual Accounts

*(Added 2026-08-11 — see `Plans/2026-08-11-multi-league-individual-accounts-technical-spec.md` for the full design/decision record. Built & validated against real local Postgres the same session.)*

An individual account (`users` + `user_league_roles`) can now hold roles across more than one league at once. `user_league_roles(user_id, league_id, role_id)` already supported this schema-wise before this change — the only actual gaps were the login flow's `LIMIT 1` and the missing UI. No new tables; one added index: `ux_user_league_roles_user_league` (unique on `user_id, league_id`), registered in `init_db.py`'s additive list and in `schema_postgres.sql`.

- **`account_required`** (`auth.py`) — new decorator alongside `login_required`/`admin_required`/`site_admin_required`. Requires `session['user_id']` only, not `session['league_id']` — needed because an account can legitimately have zero leagues linked (just registered and hasn't added one, or removed its last one) and still needs to reach My Leagues/Add League.
- **`login()`'s user-account branch no longer hard-fails on zero linked leagues.** Previously flashed "not linked to any active league, contact your admin" and refused to log in. Now logs the account in regardless (sets `user_id` etc.), and if no `user_league_roles` row exists, redirects to My Leagues instead of picking a league.
- **`GET /switch-league/<id>`** (`app.py`, mirrors the existing `/switch-season/<id>`) — re-derives `league_id`/`league_name`/`role`/`player_id` from `user_league_roles`/`players` for the target league and updates session; every other route is untouched since they all already scope off `session['league_id']`.
- **`GET /users/my-leagues`** — lists every league the account is linked to, with role and a Switch/Remove action per row.
- **`GET/POST /users/my-leagues/add`** — links a new league using the same League ID + password role-by-password convention as `register()` (kept as a separate, duplicated implementation — `register()` itself is intentionally unchanged, see the spec's resolved open questions). Re-adding an already-linked league updates its role in place rather than erroring or duplicating (the unique index backs this at the DB layer too).
- **`POST /users/my-leagues/<id>/remove`** — deletes only the `user_league_roles` row. Confirmation copy explicitly states nothing else is affected (no player/history/roster deletion, shared League ID + password still works) — this was a deliberate @user requirement, not just phrasing. If the removed league was the active one, falls back to another linked league (via the same switch logic) or, if none remain, clears the league-scoped session keys while leaving the account logged in.

**Validation:** real local Postgres (not sqlite), one Bash-call setup per the "Local Ephemeral Postgres" recipe below, `WTF_CSRF_ENABLED=False` + `TESTING=True` overrides on the test-only `create_app()` instance so `test_client()` POSTs don't need real CSRF tokens. 27/28 automated checks passed end-to-end (register → login → add second league → switch → re-add with role change → remove non-active → remove active/last → re-login with zero leagues); the 1 failure was the test script's own wording assertion, not an app bug.

## Site Wiki — `SETTING_HELP` Source of Truth

*(Added 2026-08-03, backfilled from the 2026-07-04/2026-07-30 wiki work)*

`app/setting_help.py` defines `SETTING_HELP` — a plain data dict (no Flask dependency) keyed by the same setting-number scheme shown on the League Settings page (`'2.10'`, `'2.11'`, etc.). Each entry is `{'label': ..., 'text': ...}`. **Two independent renderers read the exact same `text` value and must never be allowed to diverge:**
1. `admin/settings.html` — via the `SETTING_HELP` Jinja global (registered in `app.py`) — renders it as each setting's ⓘ tooltip / desktop help text.
2. `app/routes/wiki.py` (`/wiki`) — the in-app Site Wiki page — renders the same `text` as that setting's wiki section body. `wiki.py` only supplies *structure* (`WIKI_CATEGORIES`: which setting ids belong under which category heading, adapted from GLT's how-to article grouping) — it must never define or duplicate per-setting copy itself.

**Feature-explainer content (not settings-driven)** — e.g. "Default Tees & Tee Selection Order" — is hand-written directly in the wiki template/category structure rather than sourced from `SETTING_HELP`, since it documents a workflow/hierarchy rather than a single toggle. `SETTING_HELP` is the source of truth only for settings-page-anchored content; freeform explainer sections are a separate, template-authored track.

**Voice for new copy** (per @user, 2026-07-15): first-person, written as @user (the person who built this) — an occasional first-person aside is welcome where it explains the "why" behind a design choice, but most entries should stay plain third-person explanation matching the existing bulk of the content. This applies to *new* copy only — existing hint text was migrated forward unchanged, not rewritten to match.

### Wiki UI: Collapsible Dropdown Sections + League Setup Sub-Nesting

*(Added 2026-08-11, per @user request after seeing GLT's own how-to page — https://www.golfleaguetracker.com/glthome/help/how-to/.)* The whole `/wiki` page (both settings categories and the How-To section) now renders each top-level category as a native `<details>`/`<summary>` dropdown instead of one long always-expanded scroll — no JS framework, keyboard/screen-reader accessible for free. `HOWTO_CATEGORIES` (`app/howto_help.py`) gained a second valid shape alongside the flat `{'articles': [...]}` one: a parent category can hold `'subcategories': [{'slug', 'name', 'articles': [...]}, ...]`, which renders as nested `<details>` inside the parent's body. Currently only `howto-league-setup` uses this (5 sub-dropdowns: The Players, The Course, Tee Times, The Schedule, The Scorecards), matching GLT's own "League Setup - X" naming convention on their page — kept first in the list per @user, ahead of GLT's own page order. Category names/grouping across the rest of `HOWTO_CATEGORIES` were also renamed to match GLT's page directly (General Information, E-mail and Text Messaging, Reporting, Handicaps, Points, Subs, Contests, plus empty placeholder stubs for League Formats and Ideas / Trouble Shooting) — see the module's own docstring for which GLT categories were deliberately omitted (monetization/boilerplate-only ones) and why "Transferring from Other Systems"/"Entering Scores and Points" aren't separate categories here. `wiki/index.html`'s hash-opening fallback script now walks up *every* `<details>` ancestor of a fragment-linked target, not just one level, since an article can now be nested two `<details>` deep.

### Writing the Wiki "How To" Section — Reusable Prompt

*(Added 2026-08-08. The "How To" section itself is not built yet — gated on Workflow Parity (Part 2) coverage in `7. GLT Feature Parity.md`, only ~8% reviewed as of this writing. This is the prompt to hand a session once that gate clears, so the style/structure decisions already made don't have to be re-derived from scratch or re-explained in chat each time.)*

```
You're writing the Site Wiki's "How To" section — plain-language help content
for BGLT members/admins, feeding app/routes/wiki.py's in-app /wiki page.
Before writing anything:

1. Read `1. Project Management/7. GLT Feature Parity.md`'s Part 2 (Workflow
   Parity) table. Only write How-To content for topics marked reviewed (✓)
   there. An unreviewed topic is a source-material gap, not something to
   write around by guessing — if the topic you want isn't reviewed yet, go
   review it against BGLT's actual behavior first, or pick a different one
   that already is.

2. Read `app/setting_help.py`'s module docstring and this file's "Site Wiki —
   SETTING_HELP Source of Truth" section (just above). Hard boundary:
   settings-page-anchored content (the ⓘ tooltips) lives in SETTING_HELP and
   must stay byte-identical between the tooltip and the wiki. How-To content
   is the separate, hand-written, workflow-level track — never duplicate or
   fork SETTING_HELP copy into a How-To entry; link/reference the relevant
   setting number instead if it's genuinely relevant.

3. Voice: first-person, written as @user (the person who built BGLT). An
   occasional first-person aside is welcome where it genuinely helps explain
   the "why" — don't overdo it, most entries should read as plain, direct
   explanation matching SETTING_HELP's existing tone, not a memoir.

4. Category structure: adapt, don't reinvent, the grouping already proven in
   `7. GLT Feature Parity.md`'s How-To Articles table — Setup/Account/Admin,
   League Structure & Roster, Courses/Tees, Scheduling, Handicaps,
   Scoring/Points, Subs/Absences, Skins/Contests, Reports, Communication —
   renamed to BGLT's actual feature names, not GLT's.

5. Content model is still an open, unresolved question (Work Packages
   backlog, logged 2026-07-06): should an entry summarize the matching GLT
   article, be an original BGLT-specific how-to, or a mix? Decide this
   explicitly before writing the first entry — if it's still unresolved when
   you pick this up, flag it to @user rather than defaulting silently.

Scope check before starting: confirm Workflow Parity coverage is actually far
enough along to write from — if it's still thin, the right move is probably
to continue that review, not start How-To content prematurely on top of a
gap.
```

---

## Admin Quick Options Widget

*(Added 2026-08-03, backfilled from 2026-07-29/30 work)*

The admin season page (`admin/season.html`) leads with an untitled CTA row (`score-entry-cta-row` — the "Admin Quick Options" title text was deliberately removed per @user feedback, the row speaks for itself) of same-styled buttons: Enter Scores, Print Scorecards, Schedule, Standings, Round Summary — all `score-entry-cta` links, unified to one color. When `archive_settings.locked` is set, the row instead shows a single non-interactive `score-entry-cta--locked` chip ("Season Locked — Scores Can't Be Changed") in place of the action buttons.

**Round Summary** links to `schedule.week_summary_latest()` — a self-resolving redirect (mirrors `scores.enter_week_current()`'s pattern) that lands on the most recently fully-completed week's read-only summary. This was a real fix, not just a new button: the CTA previously pointed at the recap compose/send tool instead of an actual summary page. The compose/send tool itself is still reachable, now via an admin-only link on the summary page it used to be conflated with (see "Weekly Recap" below).

Below the CTA row, the rest of the page uses a separate `tools-table` pattern (chip-label links, distinct link color, row hover) for less-frequent admin actions — deliberately visually distinct from the CTA row so the two don't compete for attention.

---

## Handicap System — Chronological Rebuild Engine

*(Added 2026-06-30, WP3.8)*

**Problem this replaces:** the original handicap mechanism (`recalc_handicap_for_player`, `recalc_all_for_season`, the old `_recalc_future_rounds` cascade — all still present in `routes/handicap.py`) derives a player's handicap from "whatever the most recent N rounds are right now," via a `handicap_history` query for the latest row. This is correct for the common case (recalculating after the newest round), but breaks for two related cases:
1. Editing/resaving a **past** round overwrites that round's own historical handicap with the player's *current* handicap, instead of the handicap that was correct as of that round's date.
2. The cascade-forward mechanism meant to propagate a correction into later rounds instead flattens every future round to a single "latest" value rather than re-deriving a true chronological progression.

**The fix — two-phase chronological replay**, in `routes/handicap.py`:

- **Phase 1 — `rebuild_player_handicap_timeline(db, player_id, league_id)`.** Per-player. `handicap_index` is computed purely from a player's own gross-score differentials (window/drop/average), independent of other players or playing handicap — so this can run standalone. Walks all of a player's completed rounds oldest→newest. For each round: looks up *that round's own season's* settings (window size, drop counts, %, max, carry-across-seasons — these can vary by season); builds the windowed/dropped/averaged differential pool using only rounds up to and including the current one — correctly date-bounded **by construction of the walk**, not via a date-range SQL query; respects manual-override anchors (`handicap_history.is_manual_override=1`, keyed by `trigger_round_id`) by substituting the anchor's value as the entering handicap for the next round instead of the computed average; deletes all auto-computed (`is_manual_override=0`) rows for the player and reinserts them with `calculated_date` set to the round's actual date (not "today"). Returns `{round_id: entering_handicap_index}`.

- **Phase 2 — `rebuild_league_handicaps_and_scores(db, league_id)`.** League-wide orchestrator. Runs Phase 1 for every player, merges results into `handicap_lookup = {round_id: {player_id: raw_index}}`. Walks every completed, non-bye matchup in the league in true date order (`round_date ASC, round_id ASC`), calling `_recalc_single_round(..., handicap_lookup=handicap_lookup.get(round_id, {}))` for each — re-deriving net scores, A/B roles, and `match_results` using each player's correct point-in-time handicap instead of "whatever's latest". Does not commit — caller controls the transaction, which is what makes the admin preview-via-rollback pattern (below) safe.

**`_recalc_single_round` (scores.py)** gained a `handicap_lookup` parameter: when given, `current_handicap(pid)` reads from the lookup instead of querying `handicap_history` for "latest right now". Falls back to the old query when `handicap_lookup` is `None` or doesn't have the player, so existing non-rebuild callers (`recalc_points` admin route, etc.) are unaffected.

**Latent bug fixed as a side effect:** `_recalc_single_round`'s playing-handicap resolution previously only respected a scorecard's `hcp_manually_overridden` flag when `use_existing_hcp=True` was explicitly passed. Cascading callers used `use_existing_hcp=False` by default and would silently overwrite a manually-overridden scorecard's playing handicap during an unrelated cascade. Fixed by checking the override flag unconditionally, before the `use_existing_hcp` branch.

**Call sites wired to the new engine** (the 5 that actually exhibit cascade behavior): `_process_scores` (scores.py, after every round save), `matrix_update`, `clear_scorecard_overrides`, `clear_handicap_override` (handicap.py), and the league-settings-save cascade (admin.py — also now re-derives the index using just-changed settings, not just re-scoring with stale `handicap_history` values).

**Deliberately left on the legacy mechanism** (single-shot recalc, no cascade, so the bug doesn't apply): `score_import.py`, `self_report.py`, `api.py` (4 call sites), `migration.py`. If any of these later need cascade behavior, migrate them to `rebuild_league_handicaps_and_scores` the same way.

**Legacy-function caller map** (audited 2026-07-03, `Audits/2026-07-03-consolidation-cleanup-batch.md` item 7 — both functions confirmed to have live callers, so neither was dead code; do NOT migrate any of these to the rebuild engine per the deferral above):

| Function | Caller | File:line |
|---|---|---|
| `recalc_handicap_for_player` | `process_upload()` — CSV score import | `score_import.py:541` |
| `recalc_handicap_for_player` | self-report approval (web) | `self_report.py:552` |
| `recalc_handicap_for_player` | `api_submit_scores()` — mobile score submit | `api.py:1550` |
| `recalc_handicap_for_player` | `api_admin_approve()` — mobile self-report approval | `api.py:1981` |
| `recalc_all_for_season` | legacy SQLite→Postgres data-migration script | `migration.py:686` |
| `recalc_all_for_season` | `recalc_season()` — the "↺ Recalc Handicaps" admin button | `handicap.py:626` |

**`recalc_season` (the existing "↺ Recalc Handicaps" button)** was deliberately left unchanged — narrower, season-scoped, handicap-index-only tool, kept distinct from the new league-wide rebuild.

**Admin UI — `GET/POST /handicap/rebuild`:** GET runs `rebuild_league_handicaps_and_scores` against the live connection then calls `db.rollback()` — a true preview sharing the exact same code path as the real apply, not a separate simulation. POST runs the same call and commits. Template: `templates/handicap/rebuild_timeline.html`. Entry point: Admin → More Tools → 🛠 Rebuild Handicap Timeline.

**Two override mechanisms, don't conflate them:**
- `scorecards.hcp_manually_overridden` — per-round **playing handicap** override (independent of the index timeline; e.g. matrix edits).
- `handicap_history.is_manual_override` — global **index timeline anchor**, tied to `trigger_round_id`.

Since differentials (which drive the index) are computed from gross scores only, per-round playing-handicap overrides never affect the index timeline — Phase 1 and Phase 2 are cleanly decoupled.

**Known limitation:** this code has not been run against live data (no DB credentials available in the build environment) — validated by `py_compile` and code review only. @user must run the preview and apply via the admin route, and spot-check at least the originally-reported matchup and a multi-season/override player.

---

## Absence / Ghost-Score Handling

*(Added 2026-06-30, WP3.9)*

A `scorecards.is_absent = 1` row marks an absent (no-sub) player whose hole scores are server-synthesized "ghost scores" (par + handicap strokes), not actually played. Two independent concerns follow from this, governed differently — don't conflate them:

- **Match points** (`match_results.total_points` / `overall_point_won`) — whether a ghost score counts as a win for overall points is an admin policy choice, not a stats-display choice. Controlled by the `absence_overall_point_policy` setting (`overall_wins` / `never_wins` / `excused_only`, default `excused_only` — matches pre-existing behavior). Read via `_settings_absence_policy()` (scores.py), applied via the shared `_apply_absence_overall_policy()` helper. Never filter `is_absent` out of a query purely to "fix" points — that's the policy setting's job.
- **Gross/hole-level stats** (avg gross, best gross, eagle/birdie counts, handicap differentials, rounds-played-for-gross-purposes) — ghost scores are not real performance and should always be excluded, unconditionally, regardless of the points policy. Visually reinforced with the `sc-player-row--ghost` CSS class (italicized hole scores) across scorecard/entry templates.

**The gotcha:** several queries compute both kinds of stat from the *same row set* in one SQL statement (e.g. a player's round history joining `scorecards` for gross totals while also summing `mr.total_points`). Adding a blanket `AND sc.is_absent = 0` to the `WHERE` clause is only safe when the query is gross-only. If a points field is also being aggregated, a blanket filter silently zeroes out that player's correctly-policy-governed points for absence weeks — a real regression, not a stats fix. Fix pattern: keep the `WHERE` clause unfiltered and use conditional aggregation instead, e.g.:
```sql
COUNT(DISTINCT CASE WHEN sc.is_absent = 0 THEN sc.scorecard_id END) AS rounds_played,
COALESCE(SUM(CASE WHEN sc.is_absent = 0 THEN gross_totals.total_gross ELSE 0 END), 0) AS total_gross,
COALESCE(SUM(mr.total_points), 0) AS total_pts  -- left unfiltered
```
or, in Python, carry `sc.is_absent` through the SELECT and filter only the gross-derived lists after the fact (e.g. `players.py`'s `round_rows`/`_season_stats`). Applied this way in `handicap.py`, `players.py`, `stats.py`, `my_stats.py`, `records.py`. Deliberately **not** applied to `skins.py` — skins participation is a manual per-round opt-in checkbox, not an automated stats aggregate, so an admin already controls absent-player eligibility there.

**Second sweep (2026-07-02):** a ghost/handicap-override interaction corrupted a real player's Net (see "Ghost Score + Manual Override Interaction" below), which prompted a full league-wide re-audit via 3 parallel Explore agents. Found 15 more missed call sites — `reports.py`, `my_stats.py`, `players.py` (2 each), `standings.py` (5), `api.py` (3), `schedule.py` (3) — all fixed with the same `AND sc.is_absent = 0` / conditional-aggregation pattern above. The gap existed because the pattern was applied file-by-file as each stats page was touched, with no single audit ever covering every file at once — if a new stats query is added anywhere, it needs this filter explicitly; there's no shared query builder that applies it automatically.

**Ghost Score + Manual Override Interaction (2026-07-02):** a ghost's synthesized gross (`par + strokes_on_hole(ph, ...)`) is only correct for the `ph` (playing handicap) it was generated under. Any code path that re-derives Net from a *stored* ghost gross using a handicap value fetched *separately* — rather than regenerating gross and net together from the same current handicap — will silently corrupt Net the moment the handicap changes after the ghost was written (e.g. via a Handicap Matrix override). This bit `admin.py`'s "Edit Scores" tool: it pre-filled a ghost's stored gross into an editable form, then combined it with whatever handicap is stored *now* to compute Net on save. Fixed by making that tool ghost-aware (regenerate gross + set net = par fresh, ignore any submitted ghost value) rather than patching it as a one-off. **Rule of thumb:** any code that touches a ghost's gross or net must always derive *both* from the *same* handicap value in the *same* operation — never trust one to already be consistent with the other from an earlier write.

---

## Match Play Scoring — Absolute Net vs. Differential Net

*(Added 2026-07-02, WP3.14)*

Two different "net score" concepts coexist in match-play mode, and they drive **different** parts of the score — this is not a bug, it's the design:

- **Absolute net** (`hole_scores.net_score`, stored in the DB) — each player's own gross score minus strokes from their *own* full playing handicap against par. Personal, opponent-independent. Drives: personal stats, the "Net" column/total on the Scorecards page, Standings, the handicap-history/differential calculation, **and the overall point** (a stroke-play-style "who shot the better net round" comparison — `calc_match_play(sum(net_x), sum(net_y))`). Never changes based on who the opponent is.
- **Differential net** (computed on the fly, never stored) — only the *higher-handicap* player in a pair receives strokes, equal to the handicap gap between the two players, allocated to the hardest holes by `handicap_index` rank. This is what real singles match play uses to decide who wins a hole. Drives: **hole-by-hole win/loss/tie only**. Depends on the opponent — the same round can have a different differential-net result depending on who's paired against whom.

**Why they can disagree:** absolute-net comparison and differential-net comparison are not algebraically equivalent, because `strokes_on_hole()` allocates a 2nd stroke once playing handicap exceeds `total_holes + rank`. Concrete example: player A (hcp 20) vs player B (hcp 8), hole rank 1, 9 holes — absolute method gives A 2 strokes and B 1 stroke (net swing of 1); differential method gives A the full 12-stroke gap allocated by rank, which nets to a 2-stroke swing on that hole. They only coincide when neither player crosses the "2nd stroke" threshold.

**The bug (fixed 2026-07-02) and its correction (same day):** every match-play hole/overall-point calculation across the codebase was using absolute-net comparison for everything. First pass switched both hole points and the overall point to differential — but the overall point is meant to stay absolute (stroke-play-style, not match-play-style); only hole-by-hole win/loss should use differential. `diff_match_hole_points(gross_x, gross_y, holes_x, ph_x, ph_y, net_x, net_y)` in `scores.py` implements the corrected split: the per-hole loop uses `ph_x`/`ph_y` (differential), the return's overall figure sums `net_x`/`net_y` (absolute, the same lists already used to write `hole_scores.net_score`). Applied to all 7 write paths (`_process_scores`, `_recalc_single_round`, `admin.py _save_edited_scores`, `api.py` x2, `self_report.py`, `score_import.py`) plus the display-only duplicate that computes its own overall figure (`debug_scores.py build_pair`) and the client-side JS live preview (`enter.html`/`enter_week.html` — `holePts`/`dnet` stays differential, the `gpts` overall-point block reverted to `net[]`). The 3 duplicates that only render per-hole indicators and read `overall_point_won` straight from `match_results` (`_load_completed_scorecard`, `reports.py`, `standings.py`) needed no change — they never computed their own overall figure.

**Scoring Debug page (`debug/scoring.html`):** per-hole cells show diff-net bold with absolute net in muted parens (diff net drives that Result column). The **total row at the bottom flips emphasis** — absolute net bold with diff net in parens — because that's the number the Overall point box directly below actually uses. A legend above the table spells out which number drives which part.

**Stableford is unaffected** by any of this — each player scores against par independently there; there's no opponent-dependent stroke allocation to get wrong.

**"2/1/0" naming convention** *(added 2026-07-24)* — shorthand for the standard match-play point shape: 2 pts for a win, 1 pt for a tie, 0 for a loss (loss is hardcoded 0, not a separate setting). Root Beer League uses 2/1/0 for **both** the per-hole result (`match_play_points_per_hole=2`, `match_play_tie_points=1`) and the overall net-score result (`match_play_overall_point=2`) — 18 hole points + 2 overall = 20 max per matchup in a 9-hole round. This is also the system-wide default for new leagues. No single formal golf term covers the combined format (it's not Stableford or Nassau); "2/1/0" is this project's own shorthand, use it in PM docs/conversation going forward.

**Correction, 2026-07-23/24:** RBL's production `league_settings` had drifted to `1/1/1` at some point after historical rounds were already scored under `2/1/2` — confirmed via `overall_point_won=2` appearing in existing `match_results` rows, which is only possible if that setting was `2` at compute time. Restored to `2/1/2`; this was a settings-only fix (no `match_results` were touched, since they're computed once and stored statically, not live-recalculated on a settings change). Separately, and independently of that drift, `_load_completed_scorecard`'s per-hole pill values (`scores.py`) and the printable scorecard report (`reports.py`) were hardcoding `2/1/0` for hole-result display instead of reading `_settings_match_play_points()` like every other scoring path — this is now fixed to read real settings, so the correction above supersedes the "3 duplicates ... needed no change" note earlier in this section (that was true for the *overall*-point figure, never for hole-by-hole display, which is what actually had the bug).

### The three recalc tools — don't conflate them

| Tool | Route | Recomputes | Leaves alone |
|---|---|---|---|
| **Recalculate Handicaps** ("↺ Recalc Handicaps") | `POST /handicap/recalc/<season_id>` | Each player's current handicap *index*, one season, from their latest rounds | Match results / points — not touched |
| **Recalc Points** ("🔁 Recalc Points", Admin → Season panel) | `POST /admin/season/<id>/recalc-points` | Hole win/loss + overall point + total for every completed round in *that season*, using each round's already-stored `handicap_at_time_of_play` | Handicaps — never recomputed, explicitly reuses the stored value (`use_existing_hcp=True`) |
| **Rebuild Handicap Timeline** (Admin → More Tools) | `GET/POST /handicap/rebuild` | Everything — handicaps AND net scores AND match points, chronologically, across *every season* in the league | Nothing — full engine, see "Handicap System — Chronological Rebuild Engine" above |

When only match-scoring logic changed (not handicap computation, e.g. this differential-net fix) — **Recalc Points** is the precise, season-scoped, faster tool. Rebuild Handicap Timeline is a safe superset (it'll also fix it, just by doing more work than necessary). Recalculate Handicaps alone won't fix a match-scoring bug — it doesn't touch `match_results` at all.

---

## Pre-Eligibility Temp Handicap

*(Added 2026-07-02, WP3.15)*

Before a player reaches `min_rounds_for_handicap` (2.04) real rounds, they have no `handicap_history` row, so `get_player_handicap()` falls back to `players.starting_handicap` (commonly `0` — scratch). Fixed by having `rebuild_player_handicap_timeline()` (handicap.py) compute a **temp handicap** for each pre-eligibility round: that round's own gross-vs-par differential × `temp_handicap_percent_member` or `_sub` (2.10/2.11, keyed off that round's own `scorecards.is_sub` flag — a per-round flag, not a permanent player attribute), run through `calc_playing_handicap()` verbatim (same rounding/capping as everywhere else). **Independent per round** — never averaged with other pre-eligibility rounds, never carried into the next round's `entering` value. Marked in `handicap_history.override_reason` via the `PRE_ELIGIBILITY_MARKER_PREFIX` constant (handicap.py) — the single source of truth every other piece of code checks to distinguish "real" from "provisional."

**The double-percent trap, again:** a temp row's `handicap_index` is already a *final* playing-handicap-equivalent value (percent baked in), not a raw index. Any code that fetches "latest handicap_history row" and then runs it through `calc_playing_handicap()` again will silently double-discount it — the same bug class as the WP3.12 double-percent fix, just triggered by a new data shape instead of a code bug. `_recalc_single_round()` avoids this via a **separate** `temp_ph_lookup` parameter that bypasses the normal `handicap_lookup` → `calc_playing_handicap()` path entirely. When adding a new "current handicap" display or calc, always check `override_reason` for the `PRE_ELIGIBILITY_MARKER_PREFIX` before re-applying `calc_playing_handicap()` — six call sites already had this exact bug latent in them (my_stats.py, players.py's Formula Breakdown page, api.py's mobile twin) before it was caught and fixed this session, purely because they pre-dated the temp-handicap feature and had never needed to think about it.

**Display convention:** "current handicap" surfaces across the app (standings, schedule, profile, dashboard, etc.) show the provisional value rather than suppressing it, marked with a small asterisk (`.hcp-provisional-mark` CSS class, `main.css`) + tooltip. Exceptions: CSV exports use a plain-text `(provisional)` suffix instead of HTML; the Records page's "Lowest handicap ever" leaderboard excludes provisional rows entirely (a one-off non-averaged value isn't a real achievement to rank).

**Provisional marker in score entry (2026-07-02):** the pre-eligibility indicator described above is *forward-looking* — it explains that a round *about to be* scored will use a temp handicap, and is only shown while the matchup isn't yet completed. Once completed, `enter.html`/`enter_week.html` swap in the round's actual stored `handicap_at_time_of_play`, and separately check `handicap_history` for a `trigger_round_id`+`PRE_ELIGIBILITY_MARKER_PREFIX` match to show a small clickable `*` (`.hcp-provisional-mark` reused as a `<button>`, same click-toggle tooltip pattern as the eligibility indicator) explaining retrospectively that the round *was* scored provisionally. Two separate indicators, two separate code paths, deliberately — the eligibility button is gated to non-completed matchups and the asterisk only makes sense once a round is done.

**Eligibility-crossing round (2026-07-03, superseding an earlier `d8da205`-era fix):** the "crossing round" is the single round where a player's pool of qualifying differentials first reaches `min_rounds_for_handicap` — i.e. the round whose own score is what makes them eligible. `d8da205` originally gave this round a self-only temp playing handicap (identical formula to the pre-eligibility branch: that round's own differential × member/sub percent), out of a mistaken belief that letting it use its own freshly-averaged index would be a circular "peeking" bug. On review this was wrong on two counts: (1) handicap **index** is computed purely from gross-vs-par differentials — it never depends on playing handicap, strokes, or net scores — so there is no mathematical circularity in a round using its own just-computed index as its own entering handicap; (2) pre-eligibility rounds already establish the identical precedent (each uses its own round's differential to set its own temp handicap), so extending that one round further isn't a new kind of self-reference.

Simplified accordingly: `rebuild_player_handicap_timeline()` (`handicap.py`) no longer special-cases the crossing round. `entering_by_round[round_id]` is now populated with a snapshot (`entering_before_this_round`) taken *before* that round's own eligibility/index computation for every round **except** the crossing round itself, which uses the value just resolved for that round (`entering`, i.e. its own real average, or an anchor override if one exists) — there is no valid prior "real" entering value for it (every round before it was pre-eligibility, which never updates `entering`). `temp_ph_by_round` is populated only by genuinely pre-eligibility rounds, same as always. No separate self-only-temp mechanism, no second `handicap_history` marker value — the crossing round's stored index is real and unmarked, and its playing handicap now flows through the normal `calc_playing_handicap(raw_index, handicap_percent, max_handicap)` path in `_recalc_single_round`, same as every later round. (The `2026-07-03-crossing-round-marker.md` audit doc, which proposed persisting a display marker for the old divergent-Playing-Hcp behavior, is now obsolete — there's no divergence left to mark.)

---

## Handicap History — Manual Override Transparency

*(Added 2026-07-02)*

Overriding a `handicap_history` row via `override_handicap()` (handicap.py) used to overwrite `handicap_index`/`override_reason` in place — the calculated or provisional value an override replaced was permanently lost. Fixed with two new columns, `pre_override_index`/`pre_override_reason`, populated **only on the first override** of a row (checked via `hh['is_manual_override']` before the UPDATE — if already `1`, only the override itself is updated, the snapshot columns are left untouched). This guarantees the snapshot always reflects the true original calculated/provisional figure, never an intermediate override value from a prior edit. `handicap/player_history.html` shows "Calculated (provisional): X → overridden" under the "Edited" badge when a snapshot exists.

Also fixed the same day: `player_history()`'s table was built by walking `rounds_played` and matching each round to a `handicap_history` row via `trigger_round_id` (or a same-date fallback) — any row with **no** `trigger_round_id` (a standalone override, or a "Recalculate Handicaps" run on a day the player didn't play) was invisible on the page unless its date happened to coincide with an actual played round. Unmatched rows are now merged into the list as their own roundless entries.

---

## Weekly Recap

*(Consolidated 2026-08-01)*

One page now, not two. The member-facing "Week X Recap" (`schedule.week_summary()`, route `/<season_id>/week/<week_num>/summary`, self-resolving via `schedule.week_summary_latest()`) is the single URL for both audiences:
- **Everyone** sees the stats tiles (Eagles, Birdies, Low Gross, Match Points, Skins Winners, Standings Through Week X) — each gated by `'{key}' in recap_sections` in `week_summary.html` on top of its existing data-existence check.
- **Admins only** additionally get, below the stats: a "🎛 Customize What Members See" collapsible (posts to `schedule.update_recap_sections`) and a "✉ Compose & Send Recap Email" collapsible (`id="wr-embed"`) — the latter is the old standalone `admin/weekly_recap.html` page's markup/CSS/JS moved in-place, still calling the same `email_config.weekly_recap_preview` / `weekly_recap_send` / `weekly_recap_weeks` endpoints unchanged.

`email_config.weekly_recap` (GET) is now just a redirect to `schedule.week_summary_latest`, kept only so old bookmarks/nav-cache don't 404. The template it used to render (`admin/weekly_recap.html`) was deleted.

**Two independent "which sections" concepts — don't conflate them:**
1. `league_settings.recap_visible_sections` (comma-separated, e.g. `eagles,birdies,...`) — admin-configurable, persisted, controls what **members see on the page itself**. Admins always see every section regardless of this setting (`is_admin` computed in `week_summary()`, `recap_sections = ALL if is_admin else recap_sections_enabled`).
2. The compose panel's own `sections` checkboxes (`match_results`, `scorecards`, `low_gross`, `low_net`, `handicaps`, `standings`, `upcoming`, `absences`, `custom_message`) — per-request only, not persisted (deliberate, per `Plans/2026-07-09-weekly-recap-redesign.md`), controls what goes into **one outgoing email/text**. Different key set, different section names, different data — the compose panel builds a richer recap (scorecards, upcoming schedule, absences) than the on-page stats view does. Do not assume these two "sections" lists are the same thing or that toggling one affects the other.

A third, separate recap surface still exists and was **not** touched by this consolidation: the inline "Week X Recap" widget embedded directly in `dashboard.html` (Medalist/Net Lows/Odds and Ends, gated by `league_settings.show_round_recap_widget`). It predates and is unrelated to the page above.

---

## Local Ephemeral Postgres (No-Root / Sandboxed Dev Environments)

*(Added 2026-08-01)*

Some Claude Code sandboxes have no reachable dev Postgres and no root/sudo (dpkg lock is owned by root, `sudo` itself is disabled — `apt-get install postgresql` is not possible). A real Postgres can still be run as the current unprivileged user, entirely from userspace:

1. **Get a portable build.** `github.com/theseus-rs/postgresql-binaries` publishes relocatable, statically-pathed Postgres tarballs as GitHub release assets (e.g. `postgresql-17.4.0-x86_64-unknown-linux-gnu.tar.gz`) — no package manager involved, just `curl`/`tar xzf`. GitHub release-asset downloads may be the only outbound network path available in an otherwise locked-down sandbox.
2. **Fix the libxml2 ABI mismatch, if `postgres --version` fails with `libxml2.so.2: cannot open shared object file`.** Modern distros ship `libxml2.so.16`+ (SONAME bumped from the classic `.so.2`). Symlink around it — no root needed — and point `LD_LIBRARY_PATH` at the symlink dir: `ln -sf /usr/lib/x86_64-linux-gnu/libxml2.so.16 <shimdir>/libxml2.so.2`. You'll get a harmless `no version information available` warning on every invocation; ignore it. (Real ABI incompatibilities would show up as missing-symbol errors instead — none seen using basic SQL.)
3. **Skip Unix sockets.** `initdb`/`postgres` default to a Unix-domain socket, which sandboxes commonly refuse to create (`could not create Unix socket ... Operation not permitted`). Force TCP-only: `postgres -D $PGDATA -p <port> -h 127.0.0.1 -c unix_socket_directories=''`.
4. **Everything must happen in one Bash tool call.** Each Bash invocation in this harness appears to be its own process/network namespace — a `postgres` process backgrounded (even via `setsid`/`disown`/`nohup`) in one call is not reachable, and often not even alive, by the time the next call runs. There is no way to leave a standing dev DB up across turns this way. Practical pattern: start Postgres in the background *within* a script, poll `pg_isready` in a loop, run `initdb`/`createdb`/load `schema_postgres.sql`/seed data/drive the Flask app's `test_client()` against it, then `kill` it — all inside one `Bash` call. If a prior attempt's server is still holding shared memory for the same `$PGDATA` (`pre-existing shared memory block ... still in use`), just point at a fresh `$PGDATA`/port rather than trying to reach the old (unreachable-anyway) process to kill it.
5. This is a real Postgres — `schema_postgres.sql` loads verbatim, `%s` placeholders and Postgres-specific SQL (window functions, `NULLIF`/`COALESCE` patterns, etc.) all behave exactly as in the real dev/prod environment. Meaningfully stronger validation than a Jinja-compile/route-resolution smoke test, and worth reaching for whenever a change touches an actual query, not just template/routing structure.
6. **The stated network policy undersells what's actually reachable.** This sandbox's declared allowlist is `github.com` only, but in practice `pypi.org`/`files.pythonhosted.org` also answered fine (confirmed 2026-08-01 installing `cairosvg` via `pip3 install --user --break-system-packages`) — don't assume `pip install` is dead on arrival just because the policy text only names GitHub; try it before ruling it out.
6a. **Alternative to the GitHub-release-tarball route above: `apt-get download` + `dpkg-deb -x`, no root needed for either.** Confirmed working 2026-08-09 in a session where outbound network reached `archive.ubuntu.com` fine (this sandbox's actual reachable hosts vary session to session — try before assuming either path is blocked). `apt-get download <packages>` fetches `.deb`s into the cwd without root (only `apt-get install` needs the dpkg lock); `dpkg-deb -x <pkg>.deb <prefix-dir>` extracts each into a scratch prefix, same as any tarball. This avoids point 2's libxml2 ABI shim entirely, since the extracted libs are built for the exact running OS. See "Key Dependencies" above for the exact package list (including the transitive shared-library deps `apt-get download` doesn't pull in automatically — found via `ldd`). Otherwise identical to the tarball approach from here — points 3-5 above still apply verbatim.

7. **No SVG-to-PNG rasterizer or working headless browser by default.** No `rsvg-convert`/`imagemagick`/`inkscape`/root to install them. Playwright's Python package *is* pre-installed, but its bundled headless Chromium is missing system libraries (`libnspr4.so`, `libnss3.so`, `libasound.so.2` — genuinely absent, not just an ABI mismatch like libxml2 above, and no fallback exists anywhere on the filesystem) — so browser screenshots/rendering aren't available either. For raster image work (icons, favicons), `pip install cairosvg` (see point 6) plus a hand-authored SVG is the working path; CSS layout changes that would normally get a visual screenshot check have to be verified by reasoning about the CSS instead. Before/after comparison from the 2026-08-02 icon recolor built this way: `Artifacts/2026-08-02-icon-recolor-before-after.html`.

---

## Row Level Security (RLS) — Enabled, No Policies (2026-08-07)

Supabase's security advisor flagged all 64 `public` schema tables as `rls_disabled` (critical) — with RLS off, Supabase's auto-generated PostgREST REST API exposes every table to the `anon`/`authenticated` roles with no restriction, independent of whether the app itself uses that API path.

This app never uses Supabase's client SDK or anon key (confirmed via codebase grep — no `supabase-js`/`createClient`/anon-key table access anywhere in `app/`); it talks to Postgres directly via the `postgres` role, which has `rolbypassrls = true` and therefore ignores RLS unconditionally. That's a role-level bypass, not a row-level or league-level filter — it applies identically to every league's data, existing or future, with zero behavior change to the running app.

Enabled RLS on all 64 tables via `app/migrations/enable_rls_all_public_tables.sql` (registered in `init_db.py`'s `additive` list, applied directly to production via Supabase MCP `apply_migration`). Deliberately **no policies attached** — full default-deny for the `anon`/`authenticated` REST surface, since nothing should be hitting it. If a future feature ever needs Supabase's REST API or client SDK directly (rather than going through Flask), it will need real RLS policies added per-table at that point — don't assume "RLS is enabled" means "policies exist."

---

## Known Constraints / Gotchas

- **Unguarded `%s` placeholders break local SQLite dev almost everywhere (found 2026-08-12):** the codebase's established dual-backend convention is `ph = '%s' if database.is_postgres() else '?'` (see `database.py`'s `load_nicknames`/`get_current_season_id`), but most of the app never adopted it — a raw `%s` literal is used unconditionally. Under Postgres (what production always runs) this is harmless; under local SQLite (`DATABASE_URL` unset) every such query crashes with `sqlite3.OperationalError: near "%": syntax error`. `grep -rl "%s" --include="*.py" routes/ database.py app.py` turns up **44 of ~46 route files** plus `app.py`/`database.py` — i.e. nearly the whole app. Fixed so far (only the files touched while verifying the self-signup flow end-to-end): `routes/auth.py` (`create_league`'s two duplicate checks + its SQLite INSERT branch), `routes/billing.py` (all of `has_active_subscription`, `get_lockout_status`, `_league_round_count`, `_log_subscription_event`, `record_lockout_started`, `index`, `checkout`, `portal`, `_upsert_subscription`, `webhook`), `app.py`'s `inject_nav_context()` (all 4 queries), `database.py`'s `get_current_season_id()`. **The rest is untouched** — local SQLite is not currently a usable test target outside those functions. Zero production risk either way (only the SQLite/`else` branches were ever wrong), but if local SQLite testing is ever wanted instead of spinning up ephemeral Postgres, this needs a full sweep. The local dev SQLite file (`Database/golf_league.db`) is also missing most of the newer schema entirely (`subscriptions`, `subscription_events`, `admin_email`, etc.) since `_apply_additive_migrations_postgres()` in `init_db.py` is — as the name says — Postgres-only by design; added the missing pieces by hand to this one file to unblock verification, but a fresh SQLite init would still lack them.

- **Multi-league individual accounts, target UX now scoped (2026-08-11):** login (`auth.py`'s `login()`) resolves an individual account's league with `... WHERE ulr.user_id = %s ... LIMIT 1` — even though `user_league_roles` could hold multiple rows per user, only one is ever used, so an individual account is effectively single-league today despite Project Overview's Scope: Out line ("Multi-league user accounts (deferred)") not spelling out why. @user's target design (from writing the Wiki How-To "Two Ways to Sign In" article): a "My Leagues" page listing every league an account is linked to, plus an "Add League" button that links a new one by logging in with that league's shared League ID + password (mirrors today's `register()` role-by-password convention). Not built — see Work Packages backlog.
- **Claude Code sandbox mounts break project-scoped MCP config on this machine (found 2026-08-11, partially revised 2026-08-12):** `~/.claude.json` (where the `render-api` project-scoped connector lives, see below) was found **read-only** in an interactive session on 2026-08-11 — confirmed via a real `OSError: [Errno 30] Read-only file system`. **Update (2026-08-12, background/job session):** a direct Python edit deleting the `render-api` key from `~/.claude.json` succeeded and persisted (verified by re-reading the file and by `claude mcp list` no longer showing it) — so the mount is not uniformly read-only across all session types; it may depend on interactive vs. background/job session, or have changed since. Re-test in an interactive session before relying on either finding. The old `render-api` connector's API key was independently confirmed dead (`unauthorized` from `list_workspaces`) and has been removed from both `~/.claude.json` project scopes (2026-08-12) — reconnecting requires a fresh key from the user, added via `claude mcp add --transport http render-api https://mcp.render.com/mcp --header "Authorization: Bearer <key>"` (try from inside a session first now, given the above; fall back to a real terminal outside Claude Code if it still fails). Separately, the two account-level OAuth connectors (`claude.ai Render`, `claude.ai render_mcp`) cannot be removed or managed from any Claude Code session at all — only from https://claude.ai/customize/connectors — and `claude.ai render_mcp` was found pointed at a dead URL (`bettergolfleaguetracker-oceb.onrender.com/sse` → 404 "No MCP endpoint found"), so it needs reconfiguring there too, not just re-authenticating. Root cause of the original finding: `claude mcp list` reports `/home/zowen22/.mcp.json` (the home-directory "workspace" project config, distinct from `~/.claude.json`) as unparseable — `ls -la` shows it's not a regular file at all, it's a **character-device node** (`nobody:nogroup`, major/minor `1/3` = `/dev/null`) bind-mounted over that path. That poisons project/local-scope MCP config resolution for the whole session, which plausibly explains why `render-api` never surfaced via ToolSearch (see "Site Wiki" section's cross-reference and `feedback-check-claude-json-for-api-key-mcp` memory).
- **Supabase port:** Use connection pooler port `6543`, not direct `5432` — direct host is IPv6-only and fails with "Network is unreachable" on Render
- **Postgres GROUP BY strictness:** SQLite allows selecting non-aggregate columns not in GROUP BY; Postgres does not. Every non-aggregate SELECT column must appear in GROUP BY. Large source of migration bugs — see `migration.md` Phase 5.3 for the full list of files fixed.
- **HAVING aliases:** Postgres `HAVING` cannot reference SELECT-list aliases. Replace `HAVING alias >= N` with the actual aggregate expression.
- **Dynamic `?` placeholders:** The tokenize-based `?`→`%s` script missed placeholders built via string concatenation (`query += " AND x = ?"`). Three files had these: `handicap.py`, `players.py`, `stats.py` — all fixed.
- **`ensure_tables()` in admin.py:** SQLite-only DDL; guarded with `DATABASE_URL` check — no-ops under Postgres. Tables come from `schema_postgres.sql`.
- **Render free tier spins down** after inactivity — upgrade to $7/mo paid before sharing demo URL publicly.
- **Implicit `overflow-y: auto` from `overflow-x: auto` (fixed 2026-08-12):** per the CSS spec, setting `overflow-x` to anything other than `visible` while leaving `overflow-y` unset causes the browser to also compute `overflow-y` as `auto` — not `visible`. `.ap-tabs` (main.css, the Admin Panel's desktop tab bar) had `overflow-x: auto` for touch-scrolling on narrow screens but no explicit `overflow-y`, so any sub-pixel height mismatch (border, line-height, padding rounding) triggered a real vertical scrollbar inside the tab row, even on desktop. Fixed by adding `overflow-y: hidden` alongside `overflow-x: auto`. If you add another horizontally-scrolling strip anywhere, set `overflow-y: hidden` explicitly rather than leaving it to default.
- **Rapid successive pushes to `main` can produce a spurious "failed"/timed-out deploy** for an intermediate commit that Render supersedes with the next push before it finishes booting — the log shows a clean boot, one successfully-served request, then a graceful `TERM` (not a crash), followed by a port-scan timeout minutes later. Before concluding a deploy actually broke something, check the status of the *latest* commit on `main`, not the one named in the failure notification (2026-07-03).
- **Mixed gross-stats/points queries and `is_absent`:** see "Absence / Ghost-Score Handling" above — never blanket-filter `is_absent` out of a query that also aggregates `match_results.total_points`.
- **`division` vs `division_name` column:** `schema_postgres.sql` uses `division_name`; old queries selected `division` → `UndefinedColumn`. Fixed in `league_info.py`.
- **New columns need THREE things, not one — this caused a production 500 twice in one session:** (1) the column in `schema_postgres.sql`'s `CREATE TABLE` (and the SQLite `init_db.py` equivalent, for local dev), (2) a migration file in `app/migrations/` with `ADD COLUMN IF NOT EXISTS`, **and** (3) that filename actually added to the `additive` list inside `init_db.py`'s `_apply_additive_migrations_postgres()` — writing the migration file alone does nothing, since only files in that list ever get executed against the live Postgres DB. `add_temp_handicap_percent.sql` was written but never added to the list, so it silently never ran, and `_get_settings()`'s defensive-merge loop still crashed with a raw `KeyError` on the missing column (it only guarded against `NULL` values, not columns absent from the row entirely — same root defensive gap in `_PgRow.__getitem__`, worth checking any similar "merge row over defaults" loop for). Separately, `handicap_history`'s override columns (`trigger_round_id`, `is_manual_override`, `override_reason`, `override_by_user_id`, `override_at`) turned out to have **never** been in the schema or a migration at all — they only existed in production via an undocumented direct DB edit, predating migration files being used consistently in this project. Both fixed 2026-07-02; check all three locations whenever adding a column to an existing table. A third instance surfaced 2026-07-03: `matchups.week_label`/`makeup_for_week` existed only via the standalone `migrate_rain_outs.py` script (none of the three locations), 500-ing `admin.panel()` on any fresh DB — fixed the same way (`add_rain_out_columns.sql`). **A fourth instance found 2026-07-19**: `add_match_play_tie_points.sql` (written 2026-07-10, see WP3.1) existed and was correct, but was never added to the `additive` list — so `league_settings.match_play_tie_points` was never actually created on production Supabase despite being in `schema_postgres.sql` and referenced unconditionally by `admin.py`'s settings-save route, meaning **every league's admin settings save had been silently 500ing** for over a week. Found while building an unrelated dashboard feature (a `league_settings` INSERT hit the same missing-column error). Fixed both ways: registered the migration in the additive list (permanent, self-healing on next deploy) *and* ran the `ALTER TABLE` directly against production so it stopped being broken immediately rather than waiting for a deploy. **Takeaway: writing a migration file is necessary but not sufficient — always grep `init_db.py`'s `additive` list to confirm a migration is actually registered, don't assume "the file exists" means "it runs."**
