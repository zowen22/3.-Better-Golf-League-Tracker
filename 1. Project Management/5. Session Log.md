# Session Log

-----

## 2026-06-26 — Absent Player Bug Fix + Handicap Matrix Overhaul + Comprehensive Audit

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**In-progress bug for rounds with absent players:**
- `_recalc_single_round` crashed silently: `if s > max_per_hole` fired before ghost synthesis when `s is None` for absent players — fixed with `if s is None: continue`
- Silent `except Exception: pass` was swallowing absence detection failures — replaced with `logging.error`
- `ORDER BY id DESC` → `ORDER BY handicap_id DESC` and `SELECT handicap FROM players` → `SELECT starting_handicap FROM players` in `current_handicap()` inside `_recalc_single_round`

**Handicap matrix — playing handicap + deduplication:**
- Replaced raw index display with playing handicap (rounded integer) everywhere: dashboard, my_stats, compare page, handicap matrix
- Fixed `GroupingError`: added all non-aggregate columns to `GROUP BY` in `handicap.py`
- Fixed duplicate date columns: multiple matchups per week share the same date — deduplicated by `round_date`, storing `round_ids` list per column
- Lazy imports in `handicap.py` to break circular dependency (`scores → handicap → scores`)

**Handicap matrix edit mode:**
- Admin-only "Edit" toggle reveals inputs for each handicap cell
- Modified cells highlighted amber; amber cleared on cancel (values restored from `data-orig`)
- Save POSTs JSON to `POST /handicap/matrix/<season_id>/update`
- Backend updates `handicap_at_time_of_play`, then calls `_recalc_single_round` for each affected matchup (re-scores net totals, A/B assignments)
- Hint text explains the cascading effect

**Comprehensive audit — 6C / 4H / 4M / 3L issues found and fixed:**
- C-1/H-3: `auth.py` — `league['league_id']` key fix + `role_row` None guard on register
- C-2/C-3: `scores.py` — `handicap_id` ORDER BY and `starting_handicap` column fixes
- C-4: `email_settings.html` — `admin.landing` back-link
- C-5: `availability/admin_grid.html` — `admin.panel` back-link
- C-6: `stats/compare.html` — `seasons.create` URL fix
- H-1: `public_view.py` — `admin_settings` now uses `@admin_required`
- H-2: `app.py` — `switch_season` route now requires `@login_required`
- H-4: `dues/admin.html` — `admin.panel` back-link
- M-1: `players.py` — removed redundant `@login_required` before `@admin_required`
- M-2: `self_report.py` — broken matchup redirect → `main.dashboard`
- M-3: `subs/admin_requests.html` — `admin.landing` back-link
- M-4: `league_info.py` — `get_db` from `database`, not `.main`
- L-3: `init_db.py` — added `preferred_tee_name TEXT DEFAULT NULL` to players schema

### Decisions Made
- Lazy imports (inside function bodies) in `handicap.py` are the permanent pattern to avoid circular imports with `scores.py`
- Playing handicap (integer) shown everywhere except the handicap calculations detail page
- Matrix edit saves trigger full `_recalc_single_round` cascade — A/B assignments and net scores update automatically

### Pending / Next Session
- Remove `#fit-debug` overlay from `enter_week.html` once name column confirmed stable (@user confirm)
- APNs credentials + TestFlight archive/upload (@user tasks)
- `migrate_api_tables.py` against Supabase if not yet run (@user)
- WP5.4 P4-2: cascade-delete FK review in Supabase (@user)

-----

## 2026-06-22 — Score Entry Polish + Handicap Features + Schedule Fixes

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Mobile scorecard name column — Builds 20260622a–r (prior session continuation):**
- Canvas `measureText` + `document.fonts.ready` approach locked in as the only reliable iOS measurement method
- Correctly identified cell anatomy: role-pill span → whitespace text node (~3px) → `.sp-trigger` button
- Final formula: `rolePillW = ceil(ctx.measureText('A').width) + 26` (accounts for padding, border, margin, whitespace)
- Build r: all names displaying correctly; debug overlay retained for future diagnosis
- Process documented in `memory/scorecard_name_col_measurement.md`

**Week dropdown — MM/DD format:**
- Score entry week dropdown now shows `Week X — MM/DD` using `{{ wdate[5:7] }}/{{ wdate[8:10] }}`
- `week_options` query fixed: `SELECT DISTINCT week_number, scheduled_date FROM matchups WHERE season_id = %s AND is_bye = 0 ORDER BY week_number`

**Handicap history pages (new feature):**
- `recalc_handicap_for_player()` extended: `trigger_round_id`, `is_manual_override`, `override_reason`, `override_by_user_id`, `override_at` columns added to `handicap_history` (SQL run directly in Supabase via `migrate_handicap_history_v2.py`)
- New routes in `handicap.py`: `player_history`, `player_history_redirect`, `league_matrix`, `league_matrix_redirect`, `override_handicap`, `clear_handicap_override`
- `player_history.html`: player dropdown + season selector + rounds filter (5/10/15/20/50/100); table with Round#, Date, Playing Hcp, Hcp Index, Notes (Edited badge + reason); admin override modal
- `league_matrix.html`: sticky name column, `col-pin`/`col-divider` classes, member/sub type badges, week cells show handicap at time of play, name links pre-select player in history view
- Fixed: `SELECT DISTINCT` ORDER BY compatibility (added p.first_name, p.last_name to SELECT)
- Fixed: `u.first_name || ' ' || u.last_name AS override_by` (no `username` column)
- Nav link "Handicaps" added to `base.html` between Records and Board

**Schedule page fixes:**
- Sticky week column: `overflow: hidden` on `.admin-widget` was killing `position: sticky` — changed to `overflow: clip` in `main.css`
- Edit mode sticky: `.data-table { overflow: hidden }` on the table element was trapping sticky — added `.schedule-yearly-table { overflow: visible }` override in schedule template
- Week column header color: explicitly set to `var(--green-dark)` to match rest of table headers
- Admin button overflow on mobile: `.admin-widget__header { flex-wrap: wrap }` + `.admin-widget__header-actions { flex-wrap: wrap; gap: 8px }`
- Edit mode row highlight: `.schedule-yearly-table.editing tbody tr:hover { background: #d4edda }` (stronger than the default near-white)

**Teams nav link:**
- Added "Teams" nav link in `base.html` pointing to `url_for('seasons.detail', season_id=nav_season_id)#teams`; gated on `nav_season_id` being set

**Completed scorecard inline on enter-week page:**
- Extracted `_load_completed_scorecard(db, matchup_id, scoring_mode)` shared helper; refactored `view` route to use it
- Completed matchups now show full read-only scorecard inline instead of "Scores already recorded" message + View Scores button
- Edit Scores button (admin only) shown in header instead
- Scorecard matches `view.html` exactly: stroke dots, per-hole win/loss/half indicators, Hdcp row, net totals, nicknames/sub/tee badges, "— vs —" separator
- Mobile CSS: `.ew-completed-scorecard .sc-col-pts { display: table-cell }` + `.sc-pt-indicator { display: block }` override the global hide rules
- Summary column fixed: header "Net" → "Gross", value `total_net` → `total_gross`
- Name truncation fixed: wrapped completed card names in `.sc-name-first` so canvas fit() picks them up
- Column widening on scroll fixed: JS detects `.ew-completed-scorecard` and uses `summaryW=138` (vs 108 for entry cards) to account for visible Pts column
- Team headers ("Team: Warner/Warner") removed from completed cards
- Matchup label ("Smith/Jones vs Warner/Davis") added small and gray beside "Group N" in all card headers — built in both Python route and template

**Late score entry retroactive cascade:**
- `_recalc_future_rounds(db, player_ids, season_id, league_id, after_round_date)` — after a late submission, finds all subsequent completed rounds for those players and re-scores them
- `_recalc_single_round()` — re-derives playing handicaps from current `handicap_history`, recomputes net scores, re-assigns A/B roles (lower hcp = A), rewrites `match_results`
- Called from `_process_scores()` after handicap recalc; errors caught and surfaced as warning flash without rolling back original scores

**Schedule week renumbering:**
- `remove_week` now renumbers remaining weeks consecutively after deletion; `week_notes` updated in sync
- One-time Supabase SQL provided and run to fix existing gaps — confirmed clean

### Builds Shipped
- r: name column locked in (fonts.ready + canvas measureText)
- s: Teams nav link
- t: Schedule sticky week column fix (overflow: visible on table)
- u: Edit mode row highlight color
- v: Completed scorecard inline on enter-week page
- w: Full scoring detail on completed inline scorecard (stroke dots, pts indicators)
- x: Retroactive cascade on late score entry
- y: Completed scorecard display fixes (pts column, gross column header, indicators)
- z: Completed scorecard polish (build tag, matchup label, team header removal, name truncation, scroll fix)
- Schedule renumber fix (no build tag — schedule.py only)

### Decisions Made
- Retroactive A/B + points cascade on late score entry is intentional — designed for leagues that join mid-season
- `overflow: clip` (not `overflow: hidden`) on `.admin-widget` — clip doesn't create scroll container so `position: sticky` works inside; hidden kills it
- Canvas `measureText` is the only reliable iOS text width measurement approach; `getComputedStyle().font` returns `""` before fonts load — must construct manually
- Completed scorecards show inline (not behind a button) — removes the need for "View Scores" CTA entirely

### Pending / Next Session
- Remove `#fit-debug` overlay from `enter_week.html` once name column confirmed stable in prod
- APNs credentials + TestFlight archive/upload (@user tasks)
- WP5.4 P4-2: cascade-delete FK review in Supabase (@user)
- Score import CSV for 18-hole rounds (WP3.1 open item)

-----

## 2026-06-21 — Admin Panel Polish + Mobile Score Entry + Nickname Logic

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Admin Panel — Score Entry Widget**
- Week tiles now link to `scores.enter_week` (weekly score entry) instead of schedule view
- "Enter Scores for Current Week" button logic fixed: lands on most recent week by date (≤ today) rather than earliest incomplete week
- Past completed weeks hidden from widget — only current + upcoming (and any incomplete past weeks) shown
- Schedule quick-action button now lands on Season Schedule (yearly view) via `?week=all`
- Admin widget header: added `gap: 12px` to fix tight emoji/button spacing on mobile web

**Mobile Web — Score Entry Scorecard**
- Current `enter_week.html` backed up to `enter_week_backup.html`
- New responsive layout at ≤640px: hides Hdcp/Gross/Pts columns + hole-hcp row + stroke dots; compresses name col (54px), hole cells (26px), inputs (22px)
- 9-hole scorecard now fits a 375px iPhone screen with zero horizontal scroll
- Desktop layout unchanged; `overflow-x: auto` still applies on wider screens

**Score Entry Week Tile Grid (mobile web)**
- Switched `.swt-grid` from `display: flex; flex-wrap: wrap` back to CSS `grid` with `repeat(auto-fill, minmax(130px, 1fr))` — tiles now fill row width evenly at any screen size

**Skins — Player Nickname / Display Name**
- `load_nicknames(db, league_id)` and `player_display_name(player_id, first, last, nicknames_map)` added to `database.py` — single source of truth; logic is nickname → "First L." fallback
- Applied to: `/api/v1/skins` (mobile API `winner_name`), web skins index `winners_summary`, web skins round detail score table + winner column
- Bug fix: `ORDER BY id` → `ORDER BY nickname_id` (column name in schema)

**iOS Score Entry — Tap-to-Type + Auto-Advance**
- `HoleScoreInputRow` rewritten: `TextField` (numberPad) replaces +/− steppers
- Same visual layout (Hole/Par/Score rows, horizontal scroll)
- Single-digit auto-advance to next hole after 0.3 s delay (allows two-digit entry for scores ≥ 10)
- Focused cell gets light accent background; OCR-filled scores re-sync when no hole is focused

### Decisions Made
- `player_display_name` lives in `database.py` (not a route helper) so it's importable everywhere without circular imports
- Mobile scorecard hides summary cols rather than transposing layout — simpler, good enough for 9-hole; 18-hole still scrolls
- Auto-advance delay of 0.3 s chosen to allow double-digit entry without UX friction

### Next Session
- Test mobile score entry tap-to-type on device
- Skins summary redesign (web + mobile) — layout described, not yet built
- Run `migrate_api_tables.py` against Supabase if not yet done
- APNs credentials (.p8 key) — user task

-----

## 2026-06-21 — iOS Gap Audit + Parity Sprint

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**iOS Functional Gap Audit — gaps found and fixed:**

1. **Schedule "All Season" flat list** → now shows week-grouped sections with week headers. Each section header shows "Week N — Date" + a color-coded badge for non-Normal week types (Rain Out, League Bye, Playoffs, Makeup). League Bye weeks show a "No play this week" label instead of matchup rows.

2. **Bye matchup rows** → filtered from play sections; League Bye weeks handled at the `ScheduleWeek` level.

3. **`weekType` not available on `Matchup`** → added `var weekType: String?` to `Matchup`; `ScheduleResponse.matchups` computed var now propagates it from the parent `ScheduleWeek`. `ScheduleViewModel` now stores `weeks: [ScheduleWeek]` directly from the API response so views have access to the full week context.

4. **Tee auto-select in ScoreInputView** → after courses load, `teeMatching(teeId:)` finds the matching `TeeInfo` from `matchup.teeId` and pre-selects it. Eliminates the mandatory manual tee pick on every score entry.

5. **Members had no self-report path in the app** → added `isSelfReport: Bool` flag to `ScoreInputView`. When `true`: posts to `/api/v1/self-report/submit`, shows orange submit button, title "Self-Report", success alert says "Submitted for Review — pending admin approval", OCR scan option hidden. `MatchupDetailView` now shows "Submit Self-Report" NavigationLink for non-admin users on pending matchups.

6. **Prior commit (same session):** `/api/v1/courses` response shape fixed (`tee_colors` dict → flat `tees` array matching `CourseInfo`/`TeeInfo` Swift models).

7. **Prior commit (same session):** `ScheduleView` NavigationLink destination fix — `.navigationDestination` moved after `.listStyle`.

**Web (same session):**
- Admin login redirect → admin panel
- Score Entry widget on admin panel (green CTA + per-week status tracker)
- Admin quick actions collapsed to 6 visible + "More Tools" expandable
- Schedule "Season Schedule" rename + admin-widget card styling on yearly view

### Decisions Made
- Self-report for members reuses `ScoreInputView` with `isSelfReport` flag — no separate view needed
- "All Season" schedule defaults to upcoming week on load; user can switch to "All Season" via picker

### Blockers / Issues
- None

### Next Session
- WP3.3 remaining: test self-report flow end-to-end on device; verify tee auto-select with real course data
- Run `migrate_api_tables.py` against Supabase if not yet done
- APNs credentials (.p8 key) — user task

-----

## 2026-06-21 - Rain Out Feature + Bug Fixes

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Rain Out Feature (backend + frontend)**
- `week_label TEXT` and `makeup_for_week INTEGER` columns added to matchups (migration: `migrate_rain_outs.py`; user ran via Supabase SQL editor)
- `mark_rain_out()` route returns JSON `{'ok': True}` instead of redirect
- `rain_outs.html`: two-panel manage page (left = rain out cards, right = reschedule form); Option A overwrite / Option B fractional label
- Schedule index: ☔ per-row button, "Manage Rain Outs" toolbar button, week label badges (☔/🔄)
- `_build_yearly_rows()`: replaced `.get()` calls with direct key access (psycopg2 `_PgRow` doesn't support `.get()`)

**Score Entry Auto-Week Fix**
- `enter_week_current()` rewritten: finds MIN week_number where status != 'completed' AND week_type NOT IN ('Rain Out','League Bye') — no longer date-based

**Tee Auto-Population Fix**
- `enter_week()`: when matchup has no tee_id, falls back to `courses.default_tee_id`

**Popup Fix (spOpen)**
- `{% include 'components/sub_popover.html' %}` was silently not rendering — inlined popup HTML/CSS/JS directly in `base.html`
- `{% raw %}...{% endraw %}` wraps `<style>` block to prevent `@media(...){#sp-overlay` from being parsed as Jinja comment `{#`

**Per-Player Default Tee**
- Player edit form: tee color dropdown reads tees from league's courses, saves `preferred_tee_name` to DB
- `players.py edit()`: fetches tee colors, reads/writes `preferred_tee_name` in POST and GET
- Tee hierarchy: per-round selection → player default → course default → first available

**Admin Panel Fix**
- Admin matchup SELECT now includes `tee_id`, `week_label`, `makeup_for_week` (was crashing with KeyError)

**Schedule Redirect Fix**
- All schedule save/edit redirects now include `week='all'` to land on all-dates view
- `schedule.current()` forwards optional `week` param to `schedule.index`
- Dashboard schedule tile now links to `week='all'`

### What's Next
- Verify popup (spOpen) working in production after `{% raw %}` fix
- Verify tee auto-population from course default working end-to-end
- iOS: update tee picker + submission to use new `tee_colors` API shape and `tee_nine`
- Pending user decision: "League bye weeks not count toward week number progression"

-----

## 2026-06-20 - Score Entry Polish + Tee Parity

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Popup fix**
- Sub/absence popover was invisible due to `position:absolute` being clipped by `.sc-entry-section { overflow:hidden }` — switched to `position:fixed` with viewport coordinates

**Player name popup — profile link**
- Owner name in sub popover header is now a clickable link to `/players/<id>` (opens new tab)
- Removed redundant team header profile links from `enter.html` (previously added in an earlier session) — player access now consolidated in the popup

**Dashboard — admin tile reorder**
- Score Entry and Print Scorecards moved to top of dashboard grid for admins
- Duplicates at bottom removed

**Tee deduplication + defaults — weekly entry parity**
- `enter_week` now builds `player_tees` (deduplicated by color, M-rep) matching `enter()` logic
- `enter_week` now does course default tee lookup + M-rep resolution
- Per-player tee dropdown in weekly entry now shows color names only (was showing full tee_name + nine)
- Per-player `player_default_tees` resolution fixed in both `enter()` and `enter_week()`: raw `tee_id` from course default now mapped through color→M-rep index so `<option selected>` actually matches

**iOS API**
- `/api/v1/courses` restructured: tees grouped by color (Option B)
- `tee_nine` added to schedule endpoints

### What's Next
- iOS: update tee picker + submission to use new `tee_colors` API shape and `tee_nine`
- Run Supabase migration for `courses.default_tee_id` if not yet applied
- Weekly entry: verify course default tee auto-populates correctly end-to-end after deploy

-----

## 2026-06-20 - Postgres Bug Fixes + iOS Tee Deduplication API

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Postgres Migration Bug Fixes**
- `GET /api/v1/courses` 500: query selected `hcp_index` but DB column is `handicap_index` — fixed column name in SQL, kept response key as `hcp_index` for iOS compatibility.
- `GET /players/<id>/handicap-detail` 500: `GROUP BY sc.scorecard_id` missing 9 non-aggregated columns — PostgreSQL strict mode rejects what SQLite silently allowed. Added all columns to GROUP BY.

**iOS Tee Deduplication — API Option B**
- `GET /api/v1/courses` restructured: tees now grouped by color with nines nested inside.
  Response shape: `{course_id, course_name, tee_colors: [{color, tees: {front/back/full: {tee_id, tee_name, holes}}}]}`
- Dedup rule mirrors web: one entry per unique color, M gender tee preferred as representative per color/nine, tees with no hole data excluded.
- `GET /api/v1/schedule` (list + detail): added `tee_nine` field at week and matchup level so iOS can resolve `color + tee_nine → tee_id` at score submission time.
- **iOS work required:** update `ScoreInputView` tee picker to use `tee_colors` array (show color names only); on submit, look up `tee_colors[color].tees[matchup.tee_nine].tee_id`.

### What's Next
- iOS: Update tee picker + submission logic to use new API shape
- Run Supabase migration for `courses.default_tee_id`
- Implement flight/division-level default tee (needs flights schema)

-----

## 2026-06-20 - Web Polish Sprint: Admin Settings, Score Entry, Schedule Fixes

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Admin Settings**
- Renamed "Handicap Scoring Type" → "Does this league use handicaps?" with Yes (Net) / No (Gross) options
- "Require Admin Approval" in Self-Reporting section now dims and auto-unchecks until "Allow Members to Submit Scores" is checked (pure JS, no backend change)

**Score Entry — Tee Deduplication**
- Extra Tees checkboxes on Print Scorecards fixed: selecting an extra color now truly adds it instead of replacing the auto tee. Root cause: when matchup had no `tee_id`, `auto_color` was None, so the fallback color wasn't stable — checked extras evicted it. Fixed by promoting the fallback to `auto_color` immediately.
- Per-player tee dropdown in score entry now shows one entry per color (no M/W/Front/Back duplicates). Backend builds `player_tees` (deduplicated, M representative, filtered to selected nine) and passes separately to template.
- Course Default Tee selector on course profile page similarly deduplicates by color.

**Player Profile Links in Score Entry**
- Team header rows ("Team: Smith / Jones") now link to each player's profile page. Links inherit the bold header style with underline on hover only.

**Course Default Tee**
- New `courses.default_tee_id` column (schema + migration script `migrate_course_default_tee.py`)
- Course profile page: admin-only "Default Tee" selector form → `POST /<id>/set-default-tee`
- Score entry tee hierarchy updated: course default → player preferred (ascending privilege). Flight defaults noted as TODO — needs a flights/divisions schema.
- **Supabase migration required:** `ALTER TABLE courses ADD COLUMN IF NOT EXISTS default_tee_id INTEGER REFERENCES tees(tee_id);`

**Schedule Bug Fixes**
- Schedule auto-advancing to wrong week: two separate UTC midnight bugs fixed
  - `schedule.index`: already fixed previous session (−6h grace period)
  - `enter_week_current`: same bug, `date.today()` was UTC → applied same −6h fix
- Schedule bulk-edit not saving: `AND status != 'completed'` on date/type/course/side updates was blocking edits for any week with a completed matchup. Week-level metadata now always writable; team assignment changes remain protected.
- Clear Schedule button: moved from page header into the edit-mode-only save bar; two-step confirm dialog; danger button retains red styling (CSS override scoped to non-danger buttons).

**`multi_course` Admin Setting** *(carried from prior session)*
- Supabase migration SQL provided; schema updated; `alter table` migration script added.

### What's Next
- Run Supabase migration for `courses.default_tee_id`
- Implement flight/division-level default tee (needs flights schema)
- Score import CSV (WP3.1 open item)
- API key management UI (WP3.1 open item)
- APNs credentials + TestFlight (WP3.2 user tasks)

-----

## 2026-06-17 - Sprints 3-9: Full iOS Feature Sprint

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Sprint 3 — Data Presentation Parity**
- `MatchupDetailView`: team rosters with handicaps, course/tee/date, nav to scorecard
- `ScorecardView`: per-player sections, `HoleScoreGrid` with gross/net/+par per hole, totals column
- `StatusBadge`: color-coded match status component

**Sprint 4 — Admin Score Entry**
- `ScoreEntryView`: admin tab lists scheduled matchups for direct score entry
- `ScoreInputView`: tee picker (from `/api/v1/courses`), per-player 9-hole stepper grid, submit
- Backend: `GET /api/v1/courses` returns courses + tees + hole pars
- `MatchupDetailView`: "Enter Scores" now navigates to ScoreInputView

**Sprints 5/6 — Camera OCR Pipeline**
- `ScorecardScanner.swift`: `VNRecognizeTextRequest` extracts 9-hole rows from scorecard photo
- `ImagePicker`: wraps camera and photo library (UIKit/PHPickerViewController)
- `ScanResultView`: review + correct OCR output before confirming into score grid
- ScoreInputView: "Scan Scorecard (OCR)" menu wires camera → OCR → review → fill
- Backend: `POST /api/v1/self-report/submit` creates pending `score_submission` for admin review

**Sprint 7 — Admin Detail Views**
- `PendingSubmissionDetailView`: full submission info (teams, course, tee, submitter, hole count)
- One-tap Approve button → navigates back to pending list on success
- `AdminView`: nicer PendingSubmissionRow, ContentUnavailableView for empty state

**Sprint 8 — Push Notifications**
- `AppDelegate`: requests `UNUserNotificationCenter` permission, registers for APNs
- Device token forwarded to existing `/api/v1/apns/register` via NotificationCenter bridge
- `EmptyResponse` codable wrapper for fire-and-forget endpoints

**Sprint 9 — Offline Support (Lightweight)**
- `ResponseCache`: UserDefaults JSON cache keyed by endpoint path (24-hour TTL)
- `ScheduleViewModel` + `StandingsViewModel`: serve stale cache on `noNetwork` error
- Offline banner (wifi.slash) shown when displaying cached data

**Additional Polish**
- Schedule: week dropdown shows "Week X — Apr 22" with date, alignment bug fixed
- Standings: gold/silver/bronze rank badges, rounds-played display, insetGrouped
- Stats hub: color-coded icon tiles with subtitle for each category
- StatsTrendView: Swift Charts `LineMark` chart for cumulative points (iOS 16+ Charts framework)
- LoginView: golf icon, green CTA, keyboard focus management, submit on return key

### What's Next
- Rebuild in Xcode — new files: ScoreInputView, ScorecardScanner, ResponseCache
- Test score entry flow end-to-end with real matchup
- Verify Charts framework renders correctly on device (requires iOS 16+)
- APNs requires physical device + paid developer account for testing

-----

## 2026-06-17 - iOS Sprint 2: Schedule + Standings Live, Tab Planning

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Fixed all schema mismatches between API endpoints and Postgres schema:
  - `home_team_id`/`away_team_id` → `team1_id`/`team2_id` in schedule queries
  - `handicap_index` doesn't exist on players table — replaced with LATERAL join from `handicap_history`
  - `ScheduleResponse` Swift model updated to decode `weeks` array, flatten to matchups
  - `Matchup` struct fixed: removed non-optional week-level fields (`week_number`, `course_name`, `tee_name`) that don't exist per-matchup in JSON; injected from parent `ScheduleWeek` post-decode
  - `MatchupStatus` enum made resilient to unknown values
  - `Standing.points` CodingKey fixed to `"total_points"`
- Schedule and Standings views both loading real data from Render
- Tab structure planned: Schedule (1), Standings (2), Score Entry (3, admin-only), Stats (4, catch-all), Admin (5, admin-only)
- Score entry decision: admin-only for now — member OCR self-report considered but rejected due to validation/photo-storage complexity (logged in Decisions Log)
- Stats tab: table-of-contents hub linking to All-Play, Weekly, Handicap Trend, Leaders, Records
- Stats sub-views require new JSON API endpoints (backend Python work, no Xcode needed)

### What's Next
- Tab restructure in `MainTabView.swift`
- Stats API endpoints (Python) + Stats hub view + sub-views (Swift) in parallel
- Schedule auto-scroll to current/upcoming week
- Score Entry tab (admin-only)

-----

## 2026-06-16 - WP1.2/1.3 iOS Auth + JWT Fix

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Diagnosed login flow: login returned valid JWT but app immediately bounced back to LoginView
- Root cause: PyJWT 2.x requires `sub` claim to be a string; we were encoding `sub: 0` (int) for league-level auth → `InvalidSubjectError`
- Fix: `create_token()` now encodes `sub` as `str(user_id)`; `require_jwt` casts back to `int` on decode (`jwt_utils.py`)
- Added `jwt.PyJWTError` logging (class name + message) to aid future debugging
- Simplified login to League Code + Password only (removed email field from `LoginView.swift` and `AuthViewModel.swift`)
- `csrf.exempt(api_bp)` added to `app.py` — API uses JWT, not CSRF tokens
- `APIClient.swift` hardcoded to Render URL (no localhost debug path) — Mac is too slow for local dev
- Session migrated to work machine (repo fully pushed to `postgres-migration`)

### Tasks Checked Off
- WP1.2: Networking layer — building clean
- WP1.3: Auth flow — login → dashboard working end-to-end

### What's Next
- Verify Schedule and Standings views load data after auth fix
- Score entry redesign: admin-enabled flow where user picks member to enter for (no individual accounts in v1)
- Simulator testing requires Mac + Xcode; backend work can run from any machine

-----

## 2026-06-15 - WP1.1 Xcode Project Setup (iOS Sprint 2 Start)

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Cloned `postgres-migration` branch to `/Users/lindsaygatsios/Claude/GolfLeagueApp/repo`
- Created Xcode project at `/Users/lindsaygatsios/Claude/GolfLeagueApp/Xcode/BetterGolfTracker/`
- Created full folder structure on disk: `App/`, `Core/Networking/`, `Core/Auth/`, `Features/Auth/`, `Features/Schedule/`, `Features/Standings/`, `Features/ScoreEntry/`, `Features/Admin/`, `Shared/Models/`, plus persistence/push/profile/components stubs
- Copied all 10 pre-written Swift files from `ios_swift/` into correct target folders
- Wrote new files: `BetterGolfTrackerApp.swift`, `RootView.swift`, `MainTabView.swift`, `LoginView.swift`, `ScheduleView.swift`, `StandingsView.swift`, `AdminView.swift`, `ScoreEntryPlaceholderView.swift`
- Resolved duplicate file issue (Xcode "Add Files → Copy" created `App 2/` / `Core 2/` etc.) — deleted duplicates from disk
- Project builds clean (only remaining prompt is signing team, which user set via Signing & Capabilities)
- Note: this is a modern Xcode 16 project using `PBXFileSystemSynchronizedRootGroup` — no manual pbxproj file registration needed; Xcode auto-picks up all Swift files on disk

### Tasks Checked Off
- WP3.2: `@user` Sprint 1 Xcode task checked off; WP1.1 fully complete

### First Checks Next Session
- Open Xcode from **`repo/ios/BetterGolfTracker.xcodeproj`** — project was moved into repo this session; old path (`/Xcode/BetterGolfTracker/`) no longer exists
- Set git identity (not yet configured on this machine):
  ```
  git config --global user.name "Your Name"
  git config --global user.email "zowen22@gmail.com"
  ```

### What's Next
- WP1.2: Networking layer (already written — files are in place, verify they compile correctly)
- WP1.3: Auth flow views — LoginView is written; verify end-to-end login against Render backend
- Sprint 2 goal: login → tab bar → schedule/standings visible on simulator

-----

## 2026-06-14 - Schedule Page UX Polish

*Status: `COMPLETED`*
*Type: `Ad Hoc`*

### What Was Done
- **3-way name toggle** on yearly view: cycles Team Numbers → Last Names → First Names → repeat. Grey "Showing: …" note beneath button for persistent reference.
- **T# badge** in weekly detail view: replaced `#N` text with a green-bordered white pill badge ("T2") matching the score entry page visual; column header `#` removed.
- **Course + Type inline edit**: Edit Schedule mode now includes Course (dropdown) and Type (text input) cells. Save handler handles `course_{week}` and `type_{week}` POST keys. `courses_list` passed to template from yearly view route.
- **Bug fix**: `applyNamesToSelects(showingNames)` was referencing a deleted variable — corrected to `DISPLAY_MODES[displayModeIdx]`.

### No PM tasks affected (UX polish, untracked)

-----

## 2026-06-14 - WP5.4 Edge Cases (Scheduled)

*Status: `COMPLETED`*
*Type: `Scheduled`*
*Linked Audit: `Audits/2026-06-12-score-entry-audit.md` (P4-1, P4-3 — resolved; P4-2 remains @user)*

### What Was Done
- **P4-1:** Added explicit `rounds` table check in `self_report.py` `approve()` after the `matchup_status == 'completed'` guard. If a round row already exists for the matchup (admin entered directly while submission was pending), the approval is auto-rejected with a clear flash message and an `admin_note` of "Round already exists". Prevents the race-condition duplicate round.
- **P4-3:** Added NULL starting_handicap warning in `scores.py` `enter()` route. After the player list is built, any player whose handicap is 0 *and* has no handicap history *and* has `starting_handicap = NULL` is flagged. Admin sees a yellow flash: "{Name} has no starting handicap set and will play as scratch (0). Set a starting handicap in Player settings to correct this."

### Tasks Checked Off
- WP5.4: P4-1, P4-3 checked off; P4-2 remains @user

-----

## 2026-06-13 - WP5.3 Audit Trail + UX Polish (Scheduled)

*Status: `COMPLETED`*
*Type: `Scheduled`*
*Linked Audit: `Audits/2026-06-12-score-entry-audit.md` (P3-1 through P3-4 — resolved)*

### What Was Done
- **P3-1:** `entered_by_user_id` added to `rounds` table (migration: `add_rounds_entered_by.sql`, schema updated). `_process_scores()` now writes `session['user_id']` on INSERT — admin score entry now has a full audit trail matching self-report approval.
- **P3-2:** Hidden `loaded_tee_id` input rendered server-side with the tee that holes were loaded for. On POST, server compares against submitted `tee_id`; logs warning + flashes if they differ. Guards against the silent JS/server tee mismatch scenario.
- **P3-3:** Second-admin double-submit now shows a clear flash message ("Scores for this matchup have already been recorded by another admin.") before redirect to the view page. Was a silent redirect before.
- **P3-4:** "Allow double-digit scores" toggle now persisted in `localStorage` — preference survives page reloads and new sessions. Label updated with hint text clarifying the auto-advance behavior.

### Tasks Checked Off
- WP5.3: All 4 tasks (P3-1, P3-2, P3-3, P3-4) — WP5.3 marked Complete

### Migrations Pending (run against Supabase before deploy)
```sql
ALTER TABLE rounds ADD COLUMN IF NOT EXISTS entered_by_user_id INTEGER REFERENCES users(user_id);
```

### Commit
`96c6cb0` — WP5.3: audit trail + UX polish (P3-1 through P3-4)

### What's Next
- WP5.4: Edge cases (P4-1, P4-3 are @claude; P4-2 is @user)

-----

## 2026-06-13 - Handicap Re-ranking + Player Preferred Tee

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- **9-hole handicap re-ranking:** Added `_rerank_hcp()` helper in `courses.py` `api_import()`. Front/back splits from 18-hole API tees now get handicap indices re-ranked 1–9 (preserving relative difficulty order) instead of keeping sparse 18-hole indices (1,3,5... / 2,4,6...). Cleaner scorecards, correct stroke allocation.
- **Player preferred tee (WP3.1):** Full hierarchy implementation:
  - `preferred_tee_name` column added to `players` (migration: `add_preferred_tee_name.sql`, schema updated)
  - Player edit form shows dropdown of distinct tee names across league's courses
  - Score entry resolves per-player tee: league default → player preferred. Nine (front/back/full) inherited from matchup tee so player only stores the color ("White"), not the full tee variant.
  - Both desktop and mobile scorecard selects pre-populate from `player_default_tees` dict

### Pending Before Deploy
- Run migration: `ALTER TABLE players ADD COLUMN IF NOT EXISTS preferred_tee_name TEXT DEFAULT NULL` against Supabase

### What's Next
- WP5.3: Audit trail + polish (P3-1 through P3-4)

-----

## 2026-06-13 - iOS Auto-Advance: Root Cause Found + Fixed

*Status: `COMPLETED`*
*Type: `Manual`*
*Linked Audit: `Audits/2026-06-12-score-entry-audit.md` (P0-5 — resolved)*

### What Was Done
- **Root cause identified:** Missing `</script>` before `{% endif %}` in `enter.html` — the `toggleAbsenceRow` `<script>` tag rendered inside the unclosed holes script, causing a JS syntax error that silently killed all holes JS (live calc, stroke dots, auto-advance) since the feature was built. Fixed in commit `37c8fb5`.
- **Auto-advance confirmed working** on iOS Safari and Chrome iOS — `input` event + `requestAnimationFrame(focus)` is the correct approach. All iOS focus restriction research was a red herring; the script just wasn't executing.
- **Added:** `enterkeyhint="next"`, `autocorrect="off"`, `autocapitalize="off"`, `spellcheck="false"` on score inputs — these improvements stay in place
- **Removed:** debug overlay and build badge (commit `fc03cf7`)

### What's Next
- WP5.3: Audit trail + polish (P3-1 through P3-4)

-----

## 2026-06-13 - WP5.2 Validation Fixes

*Status: `COMPLETED`*
*Type: `Manual`*
*Linked Audit: `Audits/2026-06-12-score-entry-audit.md`*

### What Was Done
- **P2-1:** `max_score_per_hole` now read and enforced in `_process_scores()` — reads `max_score_action` (warn/block) from settings; flashes per-violation message and either continues or redirects
- **P2-2:** `enter()` GET flashes warning if selected tee has any NULL `handicap_index` (lists affected holes)
- **P2-3:** Self-report max score raised from 15 → 20 (HTML inputs + server validation); `approve()` re-validates range before writing round rows

### User Actions Required
- None

### What's Next
- WP5.3: Audit trail + polish (P3-1 through P3-4)

-----

## 2026-06-13 - Print Scorecards Feature

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- **Print Scorecards** — full feature shipped end-to-end
  - `GET /scores/print-scorecards` route in `scores.py` — auto-detects next scheduled week, loads matchups with tee/hole data, computes playing handicaps + stroke dots
  - `app/templates/scores/print_scorecards.html` — screen controls (week, format, extra tees), scorecard cards, `@media print` stylesheet
  - Scorecard format: hole numbers, yardage rows per tee, M/W HCP rows, Par row, player rows (last name + superscript handicap, stroke-dot indicator per receiving hole), signature block
  - 18-hole: front-9 and back-9 tables stacked; 9-hole: single table
  - Format toggle: Group (1 card per group of 4) vs Per Matchup (2 copies per group, one per team)
  - Dashboard tile added (admin-only, `🖨 Print Scorecards`)

### User Actions Required
- None — fully server-side, works from existing data

### What's Next
- Sprint 2: iOS app shell (WP1.1 user task + WP1.2 networking + WP1.3 auth flow)
- WP5.2: Validation fixes (P2-1 enforce max_score_per_hole, P2-2 null handicap_index warning, P2-3 self-report range)

-----

## 2026-06-13 - WP5.1 Data Integrity + Postgres RETURNING Fix

*Status: `COMPLETED`*
*Type: `Manual`*
*Linked Audit: `Audits/2026-06-12-score-entry-audit.md`*

### What Was Done
- **WP5.1 complete (P1-1 through P1-4)**
  - P1-1: Handicap recalc wrapped in try/except post-commit; warning flash on failure
  - P1-2: `UNIQUE(matchup_id)` on rounds; `migrate_rounds_unique.py` created; duplicate-submit guard added
  - P1-3: Bare `except: pass` removed from absence backfill — UPDATE in same transaction
  - P1-4: Per-player tee validated against matchup's course_id before score parse
  - **Bonus:** `last_insert_rowid()` (SQLite-only) replaced with `RETURNING` in all 5 affected active routes — score entry, self-report, player_reg, playoffs, score_import were all broken on Postgres
- **iOS Sprint 1 complete (WP0.1 + WP0.2)**
  - `PyJWT>=2.8.0` added to requirements.txt
  - `app/jwt_utils.py` — `create_token()`, `decode_token()`, `require_jwt`, `require_jwt_admin` decorators
  - `POST /api/v1/auth/login` — email + password + league_code → JWT
  - `POST /api/v1/auth/refresh` — 7-day grace window refresh
  - `GET /api/v1/auth/me` — profile from JWT (no DB hit)
  - `GET /api/v1/schedule` — current season schedule with full team+player data
  - `GET /api/v1/schedule/<matchup_id>` — single matchup detail + round_id if completed
  - `GET /api/v1/standings` — current season standings with W/L/T
  - `GET /api/v1/players/nicknames` — all players + OCR nicknames for iOS name matching
  - `GET /api/v1/scorecards/<round_id>` — hole-by-hole scores + match results
  - `POST /api/v1/nicknames` + `DELETE /api/v1/nicknames/<id>` — nickname management
  - `app/migrate_player_nicknames.py` rewritten for Postgres

### User Actions Required
- Run `migrate_rounds_unique.py` against Supabase to add UNIQUE constraint (or run ALTER TABLE in SQL Editor)
- Run `migrate_player_nicknames.py` if player_nicknames table is missing (schema already has it)
- Sprint 1 (user): Create Xcode project per WP1.1 structure in `ios_app_plan.md`

### What's Next
- Sprint 2: iOS app shell (WP1.1 user task + WP1.2 networking layer + WP1.3 auth flow)

-----

## 2026-06-13 - Score Entry Audit Remediation — P0-3 + P0-4 (Scheduled Session)

*Status: `COMPLETED`*
*Type: `Scheduled`*
*Linked Audit: `Audits/2026-06-12-score-entry-audit.md`*

### What Was Done
- **P0-3 Fixed** — Replaced `reloadForm()` form POST with AJAX + GET redirect. New `GET /scores/tees-json/<course_id>` endpoint returns nine-options JSON. Course dropdown now AJAX-populates tee dropdown (~200ms, no reload). Tee dropdown change redirects via `?course_id=X&tee_id=Y` GET (cleaner than POST). Route reads `request.args` so GET query params load the scorecard correctly.
- **P0-4 Fixed** — Added `validateScores()` pre-submit hook on form `onsubmit`. Checks all `.score-input` elements before submit; highlights missing cells with red outline (`.score-cell-missing` class); shows inline message "N scores missing — fill all holes before saving"; on mobile (≤900px) navigates to first missing hole via `window._mobGoToHole`. Missing highlight clears when the cell gets a value.
- Also fixed pre-existing template bug: `.team-pill--1 { background:` CSS was incomplete (truncated); added colors for both team pills + closed `</style>` tag.
- Marked P0-3 and P0-4 as Fixed in audit file
- Checked off WP5.0 tasks, updated progress count (18 → 20/36)

### Decisions Made
- Course change = AJAX only (no reload) — tees are lightweight JSON
- Tee change = GET redirect (not AJAX) — scorecard HTML is complex Jinja2, rebuilding in JS would be a larger effort; with P0-1 pre-setting the tee at matchup time, this reload path is now an edge case

### What's Next (WP5.1 — Data Integrity)
- P1-1: Wrap handicap recalc in try/except
- P1-2: Add `UNIQUE(matchup_id)` to `rounds` table
- P1-3: Backfill `player_absences.round_id` inside the round creation transaction
- P1-4: Validate per-player tee belongs to the matchup's course

**P0 STATUS: ALL 4 actionable P0 items complete. P0-5 (mobile swipe) remains deferred.**

-----

## 2026-06-12 - Score Entry Audit Remediation — P0-1 + P0-2 (Scheduled Session)

*Status: `COMPLETED`*
*Type: `Scheduled`*
*Linked Audit: `Audits/2026-06-12-score-entry-audit.md`*

### What Was Done
- **P0-1 Fixed** — Updated `edit_matchup` GET+POST in `schedule.py` to load/save `course_id` and `tee_id`; added Course+Tee dropdowns to `edit_matchup.html` with `onchange` reload for tee list; shows "no tees defined" hint when applicable. Score entry route already used `matchup['course_id']` and `tee_id` as defaults — no further change needed there.
- **P0-2 Fixed** — Restructured `enter.html` so `<form id="score-form">` opens *before* the absence section; removed separate `<form id="absence-form">` and "Save Absences" button. In `scores.py`, moved inline absence processing block to *before* the tee validation check so absences save even if tee isn't set.
- Marked P0-1 and P0-2 as Fixed in audit file
- Checked off WP5.0 tasks, updated progress count (16 → 18)

### Decisions Made
- Per-player tee validation (P1-4) deferred — validates against wrong-course, not a P0 concern
- Edge case: admin marking new sub + entering scores in same submit shows one extra redirect (known acceptable; second submit succeeds)

### Blockers / Issues
- None

### Next Session
- P0-3: AJAX course/tee selection (replace `reloadForm()` POST with fetch)
- P0-4: Pre-submit validation indicator (highlight empty cells, disable Save until complete)
- P1-1: Wrap handicap recalc in try/except
- P1-2: Add `UNIQUE(matchup_id)` to rounds table

-----

## 2026-06-12 - UI/CSS Responsive Quality Pass (Session 3)

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Created `1. Project Management/Audits/2026-06-12-ui-css-audit.md` — 43 findings across P0–P3
- Added `viewport-fit=cover` to `base.html` viewport meta tag (required for iOS safe area insets)
- **Global responsive fixes** — appended new `@media (max-width: 768px)` + `@media (max-width: 480px)` block to `main.css`:
  - `main` padding reduced (0 24px → 0 14px at 768px, 0 10px at 480px)
  - `.page-header` flex row now wraps on mobile
  - `.header-actions` wraps with tighter gap on mobile
  - `archive-grid` and `courses-grid` collapse to single column at 768px
  - `settings-grid` collapses to single column at 480px
  - `ov-stat-grid` 2-column at 480px
  - `filter-group select` min-width removed on mobile (width: 100%)
  - `matchup-row` wraps on mobile
  - Word-break on player emails, forum body, list cards
- **Standings subnav** — added `overflow-x: auto` so tabs scroll instead of overflow
- **iOS safe area** — `mob-save-bar` and `sch-edit-save-bar` both use `env(safe-area-inset-bottom)`
- **Font size bumps** — `stroke-dot` 6px → 8px, `sc-pt-indicator` 10px → 11px
- **Template overflow wrappers** — added `<div class="table-scroll">` to courses/detail.html tee table and scores/enter.html absence table
- Updated CLAUDE.md breadcrumbs with new UI audit entry

### Decisions Made
- P2/P3 findings (touch target sizing, color palette consolidation, border-radius standardization) deferred to future polish pass — not critical for overflow/layout correctness

### Blockers / Issues
- None

### Next Session
- P0 score entry audit items (WP5.0): pre-set Course+Tee, combine absence form, AJAX reload
- SERIAL sequence desync fix in init_db.py (WP3.1)
- Optional: P2 UI polish pass (touch targets, color palette)

-----

## 2026-06-12 - UX Fixes + Audit Remediation Setup (Session 2)

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Fixed white-on-white table headers sitewide — `--green-dark` CSS variable was never defined in `:root`; added `:root` block with `--green-dark`, `--border`, `--card-bg`
- Compressed weekly schedule table on mobile: hide Hole/Course/Side/Tee Time/Hdcp columns at ≤768px via `sch-hide-mobile`; tighter padding/font
- Schedule edit mode + name toggle sync: added `TEAM_ID_NAMES` / `TEAM_ID_NUMS` JS maps; `applyNamesToSelects()` keeps dropdowns in sync with the Show Player Names toggle regardless of order
- Fixed `AttributeError: '_PgRow' object has no attribute 'get'` on add-week route (schedule.py:447) — replaced `(fetchone() or {}).get()` with explicit row variable + bracket access
- Deferred P0-5 (mobile swipe) from audit remediation — noted as future touchpoint
- Discussed name display format admin setting (First/Last toggle) — assessed as medium effort; not started; queued for future session

### Decisions Made
- P0-5 (swipe navigation) parked indefinitely — user wants to understand scope better first
- Name display format (First vs Last) is a future feature; approach = Jinja2 filter + `league_settings` column + admin toggle

### Blockers / Issues
- None

### Next Session
- P0 audit items: pre-set Course+Tee on matchup, combine absence+score submit, AJAX course/tee reload (WP5.0)
- Name display format admin setting if prioritized
- SERIAL sequence desync fix in init_db.py (WP3.1)

-----

## 2026-06-12 - Score Entry Audit + PM Framework Extension

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Ran comprehensive audit of the score entry process (entry flow, data integrity, validation, edge cases) — 20 findings across 5 priority tiers (P0–P4)
- Created `1. Project Management/Audits/2026-06-12-score-entry-audit.md` — new PM artifact type
- Added Phase 5 (Score Entry Audit Remediation) to Work Packages — 5 WPs, 20 tasks
- Updated `BetterGolfLeagueTracker/CLAUDE.md` with "Open Audits" breadcrumb so future sessions surface the audit automatically
- Extended `X.-Claude-Project-Framework/CLAUDE.md` with "Optional PM Artifacts" section covering Audit and Branched Plan conventions; updated Session Start Routine
- Created CronCreate scheduled task (weekly, Mondays) to work through audit findings under PM tracking strategy
- Also shipped: hamburger mobile nav, delete course feature, schedule edit-mode visibility fix (pushed to `postgres-migration`)

### Decisions Made
- Audit findings are a first-class PM artifact: dedicated file in `Audits/` sub-folder, always linked to a WP phase, lifecycle tracked in the file header
- Branched Plans (`Plans/` sub-folder) codified in framework for parallel implementation path evaluation
- Breadcrumb pattern: live references in CLAUDE.md updated as audits open/close — ensures Claude loads them every session

### Blockers / Issues
- None

### Next Session
- P0 audit items (score entry flow): pre-set Course+Tee on matchup, combine absence + score submit, AJAX course/tee reload elimination (WP5.0)
- SERIAL sequence desync fix in init_db.py (WP3.1, pending)

-----

## 2026-06-12 - UX Improvements + Feature Additions

*Status: `COMPLETED`*
*Type: `Scheduled`*

### What Was Done
- **login_code feature**: unique human-readable league identifier added to create-league form (required, case-insensitive, stored lowercase); admin can edit via new League Profile page (`/admin/league-profile`); UNIQUE constraint migration in `app/migrations/add_login_code_unique.sql`; `schema_postgres.sql` updated
- **Player add UX**: Enter on last name submits, form redirects back to add page (stay in rapid-entry flow); optional email/handicap collapsed in `<details>`; "Next step: create teams" banner on roster when ≥2 players exist but no teams yet for current season
- **Team nickname**: removed required validation (add + edit routes); stores `''` instead of NULL to satisfy DB NOT NULL constraint
- **Schedule week management**: "Add Week" button (inline panel) adds play weeks (greedy least-played-together pairing) or League Bye weeks (no-games placeholder); "Remove Week" available on any non-completed week in yearly and weekly views
- **Bug fixes**: Postgres GROUP BY errors in admin overview (standings leader + recent rounds queries); register route `last_insert_rowid()` → `RETURNING user_id`; team_name NOT NULL handled
- **SERIAL sequence desync** (recurring issue): players_pkey hit; corrected comprehensive setval SQL provided to user; root fix (add setval block to init_db.py) logged as pending task

### Decisions Made
- Team nickname stored as `''` not NULL — `team_name` column is NOT NULL in DB, no migration needed
- Schedule "add play week" uses greedy min-played-together algorithm (not round-robin continuation) since original shuffle order is not recoverable

### Blockers / Issues
- SERIAL sequences desync whenever seeded data is inserted with explicit IDs — need setval block in init_db.py (logged as WP3.1 task)

### Next Session
- Fix SERIAL sequence desync in init_db.py (add setval block after seeding)
- Continue UX/feature work per WP3.1 backlog

-----

## 2026-06-11 - Framework Migration + PM Setup

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Cloned repo to WSL2 Ubuntu environment (`~/projects/`)
- Set up SSH authentication for GitHub pushes
- Created `1. Project Management/` folder with all 5 framework PM files (this session)
- Added two-instruction-file cleanup notes to framework repo `_framework-dev.md`

### Decisions Made
- Claude Code (CLI) is the interface for this project — `Cowork Instructions - Paste.md` can be deleted
- PM files synthesized from: `Handoff.txt`, `StatusReport.md`, `migration.md`, `gap_analysis.md`, `memory.md`

### Blockers / Issues
- None

### Next Session
- Resume Postgres migration smoke testing (WP1.3)
- Run pending manual migration scripts if still on SQLite (WP2.1)

-----

## 2026-06-11 - Postgres Migration Phase 5 (Deploy + Smoke Test)

*Status: `COMPLETED` (smoke test loop ongoing)*
*Type: `Scheduled`*

### What Was Done
- Configured Render dashboard (postgres-migration branch, DATABASE_URL → Supabase port 6543)
- Deployed live at https://bettergolfleaguetracker-oceb.onrender.com
- Smoke-test fix loop: fixed Postgres GROUP BY strictness bugs across 13 route files
- Fixed HAVING alias issues in records.py, stats.py
- Fixed `division` → `division_name` column mismatch in league_info.py
- Fixed `tiebreaker_settings` key mapping bug (priorityN → priority_N)
- Final grep re-pass: fixed 3 additional GROUP BY issues in reports.py and my_stats.py
- Leftover `?` placeholder pass: fixed 3 files (handicap.py, players.py, stats.py)

### Decisions Made
- Supabase connection pooler port 6543 required (direct 5432 is IPv6-only on Render)

### Blockers / Issues
- Runtime verification of remaining pages still needed (league_info, archive, public_view, playoffs, admin, email, API, my_stats)

### Next Session
- Verify remaining pages on live Render deploy
- Confirm schema applied correctly, then move to data migration (Phase 4)

-----

## 2026-06-10 - Phase 2 Completion + schema_postgres.sql

*Status: `COMPLETED`*
*Type: `Scheduled`*

### What Was Done
- Completed remaining Phase 2 code changes (admin.py datetime, players.py PRAGMA, migration.py lastrowid, courses.py/forum.py RETURNING)
- Wrote `schema_postgres.sql` — 51 CREATE TABLE statements in Postgres syntax
- Verified schema via sqlglot postgres-dialect parse (0 errors)
- Added `init_db.py` Postgres branch (`_init_db_postgres()` + `_seed_if_empty()` dialect param)

### Decisions Made
- `run_migrations()` short-circuits under Postgres (tables from schema_postgres.sql, not migrations)

### Blockers / Issues
- None

### Next Session
- WP1.3: Deploy on Render, smoke test

-----

## 2026-06-21/22 — Mobile Score Entry Polish + Print Scorecard Fixes

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**Mobile score entry (enter_week.html) — Builds 20260622a–d:**
- `table-layout: fixed; width: 100%` enforced so explicit column widths hold
- Hole columns narrowed to 18px; `-webkit-appearance: none` removes iOS input inflation
- `.sp-badge:empty { display: none }` eliminates phantom 14px padding in name cell
- Score input borders removed (grid serves as visual container)
- Summary cols (Tot/Hcp/Gross) set to identical explicit 36px; name col gets ~89px remainder on 375px phone
- `"Hdcp"` header abbreviated to `"Hcp"` throughout
- Hole handicap row (sc-row-hcpidx) restored between Par and player rows, labeled "Hcp"

**Tee picker in player name popup (base.html):**
- `SP_TEE_OPTIONS` injected from `player_tees` Jinja variable in enter_week.html
- Tee row added to sp-popover; visible only when course has >1 tee option
- `spOnTeeChange()` syncs selection back to hidden form field + triggers ewUpdatePlayerTee

**Course/side bar (enter_week.html + main.css):**
- Both dropdowns fit on one line on mobile via `flex-wrap: nowrap` + label hidden

**More Tools dropdown (main.css):**
- Panel set to `position: static; width: 100%` on mobile — no longer clips off-screen

**Admin panel — Submissions button (admin.py + season.html):**
- Button gated on `self_reporting_enabled`; fetched fresh in `panel()` route

**Print scorecards (scores.py + print_scorecards.html):**
- 9-hole fix: filter tee rows by `auto_nine` (Front/Back) from matchup's assigned tee
- Group format: 1 card per group (all 4 players)
- Matchup format: 2 cards per matchup (1 per pair)
- Card header: all 4 first names, centered, not bold
- Bold divider line restored between pairs in group format
- M/W handicap cell positions corrected

**Branch + deploy:**
- Merged `postgres-migration` → `main` (fast-forward, 186 commits)
- User directed to switch Render deploy branch to `main`

**Undo rain-out route (schedule.py):**
- `POST /<season_id>/week/<week_num>/undo-rain-out` sets week_type back to 'Normal'

### Decisions Made
- `table-layout: fixed` is required for explicit hole column widths to hold on mobile
- Column budget on 375px: 9×18px holes + 3×36px summary = name gets ~89px

### Blockers / Issues
- None

### Next Session
- Run `migrate_api_tables.py` against Supabase
- APNs credentials (Apple Developer portal → Render env vars)
- TestFlight archive + upload
- WP5.4 P4-2: cascade-delete FK review in Supabase

-----

## 2026-06-17/18 — iOS Build Fixes + API Infrastructure + CSS Audit Completion

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done

**iOS build fixes (3 errors):**
- `ScorecardScanner.swift`: `some UIViewController` → `UIViewController` (two concrete return paths)
- `Models.swift`: Added `seasonId`/`seasonName` to `StandingsResponse` with `CodingKeys` to match API snake_case
- `ScoreInputView.swift`: Extracted `playerScoreSections()` and `submitSection()` as `@ViewBuilder` helpers to resolve compiler type-check timeout; also fixed `HierarchicalShapeStyle`/`Color` ternary mismatch in toolbar

**API cache & rate limiting (WP6.1 + WP6.2):**
- Added `course_api_cache` and `api_request_log` tables to `schema_postgres.sql`
- Created `migrate_api_tables.py` migration script
- Rate limiting scoped per-league: 45 calls/month hard cap + 30-second throttle between calls
- Added 3 covering indexes for fast per-league month/recency queries
- `_monthly_request_count()` and `_seconds_since_last_request()` both filter by `league_id`

**API request log page:**
- `/courses/api-log`: monthly usage bar (orange 70%, red 90%), 6-month summary table, last 200 individual calls with status chips
- Admin overview quick-links: added "📡 API Log" button

**CSS audit — P2 + P3 complete:**
- Expanded `:root` with full variable system: `--green-*`, `--red`, `--orange`, `--radius-sm/md/lg`, `--touch-min`
- Touch target fix: `@media (pointer: coarse)` enforces `min-height: 44px` on nav/tab/filter elements
- All P2 and P3 audit items marked complete; audit status → Complete

### Decisions Made
- API limits are per-league (not global) — each league gets 45 req/month + 30s throttle
- CSS variable system established for future new code; legacy inline values are a future cleanup pass

### Blockers / Issues
- None

### Next Session
- Run `migrate_api_tables.py` against Supabase to activate cache/log tables in prod
- Score CSV import for 18-hole rounds (WP item)
- API key management UI (`migrate_api_key.py` already run)

-----

## Sessions 1–15 (Automated) — Core Feature Build (2026-05-26 to 2026-06-07)

*Status: All `COMPLETED`*
*Type: `Scheduled`*

38 features built across 15 automated sessions. Full history in `build-log.md` and `gap_analysis.md`.
Final feature: Playoff bracket print view (session 15, 2026-06-07).

-----
