# Work Packages

*Progress: [189 / 221 active tasks complete] — WP3.20 complete (code) — live spot-check pending @user; WP5 complete; iOS Sprint 1 complete; WP3.3 complete; WP3.4 complete; WP3.5 complete; WP3.6 complete; WP3.7 complete; WP3.8 complete (code) — live rebuild execution pending @user; WP3.9 complete (code) — migration run + spot-check pending @user; WP3.10 complete (code) — spot-check pending @user; WP3.11 complete (code) — spot-check pending @user; WP3.12 complete (code) — handicap rebuild + spot-check pending @user; WP3.13 complete (code) — re-check + spot-check pending @user; WP3.14 complete (code) — handicap rebuild + spot-check pending @user; WP3.15 complete (code) — migration run + handicap rebuild + spot-check pending @user; WP3.16 complete (code) — spot-check pending @user; WP3.17 complete (code) — deploy confirmation pending @user; WP3.18 complete (code) — spot-check pending @user; WP3.19 complete (code) — handicap rebuild + spot-check pending @user*

-----

## Phase 0: Core Feature Build *(COMPLETE)*

All 38 features from GLT gap analysis built across sessions 1–15. See `gap_analysis.md` for full list and `build-log.md` for session-by-session history. Highlights:
- [x] Auth, Players, Seasons, Teams, Schedule, Score Entry, Standings, Handicap Engine
- [x] Skins, Playoffs, Courses, Admin Panel (250+ settings), Reports, Records, Stats
- [x] Individual user accounts, Forum, Email notifications, Sub requests, Self-reporting
- [x] Stableford scoring, Custom contests, Dues tracking, Tee sheet, Notifications center
- [x] Playoff bracket print view (last feature added, session 15 — 2026-06-07)

-----

## Phase 1: PostgreSQL Migration

### WP1.1 - Audit & Code Changes *(COMPLETE)*
- [x] @claude - Inventory all SQLite-specific syntax (routes/, migration scripts)
- [x] @claude - `?` → `%s` placeholders (1590 replacements across 36 files)
- [x] @claude - SQLite date functions → Postgres equivalents
- [x] @claude - `INSERT OR IGNORE` → `INSERT ... ON CONFLICT DO NOTHING`
- [x] @claude - `PRAGMA table_info` → `information_schema.columns` (dialect-branched)
- [x] @claude - `sqlite3.Row` → `RealDictCursor` in database.py
- [x] @claude - `lastrowid` → `RETURNING` clause for psycopg2
- [x] @claude - `last_insert_rowid()` → `RETURNING season_id` dialect branch
- [x] @claude - Fix dynamic `?` placeholders in string-concatenated queries (handicap.py, players.py, stats.py)

### WP1.2 - Schema Creation *(COMPLETE)*
- [x] @claude - Write `schema_postgres.sql` (51 CREATE TABLE statements; AUTOINCREMENT → SERIAL)
- [x] @claude - Verify schema via sqlglot postgres-dialect parse (0 errors)
- [x] @user - Confirmed all tables present in Supabase dashboard (2026-06-12)

### WP1.3 - Deploy & Smoke Test *(COMPLETE)*
- [x] @user - Set up Render dashboard (build cmd, start cmd, env vars, DATABASE_URL → Supabase port 6543)
- [x] @user - Push to GitHub, trigger Render deploy — live at https://bettergolfleaguetracker-oceb.onrender.com
- [x] @user/@claude - Smoke test + fix loop complete — all pages verified working (2026-06-12)
- [ ] @user - Point domain / share demo URL (Render free tier spin-down is acceptable for demo; upgrade to paid when going live)

-----

## Phase 2: Pending Manual Steps

### WP2.1 - Migration Scripts *(SKIPPED — Postgres-only going forward)*
- [x] @user - Confirmed SQLite data is test data only — no migration needed; schema_postgres.sql is source of truth

### WP2.2 - Data Migration *(SKIPPED — no real data to preserve)*

-----

## Phase 3: Feature Backlog

### WP3.0 - Print Scorecards *(Complete)*
- [x] @claude - `GET /scores/print-scorecards` — auto-detects week, loads matchup/tee/hole data, computes playing handicaps + stroke dots
- [x] @claude - `print_scorecards.html` — screen controls + `@media print` scorecard layout (yardage rows, M/W HCP, Par, player rows with superscript HCP + stroke dots, signature block)
- [x] @claude - Format toggle: Group (1 card) vs Per Matchup (2 copies) + dashboard tile (admin-only)

### WP3.1 - Near-Term Features *(In Progress)*
- [x] @claude - Add psycopg2 connection pool (ThreadedConnectionPool) to database.py
- [x] @claude - login_code: unique human-readable league identifier; required on create-league; editable via admin League Profile page; UNIQUE constraint (migration: add_login_code_unique.sql)
- [x] @claude - Player add UX: keyboard-driven flow (Enter submits, returns to add form), optional fields collapsed, "Next step: create teams" banner on roster when teams not yet created
- [x] @claude - Team nickname no longer required (add + edit)
- [x] @claude - Schedule: add play weeks (greedy min-played pairing) or league bye weeks; remove non-completed weeks
- [x] @claude - Fixed multiple Postgres GROUP BY bugs (admin overview, standings, playoffs, records, stats)
- [x] @claude - Fixed register route: lastrowid → RETURNING user_id
- [x] @claude - SERIAL sequence reset on fresh deploy — add setval() block to init_db.py so sequences don't desync after seeding (recurring manual fix workaround: run reset SQL in Supabase)
- [x] @claude - Per-player preferred tee: `preferred_tee_name` on players (migration), player edit form dropdown, score entry pre-populates per hierarchy (league default < player preferred); nine resolved from matchup context
- [x] @claude - Scoring Debug page: per-matchup hole-by-hole breakdown, overall-point box, stored-totals cross-check (2026-07-02)
- [x] @claude - Rename "Calc Debug" → "Hcp Debug"; add "Scoring Debug" button to scorecards page (2026-07-02)
- [x] @claude - Score Sheet sticky name: pure-CSS split-cell (lastname scrolls, firstname sticky left:0); removed JS scroll listener (2026-07-02)
- [x] @claude - `.claude/settings.json` + Tech Reference: lock development to `main`, no feature branches (2026-07-02)
- [ ] @claude - Score import CSV for 18-hole rounds
- [ ] @user/@claude - Skins flights: investigate existing skins system (currently one pot); design handicap-divided flight system (e.g. low/mid/high hcp tiers get separate skins pots); check if `flights` concept already exists in schema
- [x] @claude - Season-end podium graphic — web `GET /standings/<id>/podium` + `podium.html` (dark golf theme, 2nd/1st/3rd display order, share view); iOS `PodiumView` + `PodiumGraphic` + `ImageRenderer` PNG share via `ShareSheet`; `GET /api/v1/standings/podium` JSON endpoint
- [x] @claude - iOS Profile tab — `ProfileView` (name, email, role, league, sign out); enhanced `/auth/me` to return display_name + email + league_name; `AuthViewModel.loadProfile()` called on login + launch
- [x] @claude - Standings copy for group text — "Copy for Group Text" button + Show/Hide Record toggle (web JS clipboard + iOS share sheet); W-L-T added to `_standings_rows` query; points rounded to int
- [x] @claude - iOS season selector — `GET /api/v1/seasons/list` + `GET /api/v1/seasons/<id>/standings/mobile`; season picker menu in Standings toolbar; `StandingsViewModel.loadSeason()`
- [x] @claude - Golf Course API cache (WP6.1) — `course_api_cache` table (migration); check cache before API call; upsert on fetch; 90-day TTL
- [x] @claude - Golf Course API rate limit guard (WP6.2) — `api_request_log` table; monthly count cap (45 req); 429 on limit; usage meter shown in Add Course UI (turns red near limit)
- [x] @claude - UI/CSS audit P2–P3 — touch targets ≥44px (btn/nav-link), color variable consolidation, schedule-filter-bar 360px fix, col-player-name ellipsis, ov-stat-grid 320px single col, ov-stat-label 12px, form-group min-width:0 mobile, text-overflow ellipsis on player cells, forum-row-meta wrap
- [ ] @user/@claude - API key management UI (column added via migrate_api_key.py)
- [ ] @user/@claude - In-app site wiki for members/admins (plain-language explanations — handicap calc, scoring rules, etc.) — distinct from Technical Reference.md, which is dev-only. Not yet designed (2026-07-02).
- [ ] @user/@claude - Admin control over what shows on members' dashboards: doesn't exist today — the member dashboard is a fixed, hardcoded subset (`session['role']` checks in `dashboard.html`/`main.py`); the only precedent, `show_dues_shame_widget`, is a single-purpose flag on the Dues page, not a general mechanism. Would need a real per-widget visibility system built from scratch if wanted (added 2026-07-03)
- [ ] @claude - **Bug (not a gap):** Stableford scoring mode never activates — `_settings_scoring_mode()` (`scores.py:154-162`) reads the `scoring_type` column (net/gross) instead of `scoring_mode` (match_play/stableford), so selecting "Stableford" in league settings silently has zero effect on live scoring. Found 2026-07-04 during the GLT settings audit (`7. GLT Feature Parity.md`) — fix is a one-line column-name correction, higher priority than the parity-gap items below since it's a real defect in a shipped feature (added 2026-07-04)
- [ ] @user/@claude - GLT feature-parity settings audit: broad pass complete for all categories; deep-dive (full GLT descriptions, not just names) done for Handicap Settings, Hole Stroke Limits, Match Stroke Limits, Match Play Points, Stableford Points, General Point Rules, Low Net, Skins, Absent Players and Subs (`7. GLT Feature Parity.md`). Surfaced 5 dead settings total this session beyond the known WHS dropdown (`max_score_over_handicap`; `match_play_points_per_hole`/`match_play_overall_point`; **`ab_designation_method`**, most consequential — determines A/B pairing and points every week and is never read by the engine; `skins_config.handicap_percent`). Absent Players/Subs came back with the starkest gap ratio found so far (10 of 12 settings are real gaps, all variations of a missing point-clamp mechanism). @user to review; Fable to plan gap-closure work (explicitly requested to route through Fable, not build directly from this audit)
- [ ] @claude - Continue GLT settings deep-dive, one bite-size category group per pass (per @user's explicit "not all at once" instruction): Skins and Absent Players/Subs done 2026-07-04; Schedule (2) and Beta Testers (1) first-look done same day (Beta Testers confirmed not applicable — GLT-internal beta flag, not a league feature). Standings (8), E-mails (10), League Size and Length (6), General/Administrative/Scorecards/Signup (16) still broad-pass-only (setting names, not full descriptions) — candidates for the next pass. All 24 categories now have at least a first look.
- [ ] @user/@claude - Remaining GLT site-map categories not yet reviewed: Schedule/Results and Admin pages in `7. GLT Feature Parity.md` are still bare TBD — "Starting a New Season" and "League Year-End Maintenance" how-to articles flagged as priority reviews (direct overlap with WP3.20)
- [x] @claude - Admin settings: "Does this league use handicaps?" replaces "Handicap Scoring Type"; Require Approval dims until Allow Members is checked
- [x] @claude - Score entry tee dedup: player dropdown shows one option per color (no M/W/Front/Back); print scorecards extra tees truly additive (auto_color fallback fix); course default tee selector also deduped
- [x] @claude - Course default tee: `courses.default_tee_id` column + set-default-tee route + course profile UI; score entry hierarchy: course default → player preferred
- [x] @claude - Player profile links in score entry team headers (inherit bold style, underline on hover)
- [x] @claude - Schedule bulk-edit save bug: week-level metadata (date/type/course/side) no longer blocked by `status != completed`; enter_week_current UTC midnight bug fixed (−6h grace); Clear Schedule moved to edit-mode save bar with two-step confirm
- [ ] @user/@claude - `diff_calculation_type='whs'` dead option: `admin/settings.html` offers a WHS (slope/rating) dropdown choice, but `handicap.py`'s engine always computes par-based differentials (gross_total − par_total) regardless — selecting WHS currently does nothing. Decide: implement real WHS slope/rating math, or remove the dropdown option. (Found 2026-06-30 during WP3.12 handicap bug fix.) **Update 2026-07-04**: GLT's own docs (`7. GLT Feature Parity.md`) confirm course-difficulty/slope-adjusted handicapping ("2020 Rules (Custom)") is GLT's own *recommended default*, not a niche feature — leans toward implementing it over removing the option.
- [x] @claude - UX consistency pass: unified `.status-badge` (deleted dead CSS block, consolidated `ew-*-badge` trio), promoted `.dbg-badge`, renamed page-local badge classes, fixed unstyled `class="table"`, standardized "Hdcp" column labels + "provisional handicap" tooltip copy, corrected stale mobile-layout doc claim (`Audits/2026-07-03-ux-consistency-pass.md`)
- [x] @claude - Override-clear atomicity fix: `clear_scorecard_overrides()` and `clear_handicap_override()` (`app/routes/handicap.py`) now commit once after rebuild success (mirrors `matrix_update()`'s WP3.19 fix), rollback + visible error flash on failure — same bug class, lower severity (`Audits/2026-07-03-override-clear-atomicity.md`)
- [x] @claude - Sub popover dead-script fix: removed duplicate `{% include 'components/sub_popover.html' %}` from `subs/manage.html` (its script had smart-quote syntax errors, dead on arrival, and duplicate `sp-*` DOM ids masked base.html's live popover); deleted the stale component file; page now renders base.html's single canonical popover only. Found and deliberately left alone: `sub_new_name_{pid}` (free-text "+ New Sub" entry) has no hidden input on this page and `subs.py`'s `manage()` POST handler doesn't read that field at all (unlike `scores.py`'s `_process_absences`) — pre-existing gap, not a regression, flagged for a follow-up decision rather than fixed blind (`Audits/2026-07-03-sub-popover-dead-script.md`)
- [x] @claude - "+ New Sub" free-text now works on Subs → Manage (user chose option a): extracted `resolve_or_create_sub_player()` shared helper into scores.py (refactored `_process_absences` to use it, behavior identical), wired it into `subs.py manage()` POST, added the missing `sub_new_name_{pid}` hidden input to the page (2026-07-03)
- [x] @claude - Consolidation & cleanup batch: extracted `_hcp_marker_map()` (scores.py, 3 duplicate sites → 1), shared `spTipToggle()` JS helper (base.html, 3 duplicate tooltip implementations → 1), promoted `.hcp-override-dot` to main.css (2 duplicate CSS copies → 1), deleted dead `enter_week_backup.html` + `my_stats.py`'s unused `mrs` + `players.py`'s unreachable `_get_player_compare_stats`/`_get_h2h`, recorded legacy handicap-function caller map in Technical Reference. Crossing-round `hcp_marker` value (item 4) skipped per its own stop condition — needs @user sign-off on a schema addition (`Audits/2026-07-03-consolidation-cleanup-batch.md`)
- [x] @claude - Crossing-round marker decision resolved: user rejected the schema-column proposal on review (correctly identified the underlying `d8da205` self-only-temp-handicap mechanism as based on a false "formula loop" premise) and chose to simplify instead. `rebuild_player_handicap_timeline()` (`handicap.py`) reworked so the crossing round uses its own freshly-computed real index directly (no self-only temp mechanism, no marker needed — no divergence left to mark). Verified end-to-end against a fresh seeded DB. `Audits/2026-07-03-crossing-round-marker.md` marked OBSOLETE (2026-07-03)
- [x] @claude - Mobile score-entry unification, both parts complete: Part 1 deleted the dead, invisible hole-by-hole card view from `enter.html` (zero visible change). Part 2 gave `enter.html` the same compact `fit()` mobile table as `enter_week.html` via shared extraction (`fit()` → base.html, shared CSS → main.css, `sc-completed-view` rename); also fixed a real CSS-cascade bug the naive port would have introduced (duplicate `.hcp-cell-wrap` base rule). Verified via Jinja parse, brace balance, live Flask test-client renders. `Audits/2026-07-03-mobile-entry-unification.md` → Complete (2026-07-03)
- [x] @claude - Local dev environment: `.claude/hooks/session-start.sh` (installs deps into `.venv`, starts local Postgres, provisions `golf_league_dev`) + `app/seed_dev_db.py` (drives real `/scores/enter` route to seed 5 edge-case matchups) + Playwright smoke test confirming score entry/Handicap History/Matrix render correctly. Corrected Technical Reference's stale "SQLite local dev works" claim (it never did — every route uses %s placeholders, which raw sqlite3 rejects)
- [x] @claude - Found + fixed 6 real fresh-Postgres-DB bugs while getting local dev running (never caught before — app had apparently never been run against a genuinely empty Postgres DB): `_seed_if_empty()` sqlite cursor bug, circular FK in schema (`courses.default_tee_id`↔`tees`), 2 non-IMMUTABLE `DATE_TRUNC` indexes, no per-migration transaction isolation (one failure poisoned all subsequent migrations), 5 migration files never registered in the additive list (including `scorecards.hcp_manually_overridden` — load-bearing for this whole session's work), and `rebuild_league_handicaps_and_scores()` silently skipping every round in any season with no `league_settings` row instead of using defaults
- [x] @claude - Code-level audit of league creation + annual new-season rollover, focused on "easy new years": found and fixed 2 real bugs (`get_league_settings()` arg-swap in `admin.py`/`schedule.py` silently returning wrong/default settings for any season after a league's first; `schedule.py`'s single-course detection treating a missing `league_settings` row as multi-course, nulling out course/tee on early schedule generation). Root finding: `teams`/`league_settings` are season-scoped with zero copy/clone tooling — the actual source of annual tedium. `Audits/2026-07-03-league-creation-new-season-audit.md` (2026-07-03)
- [x] @user - Decide priority on the audit's Recommendations: user greenlit and expanded the rollover feature → executed as WP3.20 (2026-07-03)
- [ ] @user/@claude - Audit league creation process using a real reference case: create a league via the normal admin flow using the Buckeye league's GLT export data, and validate the result against that reference data (added 2026-07-03)
- [ ] @user/@claude - Test `migration.py`'s CSV import (players/teams/schedule/scores) against the Buckeye league GLT export — `@user` supplies the export files (added 2026-07-03)
- [ ] @claude - Reformat the Buckeye GLT exports to match `migration.py`'s expected CSV column formats, if the raw export doesn't already conform (added 2026-07-03)

### WP3.20 - Season Rollover & Pre-Season Setup *(Complete (code) — 2026-07-03; live spot-check pending @user)*
Fable-planned, Sonnet-executed (two sequential agents, handoff docs in `Audits/`).
- [x] @claude - WP-A: unify current-season context — shared `get_current_season_id()` (database.py) honoring `session['current_season_id']` with league validation; 12 "newest-by-id" route sites converted; `admin.overview()` left per stop condition (`Audits/2026-07-03-season-context-nav.md`)
- [x] @claude - WP-A: "Switch Season" label on drawer switcher + year prefix on league-name headers (drawer + dashboard, derived from start_date or name token)
- [x] @claude - WP-A: role-pill vertical alignment fix (merged duplicate `.page-header` CSS blocks, zeroed h1 bottom margin in flex row); verified via one Playwright screenshot pair
- [x] @claude - WP-B: "Start Another Season" wizard (`/seasons/start-next`) — year-incremented name prefill, single-transaction create with bring-over checkboxes (teams & divisions; league settings with dues amount kept / due date cleared; starting-handicap seeding gated on teams) (`Audits/2026-07-03-start-another-season.md`)
- [x] @claude - WP-B: Season Setup hub (`/seasons/<id>/setup`) — status cards for roster (flags inactive players on copied teams), player activation, settings, schedule, buy-ins, starting handicaps
- [x] @claude - WP-B: CTAs — dashboard admin card + admin-panel banner when season over (`season_is_over()` helper), "Continue season setup" link for incomplete newer seasons, seasons index primary button
- [x] @claude - WP-B: extracted `_seed_starting_handicaps()` shared helper (wizard + existing seed route, behavior identical)
- [x] @claude - WP-B: fixed player-delete guard 500 (queried nonexistent `team_members`; now checks `teams.player1_id/player2_id`)
- [x] @claude - Fixed rain-out columns missing from fresh DBs (`matchups.week_label`/`makeup_for_week` only existed via standalone `migrate_rain_outs.py`; applied the three-part checklist — schema + migration file + additive-list registration)
- [ ] @user - Live spot-check after deploy: switch seasons in the drawer and confirm pages follow; run the wizard for real when the season ends; confirm pill alignment on your phone

### WP3.2 - iOS App *(COMPLETE)*
- [x] @user - Platform decision: **SwiftUI (iOS 17+)** confirmed 2026-06-13
- [x] @claude - Full development plan: 9 phases, 7 sprint milestones
- [x] @user/@claude - Resolve open questions Q1–Q4
- [x] @claude - Sprint 1: JWT auth endpoints + core read API
- [x] @user - Sprint 1: Xcode project created
- [x] @claude - WP1.1-1.3: Auth flow end-to-end; JWT sub-as-string fix; CSRF exemption
- [x] @claude - Schedule + Standings views loading real data
- [x] @claude - Schema mismatch fixes (team1_id, handicap LATERAL join, ScheduleResponse weeks)
- [x] @claude - Tab restructure: Schedule, Standings, Score Entry (admin), Stats, Admin
- [x] @claude - Stats hub + all 5 sub-views wired to live API
- [x] @claude - Stats API endpoints: leaders, all-play, trend, weekly, records
- [x] @claude - Schedule: week filter dropdown (Week X — Apr 22), auto-land upcoming week
- [x] @claude - Score Entry: tee picker, 9-hole stepper grid, absence/sub sheet, submit
- [x] @claude - OCR pipeline: Vision OCR, ImagePicker, ScanResultView review flow
- [x] @claude - NicknameMatchService: Jaro-Winkler + stored nicknames player matching
- [x] @claude - Admin: PendingSubmissionDetailView, one-tap approve
- [x] @claude - Score lock/unlock: admin toggle in MatchupDetailView, backend endpoint
- [x] @claude - Push notifications: AppDelegate APNs, push.py sender, event triggers
- [x] @claude - Offline cache: UserDefaults JSON cache for schedule/standings (read-only)
- [x] @claude - Privacy keys: NSCameraUsageDescription + NSPhotoLibraryUsageDescription in pbxproj
- [x] @claude - AbsenceSheet: mark absent players, assign sub from league roster
- [ ] @user - APNs credentials: create .p8 key in Apple Developer portal, set Render env vars
- [ ] @user - TestFlight: archive build, upload, invite testers
- [ ] @user - WP5.4 P4-2: Review cascade-delete FK behavior in Supabase

### WP3.19 - Ghost Score Correctness: Edit Scores Fix, Matrix Atomicity, League-wide Stat Audit *(Complete — 2026-07-02)*
- [x] @claude - Root-caused reported bug (Wil & Rosie, Week 5, absent + handicap-overridden, Net far below par): `admin.py` "Edit Scores" tool had no ghost/absent awareness, pre-filled a possibly-stale ghost gross into an editable form, recomputed net using whatever handicap is currently stored
- [x] @claude - `_save_edited_scores()` now regenerates ghost gross fresh from the current handicap and sets net directly to par (established ghost convention), ignoring stale submitted/pre-filled values
- [x] @claude - `edit_scores.html`: ghost rows shown as non-editable with a "GHOST" badge instead of editable inputs whose edits get silently discarded
- [x] @claude - Fixed likely trigger: Handicap Matrix override tool (`matrix_update`) committed the override before attempting a resync, silently swallowing resync failures with no rollback/no surfaced error — now atomic (commit only after resync succeeds, rollback on failure) with a visible admin-facing warning
- [x] @claude - League-wide audit (3 parallel Explore agents) of every player-facing stat computation for ghost-score leakage; found + fixed 15 missed call sites across `reports.py`, `my_stats.py`, `players.py`, `standings.py`, `api.py`, `schedule.py` (rounds played, scoring averages, birdie/eagle/bogey counts, best/worst round, records/leaderboards)
- [x] @claude - py_compile (8 files) + Jinja2 template-parse validation across all touched files
- [ ] @user - **Critical**: run Rebuild Handicap Timeline (`/handicap/rebuild`) after deploy to retroactively repair already-corrupted `hole_scores` (e.g. Wil & Rosie's Week 5 round)
- [ ] @user - Spot-check Wil & Rosie's Week 5 Net shows exactly even par afterward; confirm editing an unrelated player's score on a round with a ghost via Edit Scores doesn't re-corrupt it; spot-check standings/records/my-stats pages for a player with an absent week

### WP3.18 - Handicap History: Standalone Rows, Provisional Marker in Score Entry, Override Transparency *(Complete — 2026-07-02)*
- [x] @claude - Handicap History page: surface `handicap_history` rows with no `trigger_round_id` (standalone manual overrides, off-cycle "Recalculate Handicaps" runs) that were previously invisible on the page
- [x] @claude - Fixed a "most recent N" slicing bug found in the same code (`list(reversed(history))[-limit:]` returned the oldest entries, not the most recent, once a player had more history than the selected limit)
- [x] @claude - Score entry (`enter.html`/`enter_week.html`, desktop table + mobile card view + bulk-entry table): clickable asterisk + tooltip marking a completed round's handicap as provisional, reusing the existing click-tooltip pattern
- [x] @claude - Handicap History: capture + display the pre-override calculated/provisional value alongside a manual override ("Calculated (provisional): X → overridden") — new `pre_override_index`/`pre_override_reason` columns, populated only on first override so re-editing doesn't clobber the original snapshot
- [x] @claude - Found + fixed a second instance of the WP3.17 bug class while implementing: `trigger_round_id`/`is_manual_override`/`override_reason`/`override_by_user_id`/`override_at` were never in `schema_postgres.sql` or any migration — only existed in production via an undocumented direct DB change; backfilled into base schema + new migration
- [x] @claude - py_compile + Jinja2 template-parse validation across all touched files
- [ ] @user - Spot-check Handicap History shows standalone rows + pre-override transparency correctly; spot-check the new provisional asterisk on a completed round

### WP3.17 - Production Crash Fix: Missing Migration Registration *(Complete — 2026-07-02)*
- [x] @claude - Root-caused production 500 on `/handicap/rebuild`: `add_temp_handicap_percent.sql` (WP3.15) was written but never registered in `init_db.py`'s additive-migrations list, so it silently never ran against the deployed DB
- [x] @claude - Registered the migration so it runs automatically on next Render redeploy (idempotent `ADD COLUMN IF NOT EXISTS`, no manual DB access needed)
- [x] @claude - Hardened `_get_settings()` to check column presence before indexing instead of raising `KeyError` on a genuinely missing column (previously only guarded against `NULL` values)
- [ ] @user - Confirm Render redeploy picks up the migration cleanly and `/handicap/rebuild` + the settings-save form both work post-deploy

### WP3.16 - Detailed Score Sheet Sticky Column Polish + Minor UI Fixes *(Complete — 2026-07-02)*
- [x] @claude - Sticky name column seam removal: dropped `dss-vl` divider + doubled border, rebalanced padding, right-aligned lastname cell (column auto-sizes to longest name in table per HTML table layout, causing a large unnatural gap for shorter names)
- [x] @claude - Sticky name column bleed-through fix: sticky cell's background was losing to higher-specificity translucent rules (team-row tint, absent-row opacity), letting scrolled content show through
- [x] @claude - Sticky name column color-mismatch fix: hardcoded hex was computed against white, but the table actually sits on the page's `#f5f5f5` background (`--bg-card` undefined everywhere, silently falling back to white) — replaced with a layered-background technique that's correct regardless of true background
- [x] @claude - Score Entry (`enter_week.html`) header buttons: prevent wrap on mobile via scoped nowrap + horizontal scroll
- [x] @claude - Scoring Debug page: legend moved from repeating per-pair to once at top of page; dropped a stale display-style reference
- [ ] @user - Spot-check the Detailed Score Sheet name column (at rest + while scrolling, team-tinted and absent rows) and the Score Entry header buttons on a real mobile device

### WP3.15 - Pre-Eligibility Temp Handicap + Settings Numbering Follow-ups *(Complete — 2026-07-02)*
- [x] @claude - New settings 2.10/2.11 (`temp_handicap_percent_member`/`_sub`, default 90.0 placeholder) — migration `add_temp_handicap_percent.sql`, schema/init_db/admin.py/handicap.py all wired
- [x] @claude - `rebuild_player_handicap_timeline()`: pre-eligibility rounds get an independent temp handicap (round's own diff × member/sub percent, via `calc_playing_handicap()` reused verbatim) instead of scoring at scratch; marked via `PRE_ELIGIBILITY_MARKER_PREFIX` in `override_reason`; does not carry into `entering` for later rounds
- [x] @claude - `_recalc_single_round()`: new `temp_ph_lookup` param, bypasses re-applying `calc_playing_handicap()` (would double-discount an already-final temp value)
- [x] @claude - `_detect_eligibility_rounds()` fixed to exclude provisional rows (would otherwise permanently stop firing once a temp row exists)
- [x] @claude - Found + fixed a real double-percent-application bug in 6 other spots while implementing (my_stats.py, players.py Formula Breakdown page ×2, api.py mobile twin) — none were part of the original ask, all would've silently broken once temp rows existed
- [x] @claude - Asterisk + tooltip marker (`.hcp-provisional-mark`) across standings, schedule, reports, my-stats, player profile/compare/handicap-detail, dashboard; CSV export gets `(provisional)` text; records "Lowest handicap ever" excludes provisional rows entirely
- [x] @claude - `handicap/player_history.html`: Provisional badge (distinct from Edited) for temp rows
- [x] @claude - ⓘ tooltip component (click-toggle, wiki-link placeholder to `/wiki#setting-2.10`/`2.11`) wired to the 2 new settings only
- [x] @claude - Restored the eligibility-round ℹ indicator dropped from `enter.html`/`enter_week.html` during an earlier merge, copy corrected for the new mechanism
- [x] @claude - Secondary fix: Scorecards page Net column pill reuses this page's own `sc-pt-indicator`/`pts-sb-*` classes instead of a different page's `score-pill` style
- [x] @claude - py_compile (11 files) + Jinja2 parse (18 templates) + node --check (5 script blocks) + worked-example arithmetic check across all touched files
- [ ] @user - **Critical**: run `app/migrations/add_temp_handicap_percent.sql` against Supabase before these settings work
- [ ] @user - Spot-check a new player's first round (regular + sub) gets correct temp handicap; settings page 2.10/2.11 + tooltip; restored score-entry ℹ indicator; asterisk markers across standings/schedule/profile
- [ ] @user - **Critical**: run full league-wide Rebuild (`/handicap/rebuild`) after deploy — retroactively corrects already-saved pre-eligibility rounds scored at scratch before this fix
- [ ] @claude - Backlog: retrofit ⓘ tooltip pattern onto the other ~36 existing settings fields (deferred this session per user scope decision)

### WP3.14 - Match Play: Differential Stroke Allocation *(Complete — 2026-07-02)*
- [x] @claude - Added `diff_match_hole_points()` shared helper (scores.py) — only the higher-handicap player in a pair gets strokes, equal to the handicap gap, allocated to hardest holes by rank (matches real singles match play; was previously each player's full playing handicap vs. par, then compared — a different result whenever a hole's rank falls within the two players' handicap gap)
- [x] @claude - Applied to all 7 write paths that create `match_results`: scores.py (`_process_scores`, `_recalc_single_round`), admin.py (`_save_edited_scores`), api.py (2 mobile endpoints), self_report.py, score_import.py
- [x] @claude - Fixed 4 display-only duplicates (would otherwise show ✓/✗ indicators inconsistent with corrected totals): scores.py `_load_completed_scorecard`, debug_scores.py `build_pair` + `debug/scoring.html`, reports.py, standings.py all-play view
- [x] @claude - Fixed client-side JS live preview in `enter.html`/`enter_week.html` (`updateAll`/`ewUpdateAll`) — was previewing absolute-net results that would've disagreed with the corrected saved result
- [x] @claude - Personal stats / "Net" column / hole_scores.net_score confirmed unaffected — stays each player's own gross minus their own full playing handicap (absolute), per user directive
- [x] @claude - py_compile + ast.parse (8 Python files), Jinja2 template-parse, node --check (2 JS blocks) validation across all touched files
- [x] @claude - **Correction (same day)**: overall point reverted to absolute net (stroke-play-style, opponent-independent) — only hole-by-hole win/loss stays differential. `diff_match_hole_points()` now takes `net_x`/`net_y` (already-computed absolute lists) alongside gross/ph; all 7 write-path call sites + `debug_scores.py build_pair` + JS `gpts` block updated; `debug/scoring.html` total row flips emphasis (abs net bold) since that's what the Overall point box uses
- [ ] @user - **Critical**: run Recalc Points (season-scoped, `/admin/season/<id>/recalc-points`) or Rebuild Handicap Timeline (league-wide superset, `/handicap/rebuild`) after this deploys — existing `match_results` for completed match-play rounds still reflect the pre-fix comparison
- [ ] @user - Spot-check a round with two different-handicap players: hole-by-hole result matches hand-calculated differential strokes; Overall point matches hand-calculated absolute net totals (not diff); live entry preview matches saved result; personal stats/Net column unchanged

### WP3.13 - Handicap Calculation Page: Player+Week Snapshot Dropdowns *(Complete — 2026-06-30)*
- [x] @claude - `/players/<id>/handicap-detail` now accepts `?week=<round_id>` to reconstruct the handicap calc as it stood entering that round (truncates the round list to everything strictly before the selected round, then runs the existing window/pad/drop/average pipeline unchanged)
- [x] @claude - Added Player dropdown (small inline JS navigates to the chosen player's URL, preserving the selected week) and Week dropdown (`<select onchange="this.form.submit()">`, consistent with `handicap/player_history.html`'s existing pattern) — replaces the prior one-URL-per-player, current-only page
- [x] @claude - Snapshot banner shows the reconstructed index/playing handicap alongside the actually-recorded `handicap_at_time_of_play` for that round (ground-truth cross-check); committee adjustments are skipped for past snapshots since they aren't tracked historically
- [x] @claude - All Rounds table highlights the selected snapshot week; Index History table scoped to `calculated_date <= snapshot date` when viewing a past week
- [x] @claude - Investigated the reported "Calculation Window widget appears blank" bug — traced the full data pipeline (query → all_round_data → combined_window → counting_diffs) and found Calculation Window and Formula Breakdown share the same data source, so one being empty while the other renders doesn't fit the code as written; no live DB/browser access to reproduce. Rewrote/re-verified the surrounding logic as part of this feature; flagging for user re-check after deploy rather than a blind fix
- [x] @claude - py_compile + Jinja2 template-parse validation across all touched files
- [ ] @user - Re-check the Calculation Window section after this deploys — confirm whether it's still blank for any player/week combination, and if so, send a screenshot (no live DB access available to Claude in this environment)
- [ ] @user - Spot-check a past-week snapshot for a player with several rounds: confirm reconstructed index/playing handicap line up with the recorded ground-truth value shown in the banner

### WP3.12 - Handicap Index/Playing-Handicap Double-Percent Bug Fix + Calculation Page *(Complete — 2026-06-30)*
- [x] @claude - **Bug fix**: handicap_percent and max_handicap_index were being applied when computing/storing `handicap_history.handicap_index`, then applied a second time by `calc_playing_handicap()` when converting Index → Playing Handicap — every player's real playing handicap was discounted by percent twice (e.g. 90%×90%=81% effective) and capped twice
- [x] @claude - Fixed both index-computation engines in handicap.py: `recalc_handicap_for_player()` and `rebuild_player_handicap_timeline()` now store the raw average as `handicap_index`, no percent/cap applied
- [x] @claude - Updated module docstring in handicap.py to document the corrected Index vs. Playing Handicap split and warn against re-introducing the bug
- [x] @claude - Fixed matching duplicate formula in players.py `handicap_detail()` route and api.py `mobile_handicap_detail()` (mobile JSON twin)
- [x] @claude - Adjacent fix: "Effective index" display never actually reflected the committee adjustment — now correctly computed as `effective_index = current_handicap + committee_adjustment` in both web and mobile routes
- [x] @claude - players.py + api.py now compute and expose `playing_handicap` (via `calc_playing_handicap()`) alongside the index, for parity between web and mobile
- [x] @claude - **Calculation page**: enhanced existing `/players/<id>/handicap-detail` page — added Playing Handicap summary card, restructured Formula Breakdown box to show Sum → Average → Index → (+ Committee Adjustment → Effective Index) → × Handicap % (capped) → Playing Handicap as the final step
- [x] @claude - Added "How is this calculated?" link from Handicap History page (`handicap/player_history.html`) to the per-player calculation detail page
- [x] @claude - py_compile + Jinja2 template-parse validation across all touched files
- [ ] @user - **Critical**: run Handicap Rebuild (`/handicap/rebuild`, admin-only — GET to preview, POST to commit) after this deploys — all existing `handicap_history` rows were computed under the old buggy double-application formula and are now stale
- [ ] @user - Spot-check `/players/<id>/handicap-detail` for a player with a committee adjustment, confirm Effective Index and Playing Handicap are correct

### WP3.11 - Week-Number Row on Handicap Matrix *(Complete — 2026-06-30)*
- [x] @claude - league_matrix() route: pull matchups.week_number per round-date column alongside round_date
- [x] @claude - New header row above the date row in league_matrix.html, showing "Wk N" per column, sticky-stacked above the date row
- [x] @claude - CSS: two-tier sticky thead (week row top:0, date/label row top:22px)
- [ ] @user - Spot-check: matrix header shows correct week numbers aligned to each date column

### WP3.10 - Editable Playing Handicap in Score Entry *(Complete — 2026-06-30)*
- [x] @claude - Editable Hdcp input in scores/enter.html (single-matchup admin form), both teams
- [x] @claude - Editable Hdcp input in scores/enter_week.html (bulk week form), reuses same row for in-progress/not-entered/reopened matchups
- [x] @claude - _process_scores: hcp_override_<pid> form field feeds the existing _hcp_overrides dict (same mechanism as the Handicap Matrix), compares to computed default to detect override vs. clear
- [x] @claude - Live client-side recalc (strokes/net/pts) on edit via existing PLAYERS/EW_PLAYERS JS objects
- [x] @claude - .hcp-override-input CSS styling
- [ ] @user - Spot-check: edit Hdcp in both score-entry views, confirm matrix shows override + survives a timeline rebuild

### WP3.9 - Absence Overall-Point Policy + Ghost-Score Stats Exclusion *(Complete — 2026-06-30)*
- [x] @claude - Admin setting absence_overall_point_policy (overall_wins / never_wins / excused_only), default excused_only (matches prior behavior)
- [x] @claude - Shared helper _apply_absence_overall_policy(): unifies the two prior hardcoded forfeit closures in scores.py
- [x] @claude - Settings accessor _settings_absence_policy(): defensive try/except default for pre-migration DBs
- [x] @claude - Migration: add_absence_overall_point_policy.sql + admin/settings.html UI control
- [x] @claude - Ghost-score (absent/no-sub) hole scores italicized via sc-player-row--ghost CSS class across scorecard/entry templates
- [x] @claude - Stats-exclusion sweep: handicap.py rebuild engine, players.py (7 queries), stats.py (8 queries), my_stats.py, records.py (5 queries) — gross/hole-level aggregates now exclude is_absent=1 rows
- [x] @claude - Conditional-aggregation fix for queries that mix gross stats with mr.total_points in the same row set (players.py round history + _season_stats, records.py season/career leaders) — points remain governed solely by the policy setting, not silently dropped by a blanket WHERE filter
- [x] @claude - skins.py deliberately left unchanged (manual opt-in per round, not an automated stats surface)
- [x] @claude - py_compile + Jinja2 template-parse validation across all touched files
- [ ] @user - Run app/migrations/add_absence_overall_point_policy.sql against Supabase
- [ ] @user - Spot-check a player/season with absence history (gross stats excluded, points unaffected) and verify the new admin setting UI

### WP3.8 - Chronological Handicap Rebuild Engine *(Complete — 2026-06-30)*
- [x] @claude - rebuild_player_handicap_timeline(): per-player date-ordered handicap_history rebuild, season-settings-aware, anchor-respecting
- [x] @claude - rebuild_league_handicaps_and_scores(): league-wide orchestrator, re-scores every completed round with point-in-time handicaps
- [x] @claude - Fix: editing a past round no longer overwrites that round's handicap with today's value
- [x] @claude - Fix: cascade-forward no longer flattens future rounds to one "latest" handicap
- [x] @claude - Fix: cascading recalcs now respect hcp_manually_overridden unconditionally (was only checked when use_existing_hcp=True)
- [x] @claude - Rewire _process_scores, matrix_update, clear_scorecard_overrides, clear_handicap_override, settings-save cascade to new engine
- [x] @claude - Delete dead _recalc_future_rounds after all callers rewired
- [x] @claude - New admin route GET/POST /handicap/rebuild — apply-then-rollback preview via db.rollback(), commit on POST
- [x] @claude - New template handicap/rebuild_timeline.html + admin "More Tools" entry point
- [ ] @user - Run the rebuild from Admin → More Tools → 🛠 Rebuild Handicap Timeline against live data (no DB access available to Claude in this environment)
- [ ] @user - Spot-check matchup 93 (the originally reported bug) and a multi-season / manual-override player after running the rebuild

### WP3.7 - Handicap Override Persistence, Bug Fixes, Scorecard Page *(Complete — 2026-06-27)*
- [x] @claude - Migration: add hcp_manually_overridden to scorecards
- [x] @claude - matrix_update: set hcp_manually_overridden=1 on scorecard edit
- [x] @claude - _process_scores: capture + restore overridden playing handicaps on re-save
- [x] @claude - Matrix: per-cell amber indicator + per-player Clear override button
- [x] @claude - New route: POST /matrix/<season_id>/clear-overrides/<player_id>
- [x] @claude - Narrow recalc_handicap_for_player override guard to same-round only
- [x] @claude - Fix tiebreaker_settings: :param → %s placeholders
- [x] @claude - Migration: add scoring_mode column to league_settings
- [x] @claude - Fix stroke dots: use differential so only higher-hcp player gets dots
- [x] @claude - Scorecards page: week dropdown; dashboard links to week scorecards page
- [x] @claude - GLT Feature Parity doc (7. GLT Feature Parity.md)
- [ ] @user - Run Supabase migrations: add_scorecard_hcp_override.sql + add_league_settings_scoring_mode.sql

### WP3.6 - Scorecard Unification, Bug Sweeps, Override Protection *(Complete — 2026-06-26)*
- [x] @claude - Unify view.html + enter_week.html: dots above score, no inline hcp text, no blue stroke cell
- [x] @claude - Stroke dots: position absolute, padding-top reserve, consistent row height
- [x] @claude - Week summary: remove Match Results; add "View Scorecards" button + new scorecards page
- [x] @claude - Score entry cancel: return to locked scorecard view (not admin panel)
- [x] @claude - Status badges: 🔒 Completed / 🔓 In Progress
- [x] @claude - Remove unlock button from admin schedule management
- [x] @claude - Hole stats: split doubles_plus → doubles (=+2) + others (≥+3); sortable headers
- [x] @claude - Admin panel: playing handicaps widget (low→high, copy-as-text)
- [x] @claude - Header logo: links to admin panel for league_admin role
- [x] @claude - Standings: first name checkbox checked by default; copy reflects visible names
- [x] @claude - PostgreSQL audit: GROUP BY, HAVING alias, is_bye, ROUND::numeric, import paths, _PgRow .get()
- [x] @claude - FK violation: null out handicap_history.trigger_round_id before deleting rounds
- [x] @claude - Manual override protection: skip recalc when latest override matches current round's trigger_round_id

### WP3.5 - Score Entry Polish + Handicap Features + Schedule Fixes *(Complete — 2026-06-22)*
- [x] @claude - Mobile scorecard name col: canvas measureText + fonts.ready; role-pill anatomy; final formula in memory file
- [x] @claude - Week dropdown MM/DD format (`Week X — MM/DD`)
- [x] @claude - Handicap history pages: `player_history.html` + `league_matrix.html`; override/clear admin modal; nav link added
- [x] @claude - `handicap_history` extended: trigger_round_id, is_manual_override, override_reason, override_by_user_id, override_at (Supabase SQL run)
- [x] @claude - Schedule sticky week column: `overflow: clip` on `.admin-widget`; `overflow: visible` on `.schedule-yearly-table`
- [x] @claude - Schedule edit mode row highlight: stronger green (#d4edda)
- [x] @claude - Admin button overflow on mobile: flex-wrap on header + header-actions
- [x] @claude - Teams nav link in `base.html`
- [x] @claude - Completed scorecard inline on enter-week: `_load_completed_scorecard()` helper; full scoring detail (stroke dots, pts indicators, net totals); mobile pts column un-hidden
- [x] @claude - Completed scorecard column fixes: header Gross, value total_gross, pts column visible
- [x] @claude - Completed scorecard name truncation fix: `.sc-name-first` wrapper for canvas measurement
- [x] @claude - Completed scorecard scroll column widening fix: summaryW=138 for completed vs 108 for entry
- [x] @claude - Matchup label (Team A vs Team B) in group header for all cards
- [x] @claude - Team header rows removed from completed scorecards
- [x] @claude - Late score entry cascade: `_recalc_future_rounds()` + `_recalc_single_round()` — re-scores subsequent rounds with updated handicaps and A/B roles
- [x] @claude - Schedule week renumbering after deletion; one-time Supabase SQL cleanup

### WP3.4 - Admin Panel Polish + Mobile Score Entry *(Complete — 2026-06-21)*
- [x] @claude - Score entry week tiles link to `enter_week` (not schedule view)
- [x] @claude - "Enter Scores for Current Week" lands on most recent week by date
- [x] @claude - Admin panel score widget: hide past completed weeks
- [x] @claude - Admin Schedule quick-action lands on Season Schedule (yearly view)
- [x] @claude - Admin widget header gap fix (emoji/button spacing on mobile)
- [x] @claude - `load_nicknames` + `player_display_name` in `database.py`; applied to `/api/v1/skins`, web skins index + round detail
- [x] @claude - Fix `ORDER BY nickname_id` bug in `load_nicknames`
- [x] @claude - iOS `HoleScoreInputRow`: tap-to-type `TextField` + 0.3 s auto-advance replaces +/− steppers
- [x] @claude - Mobile web scorecard ≤640px: no horizontal scroll (hide summary cols, compress cells); backup saved
- [x] @claude - `swt-grid` CSS: switch to `display: grid; repeat(auto-fill, minmax(130px, 1fr))` for even tile fill on mobile

### WP3.3 - iOS Gap Audit & Parity Sprint *(Complete — 2026-06-21)*

*Audit: flat schedule list, no week grouping; no week-type labels; bye matchups unlabeled; tee not auto-selected; members can't self-report from app; /api/v1/courses shape mismatch (fixed prior commit)*
- [x] @claude - Fix Schedule "All Season" view: week-grouped sections with headers + week-type badge (Rain Out, League Bye, etc.)
- [x] @claude - Filter/label bye matchups in schedule list (show BYE tag, no navigation)
- [x] @claude - Tee auto-select in ScoreInputView from matchup.teeId once courses load
- [x] @claude - Self-report for non-admin members: `isSelfReport` mode in ScoreInputView; "Submit for Review" CTA in MatchupDetailView for members
- [x] @claude - Add `weekType` to Matchup model; propagate from ScheduleWeek in computed var
- [x] @claude - Store `weeks: [ScheduleWeek]` directly in ScheduleViewModel (byWeek preserved)
- [x] @claude - Fix ScheduleView NavigationLink destination order (moved after .listStyle)

-----

## Phase 5: Score Entry Audit Remediation

*Audit file: `Audits/2026-06-12-score-entry-audit.md` — tackle in priority order P0 → P4*

### WP5.0 - Entry Flow / UX *(Complete)*
- [x] @claude - P0-1: Pre-set Course+Tee fields on matchup creation/edit so they carry into the score entry form automatically, eliminating 4 clicks + 2 page reloads
- [x] @claude - P0-2: Combine absence form into main score submission — remove separate "Save Absences" POST; process inline on `submit_scores`
- [x] @claude - P0-3: AJAX course/tee selection — replace `reloadForm()` POST with fetch calls to load tee options and hole data without full page reload
- [x] @claude - P0-4: Pre-submit validation indicator — highlight empty score cells inline before submit; disable Save button until all holes filled
- [x] @claude - P0-5: Mobile score entry — full scrollable table (retired hole-by-hole card view); single-digit auto-advance (input+rAF); enterkeyhint="next"; root cause was missing </script> tag that silently killed all holes JS

### WP5.1 - Data Integrity *(Complete)*
- [x] @claude - P1-1: Wrap handicap recalc in try/except inside `_process_scores()`; log failure without rolling back committed round data, and surface warning to admin
- [x] @claude - P1-2: Add `UNIQUE(matchup_id)` constraint on `rounds` table via migration; handle duplicate-submit gracefully with redirect to existing round
- [x] @claude - P1-3: Move `player_absences.round_id` backfill inside the same transaction block as round creation (removed bare except wrapper)
- [x] @claude - P1-4: Validate per-player tee belongs to the same course as the matchup's default course before accepting submission

### WP5.2 - Validation *(Complete)*
- [x] @claude - P2-1: Enforce `max_score_per_hole` from `league_settings` in `_process_scores()` — warns or blocks per `max_score_action` setting
- [x] @claude - P2-2: Warn admin at score entry time if any `holes.handicap_index` is null for the selected tee
- [x] @claude - P2-3: Standardize self-report score range to 1–20 (HTML + server); re-validate range on approval before writing round
- [x] @user/@claude - P2-4: Confirm whether `scoring_mode` column exists in `league_settings`; add migration if missing or remove dead code references

### WP5.3 - Audit Trail / UX Polish *(Complete)*
- [x] @claude - P3-1: Record `entered_by_user_id` on `rounds` for direct admin entry (migration: add_rounds_entered_by.sql; session user_id written on INSERT)
- [x] @claude - P3-2: Hidden `loaded_tee_id` sentinel in form; server warns + logs if submitted tee differs from holes-loaded tee
- [x] @claude - P3-3: Flash "already recorded by another admin" warning on POST to completed matchup (was silent redirect)
- [x] @claude - P3-4: localStorage persistence for double-digit toggle; label updated with auto-advance hint

### WP5.4 - Edge Cases *(In Progress)*
- [x] @claude - P4-1: Check for existing round in self-report approval handler; block duplicate round creation if admin already entered scores directly
- [ ] @user - P4-2: Review cascade-delete FK behavior for courses/tees in Supabase dashboard; document in Technical Reference
- [x] @claude - P4-3: Warn admin on score entry form if any player has `starting_handicap = NULL` (would play as scratch)

-----

## Phase 6: Golf Course API — Call Management *(Future / Design Pending)*

*Background: golfcourseapi.com free tier is 50 req/month. Two complementary approaches discussed:*

### WP6.1 - API Response Cache Table
- [x] @claude - Add `course_api_cache` table: stores raw API response JSON keyed by `api_course_id`, with `fetched_at` timestamp
- [x] @claude - On `/courses/api-import`: check cache first before hitting API; only call API if not cached (or cache > N days old)
- [x] @user/@claude - Cache TTL decided: 90 days

*Note: Caching imports alone likely covers 80% of API savings since the same popular courses get imported across multiple leagues.*

### WP6.2 - Request Logging + Rate Limit Guard
- [x] @claude - Add `api_request_log` table: `timestamp`, `endpoint`, `league_id`, `user_id`, `response_code`
- [x] @claude - Hard cap: block outgoing API calls if current calendar month log count ≥ 45 (free tier buffer)
- [x] @claude - Warn admin in UI when nearing monthly limit (usage meter on Add Course panel, turns red at limit-5)
- [x] @user - Limit is per-league (45/month) + 30-second throttle between calls per league

*Tables added to schema_postgres.sql and migrate_api_tables.py migration script created.*

-----

## Phase 4: SaaS Pivot *(Future)*

- [ ] @user - Register domain via Cloudflare Registrar
- [ ] @user/@claude - Plan payment processing for web + iOS: Stripe (web) vs. Apple In-App Purchase/StoreKit (required by App Store policy for iOS digital subscriptions) — needs a cross-platform entitlement model so one paid account isn't double-charged (added 2026-07-03)
- [ ] @claude - Multi-tenant onboarding flow (league self-signup)
- [ ] @claude - Site admin dashboard (cross-league management)
- [ ] @user - Upgrade Render to production-grade plan
- [ ] @user/@claude - Revisit migrating auth to Supabase Auth (OAuth, managed password reset) — evaluate when auth pain points arise
- [ ] @user/@claude - Support inbox (Gmail "help"/contact address) for user-reported issues, with Claude given some access to triage/fix reports — **open decision before pickup:** how autonomous (human-in-the-loop review before anything ships/sends, vs. mostly-autonomous with escalation on risky/ambiguous cases, vs. fully autonomous end-to-end); also a real consideration that an inbox driving autonomous code changes from arbitrary external email content is a prompt-injection/social-engineering surface (added 2026-07-03)
- [ ] @user - Open this Claude Code on the web environment's network egress policy to allow `golfleaguetracker.com` (currently blocked at the session/proxy level — confirmed 2026-07-03, not a tool limitation; see `7. GLT Feature Parity.md`) so Claude can browse GLT directly to gauge and close feature-parity gaps autonomously, without needing pasted screenshots/text each time. Change lives in the environment config (Claude Code on the web UI), not in this repo — see https://code.claude.com/docs/en/claude-code-on-the-web (added 2026-07-03)
- [x] @claude - Migrate `Fable Audit/` → `Orchestration/` from the `X.-Claude-Project-Framework` template repo, which had ported and upgraded this project's own original files: model-agnostic Planner/Executor naming, dedicated `Handoffs/` folder (separate from `Audits/`), 4-state lifecycle (`Open`/`In Progress`/`Done`/`Blocked`), `Effort`/`Depends on`/`Parallel-safe` handoff header fields (enables concurrent executors), and an `Execution Report` section closing the loop in-document. Added new `Plans/` folder + CLAUDE.md `## Branched Plans` section (Evaluating → Decision → Archived, for comparing implementation approaches). Added CLAUDE.md `## Orchestration Handoffs` section with the explicit Planner-role gate (high-reasoning model + explicit user request only). Ported the old Session Instructions' project-specific "Key Files" table into `4. Technical Reference.md` (template moved this out of the generic instructions by design). Old `Fable Audit/` folder deleted (superseded; recoverable via git history if needed) (added 2026-07-04)
