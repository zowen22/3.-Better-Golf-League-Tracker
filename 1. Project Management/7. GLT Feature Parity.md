# GLT Feature Parity Map

Tracking equivalents between GolfLeagueTracker (GLT) and BetterGolfLeagueTracker (BGLT).
Goal: ensure all GLT functionality is covered, with better UX.

This doc covers four separate parity tracks — each answers a different question about GLT and is at a different stage of completion. Read the relevant Part below rather than the whole doc top-to-bottom.

| Track | Question it answers | Status |
|---|---|---|
| **[Part 1: Settings Parity](#part-1-settings-parity)** | What can you configure in GLT, and does BGLT have an equivalent? | **Essentially complete** — all 24 categories / 209 settings reviewed |
| **[Part 2: Workflow Parity](#part-2-workflow-parity)** | What do GLT's help articles say about actually *using* the product — any gaps a settings comparison alone can't see? | **Complete** — all ~50 how-to articles read (2026-08-08); 15 findings worth a look, 1 confirmed real structural gap (team size cap) |
| **[Part 3: UI Parity](#part-3-ui-parity--on-hold)** | Does BGLT's screen layout match GLT's for a given page? | **On hold** (2026-07-06, @user) — GLT's own UI is the weakest part of the product; not worth chasing right now |
| **[Part 4: Feature Parity](#part-4-feature-parity)** | What whole *features* does GLT have that BGLT doesn't (or vice versa) — starting with the Rules page and Stats/reporting pages? | **Open, in progress** — Stats/Reporting pages (#1–36) fully reviewed and resolved via the 2026-07-09/07-10 stats-parity pass (see Build Status Summary, refreshed 2026-07-10); Rules page (`/glthome/league/leaguerules`) still not reviewed. |

-----

## Access Notes

*(Established 2026-07-04, once network egress to golfleaguetracker.com was opened)*

- **Use `curl` with a realistic browser `User-Agent`, not the `WebFetch` tool.** GLT's own bot detection returns a `403` to `WebFetch`'s request fingerprint specifically — this is a site-side block, not a sandbox network-policy issue (confirmed: proxy status showed zero relay failures; a plain `curl -A "Mozilla/5.0 ..."` succeeds immediately). Example: `curl -sS -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" "<url>"`.
- **Two canonical starting points** for future exploration:
  - How-To article index: `https://www.golfleaguetracker.com/glthome/help/how-to/`
  - Master settings reference (every configurable GLT setting, categorized): `https://www.golfleaguetracker.com/glthome/about/settingslist`
- **URL scheme is inconsistent on GLT's own site** — most how-to articles live at `/glthome/cms/help/how-to/<slug>`, but a handful use `/glthome/cms/how-to/<slug>` (missing the `help` segment: `adding-subs`, `sub-flighting`, `scoring-against-the-course`, `course/hole-handicaps`). Not a typo on our end — GLT's own inconsistency, noted so no one "fixes" it by mistake.
- **Authenticated login (for pages behind GLT's login wall, e.g. all of Part 4's Rules/Stats pages) — confirmed working 2026-07-09.** GLT uses a standard ASP.NET login form with an anti-forgery token. **Credentials (@user-authorized to store here, 2026-07-09 — low security risk, this is a test/demo league account): League ID `Buckeye`, password `skypilot`.** Steps, all via `curl` with the same realistic `User-Agent` as above, using one cookie jar throughout:
  1. **GET the target page first** (any authenticated URL, e.g. `/glthome/statistics/`) with `-c <cookiejar>` — it 302-redirects to `/glthome/security/login?fp=<original-path>`; let curl follow (`-sSL`) so you land on the actual login page and its Set-Cookie (anti-forgery cookie) lands in the jar.
  2. **Scrape the anti-forgery token** out of that page's HTML: `<input name="__RequestVerificationToken" type="hidden" value="...">`. It's a long opaque string, unique per page load — must be freshly scraped each session, can't be reused/hardcoded.
  3. **POST to `/glthome/security/loginaction`** with `-b <cookiejar> -c <cookiejar>` (send the anti-forgery cookie back, capture the new session cookie) and form fields: `__RequestVerificationToken` (scraped value), `UserId` (League ID), `Password`, `fp` (the original redirect-target path from step 1, e.g. `/glthome/statistics/`), `ddYearNum=2026`, `RememberUserId=false`, `token=` (empty). Follow redirects (`-sSL`) — a successful login lands you directly on the originally-requested page, already rendered.
  4. **Reuse the same cookie jar (`-b <cookiejar>`) for every subsequent authenticated request** in the session — no need to re-login per page.
  - One-shot copy/paste template, credentials already filled in — just change `TARGET` to whatever page you actually want:
    ```bash
    UA="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
    COOKIEJAR=/tmp/glt_cookies.txt
    TARGET="https://www.golfleaguetracker.com/glthome/statistics/"

    curl -sSL -A "$UA" -c "$COOKIEJAR" "$TARGET" -o /tmp/glt_login_page.html
    TOKEN=$(grep -o '__RequestVerificationToken" type="hidden" value="[^"]*"' /tmp/glt_login_page.html | sed 's/.*value="//;s/"$//')

    curl -sSL -A "$UA" -b "$COOKIEJAR" -c "$COOKIEJAR" \
      -e "https://www.golfleaguetracker.com/glthome/security/login?fp=/glthome/statistics/" \
      --data-urlencode "__RequestVerificationToken=$TOKEN" \
      --data-urlencode "ddYearNum=2026" \
      --data-urlencode "fp=/glthome/statistics/" \
      --data-urlencode "UserId=Buckeye" \
      --data-urlencode "Password=skypilot" \
      --data-urlencode "RememberUserId=false" \
      --data-urlencode "token=" \
      "https://www.golfleaguetracker.com/glthome/security/loginaction" \
      -o /tmp/glt_page.html

    # then for any further page: curl -sSL -A "$UA" -b "$COOKIEJAR" "<url>" -o /tmp/out.html
    ```
  - Confirmed via this exact method: full access to `/glthome/statistics/` (~28 report pages) and `/glthome/league/leaguerules` — no bot-blocking encountered once authenticated, same as the public pages above.
- The settings-list page is large (209 settings across 24 categories — verified exact count, see below) and best processed one category at a time rather than in one pass — see the Settings Reference table below for what's been reviewed vs. not.
- **GLT's own category filter dropdown is out of sync with the page**: it lists only 19 of the 24 categories (missing Administrative, Best Ball Points, Beta Testers, Stroke Play Points - Team, Team Totals Points) — those 5 are only visible via the default "All Categories" view. Confirmed via two independent counts (`id='table-CategoryName'` elements and `class="tableHeader"` divs both = 24) — not a scraping miss on our end, a real inconsistency on GLT's site.

-----

# Part 1: Settings Parity

*What can you configure in GLT, and does BGLT have an equivalent? Reviewed via `curl` against GLT's structured settings-list page — reliable, text-based, no rendering fidelity lost. This part is essentially done.*

## Executive Summary for Fable (updated 2026-07-04)

*Read this section first. Everything below is the supporting evidence — findings, per-category deep dives, GLT descriptions — kept for verification, not required reading to start planning. Master ranking across every category audited so far, by "how much does this affect what a league actually experiences," not by setting count. All findings are audit-only; nothing has been fixed. Route closure work through `Orchestration/Handoff Template.md` — one handoff per independently-executable item, per the new Orchestration workflow (see `CLAUDE.md`).*

**Coverage: complete.** All 24 GLT settings categories now reviewed — 15 at full deep-dive depth (full GLT description text read, every conclusion verified directly against BGLT code), 9 confirmed **not applicable** (Best Ball, Stroke Play ×3, Team Totals, Low Net's side-competition concept — no matching scoring format exists in BGLT at all; adding one would be a new scoring engine, tracked separately in Tier 6). Nothing below is name-only or unverified.

### Tier 1 — Fix regardless of parity planning (real defects, not gaps)

1. **Stableford scoring mode never activates** — `_settings_scoring_mode()` (`app/routes/scores.py:154-162`) reads the wrong DB column (`scoring_type` instead of `scoring_mode`). Selecting "Stableford" in league settings has zero effect; every round scores as match play. One-line column-name fix. Not a design decision — just broken. **FIXED 2026-07-06 (`be845e6`).**

### Tier 2 — Highest-leverage closure work (one mechanism fixes many settings at once)

2. **Absent Players/Subs point-clamp gap** — 10 of 12 GLT settings in this category are variations on one missing capability: a per-category (player/team) × (min/max) × (vs-course/vs-sub) point-clamping grid. BGLT has only a single 3-way `absence_overall_point_policy` switch that touches the overall point only. A general point-clamp layer would close 10 settings in one project. The 11th (Absent Player Penalty Strokes) is a structural mismatch, not a clamp — see Tier 5.
3. **`ab_designation_method` dead setting** — most consequential of the 5 dead settings found this audit. A/B role assignment (`scores.py:573-577`) always sorts by current playing handicap every round; the setting that's supposed to control this (`'weekly'`/`'season'`) is saved, displayed, and cloned by the season wizard but **never read**. Determines match pairings and points every week, for every league. Also directly corresponds to GLT's "Static Flights" setting (General Information category) — same gap, two names.
4. **Sub-request email notification system** — 5 of E-mails' 10 settings (Copy League Officers, Copy Teammate, Sub Request Message, Reminder Days, Verification Required) all depend on one missing prerequisite: BGLT has no sub-request email notifications at all. Building that one feature would close 5 settings at once, similar in shape to item 2 above but smaller.

### Tier 3 — Cheap, well-scoped wins (small settings-branch additions onto logic that already exists)

5. **4 remaining dead settings** — wiring each up is a narrow, mechanical fix: `diff_calculation_type='whs'` (handicap.py always computes par-based differentials — this one also has a real design decision attached, see Tier 5), `max_score_over_handicap` (never read), `match_play_points_per_hole`/`match_play_overall_point` (hardcoded 2.0/0.0/1.0 in `calc_match_play()`), `skins_config.handicap_percent` (never read in `skins.py`). **1 of 4 done: `max_score_over_handicap` FIXED 2026-07-06 — see Work Packages.**
6. **Hole Tie Calculation / Match Tie Calculation** — BGLT hardcodes ties to always split points; GLT offers an "award nothing" alternative. Small toggle onto existing logic.
7. **Match Play Point Method** — whether hole points count toward the total or are bragging-rights-only. BGLT's current behavior is already one of GLT's two options; just needs the toggle.
8. **Schedule: Show Subs on Schedule / Show Tee Times** — both narrow settings-branch additions onto template logic that's already ~90% there (schedule page has no sub-display path at all; tee-time visibility is auto-detected rather than an explicit toggle).
9. **E-mails: Blind Copy (BCC) / E-mail Signature / Allow Players to Send E-mails** — 3 standalone small additions, independent of the sub-request-notification feature above.

### Tier 4 — Real but lower-stakes (admin/UX conveniences, not weekly-impact items)

10. Bonus Points for Showing Up (regular + sub) — no attendance-bonus concept exists.
11. Swap A/B Matchups — no manual per-week pairing override exists.
12. Skins: per-par/round-total stroke caps, half-strokes, multipliers, worst-score-eligibility cutoff, gross/net-simultaneous — real gaps, but narrow and specific to the skins side-pot, not standings-affecting.
13. **12 narrow General/Administrative/Scorecards/Signup gaps** — hide-player-detail, phone auto-formatting, time zone, custom theme, combine-teebox-info, round notes, distance label, pace-of-play time, scorecard message, days-between-rounds, league playfield (course vs. simulator), minimum tee times. Each independent and small; no shared mechanism to batch-fix, unlike Tier 2/3 items above.
14. **Standings: Combine Same Dates, Number of Scheduled Rounds Shown** — real gaps, narrow scope (a same-day-round merge display rule, and an "upcoming rounds" section).
15. **Standings: Show Total Points for Segment** — partial match; `segment_pts` is already computed but only surfaced on the per-player scorecards detail view, not the main League Standings page GLT's setting describes. Cheaper than a full gap since the data already exists — likely just a template/display fix.

### Tier 5 — Bigger lifts (touch the core scoring/handicap/standings loop, not just a settings branch)

16. **Stableford's 9-tier point curve** vs. BGLT's 5 hardcoded values, plus front9/back9 point splits (both Match Play and Stableford) — real gaps, but changes to the scoring engine itself, not a settings branch.
17. **WHS-style slope/rating handicap differential** (`diff_calculation_type='whs'`) — GLT's own recommended default leans this way; this is a real product decision (par-based vs. full WHS math), not just a dead-setting wiring fix. Flagged here because it's bigger than the other dead settings despite being technically one of the 5.
18. **Absent Player Penalty Strokes** — structurally different ghost-scoring model (GLT: separately-tunable "course plays for them" pseudo-player; BGLT: ghost-scores using the absent player's own handicap). Not a missing parameter on the same mechanism — would require rethinking the ghost-scoring approach itself.
19. **Standings Sort Method / Sort Order / Force Decimal Places** — all three would touch the core standings query/rendering path (currently a hardcoded `ORDER BY total_pts DESC` and default Jinja number formatting), not just add a settings branch.
20. **Multi-segment support (up to 4 segments)** — BGLT supports exactly one segment window; this is 4 of League Size and Length's 6 settings, all the same underlying gap.

### Tier 6 — Out of scope for "gap closure" (new product surface, not a fix)

21. Stroke Play (individual/team/vs-field), Best Ball, Team Totals, Low Net as scoring formats — BGLT has exactly one 2-player-team match-play/stableford structure; adding any of these is a second scoring engine, not a settings tweak.
22. Team Skins, League Groups, calculated-handicap-vs-course-handicap distinction, individual/scramble league types (BGLT is hardcoded 2-player-team) — no prerequisite concept exists in BGLT's schema at all.

### Not tiered — reviewed, no gap found (workflow comparison, not settings)

23. **Season rollover workflow** ("Starting a New Season" how-to vs. WP3.20's "Start Another Season" wizard) — full step-by-step comparison found **no new functional gaps**; every GLT step is either matched, not applicable (constraint doesn't exist in BGLT), or a deliberate design difference. One item genuinely needs @user's judgment rather than either Fable planning or silent adoption: **GLT requires a 30-day cooldown after a season ends before offering renewal; BGLT offers it immediately.** Not clearly a bug or a gap — just worth confirming intentional. See "Season Rollover Workflow" in Part 2 (Workflow Parity) below for the full step-by-step.

*Every tier above is fully evidence-backed — Fable can start writing handoffs directly from this summary without re-reading the supporting sections below, though those sections have the file:line citations if a handoff needs them.*

## Settings Reference (from `/glthome/about/settingslist`)

*(Built 2026-07-04 — this is the reference for picking a category to review next, not a changelog. Update the Reviewed column as categories get audited; don't re-derive this list from scratch.)*

24 categories, **209 total settings** (exact — sum of table rows below, cross-verified against a second, independent count of `class="tableHeader"` divs on the page), each with a full description and applicability flags (9-hole / 18-hole / individual / 2-player team / Handicap Manager). Extracted structurally via `curl` + HTML table parse (one `<table id='table-CategoryName'>` per category) — reliable enough to re-run if the page changes.

| Category | # Settings | Reviewed? |
|---|---|---|
| General Handicap Settings | 19 | Partially — the recommended-defaults subset (~6 settings) covered via the how-to article above; full category not yet cross-checked setting-by-setting |
| Hole Stroke Limits (ESC 1-6 ranges + adjustments) | 19 | Partially — the concept (tiered ranges, Net Double Bogey) covered; the other 5 ESC tiers' exact mechanics not yet detailed |
| Match Stroke Limits | 7 | ✓ *(deep-dive, done directly against schema/handicap.py — see Handicap Settings section above)* |
| Skins | 22 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| Stableford Points | 18 | ✓ *(deep-dive, full descriptions — see Point Rules deep-dive)* |
| Absent Players and Subs | 12 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| Best Ball Points | 12 | Not applicable — no best-ball format in BGLT (confirmed, no deep-dive needed) |
| General Point Rules | 11 | ✓ *(deep-dive, full descriptions)* |
| Stroke Play Points - Individual | 12 | Not applicable — no stroke-play format in BGLT |
| Stroke Play Points - vs the Field | 12 | Not applicable — same |
| E-mails | 10 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| Match Play Points | 8 | ✓ *(deep-dive, full descriptions)* |
| Standings | 8 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| League Size and Length | 6 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| Stroke Play Points - Team | 6 | Not applicable — same as Stroke Play - Individual |
| Team Totals Points | 6 | Not applicable — no team-totals format in BGLT |
| General Information | 5 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| Scorecards | 5 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| General | 4 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| Low Net | 2 | ✓ *(deep-dive, full descriptions)* |
| Schedule | 2 | ✓ *(first look, full descriptions — see Full Settings Gap Audit)* |
| Administrative | 1 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |
| Beta Testers | 1 | ✓ *(first look — confirmed GLT-internal, not applicable to parity)* |
| Signup and Renewal | 1 | ✓ *(deep-dive, full descriptions — see Full Settings Gap Audit)* |

-----

## Handicap Settings / Calculation

*(Reviewed 2026-07-04, once egress was opened — sources: [Recommended Handicap Settings](https://www.golfleaguetracker.com/glthome/cms/help/how-to/common-handicap-options), [Limiting Strokes per Hole for Handicapping](https://www.golfleaguetracker.com/glthome/cms/help/how-to/strokes-per-hole-limit))*

| GLT Setting / Option | BGLT Equivalent | Notes |
|---|---|---|
| Setting #70, "Number of Rounds Required To Establish a Handicap" (GLT recommends 2) | `league_settings.min_rounds_for_handicap` (default `2`) | Exact match on the recommended default. |
| Setting #71, "Number of Most Recent Rounds to Calculate Handicap" (GLT recommends 6) | `league_settings.rounds_to_average` (default `4`) | BGLT's default window is narrower (4 vs. GLT's recommended 6) — both are admin-configurable, not a capability gap, just a different starting default. |
| Setting #40, "Number of High Rounds to Throw Out" (GLT recommends `-33`, i.e. **33% of scores**, not a fixed count) | `league_settings.high_scores_to_drop` (default `1`, a fixed **count** of rounds) | **Real methodology difference.** GLT's high/low-drop settings are percentage-of-pool based (scales with how many rounds exist); BGLT's are a fixed round count regardless of pool size. Worth a product decision: keep BGLT's fixed-count model (simpler, predictable) or add a percentage-based option to match GLT exactly. |
| Setting #53, "Number of Low Rounds to Throw Out" (GLT recommends 0) | `league_settings.low_scores_to_drop` (default `0`) | Same fixed-count-vs-percentage caveat as above; default values match. |
| Setting #38, "Handicap Percent" (GLT recommends `1`, i.e. 100%) | `league_settings.handicap_percent` (default `90.0`) | Both configurable per-league; defaults differ (GLT recommends no reduction, BGLT defaults to the traditional 90% "playing handicap" convention) — not a gap, a different default. |
| Setting #36, "Maximum Calculated Handicap" (GLT recommends `54.0` for 18-hole leagues, `27.0` for 9-hole) | `league_settings.max_handicap_index` (season default `18.0`) + `platform_settings.max_handicap_index_ceiling` (site-wide ceiling, `54.0`) | BGLT's site-wide ceiling already matches GLT's 18-hole recommendation exactly; the per-season default (`18.0`) is far more conservative than GLT's recommended ceiling — admins can already raise it up to the platform ceiling, so this is a default-value difference, not a missing capability. No 9-hole-specific ceiling variant exists in BGLT today (GLT recommends 27.0 for 9-hole; BGLT doesn't distinguish 9 vs 18 for this setting). |
| Setting #112, "Handicap Calculation Type": **"2020 Rules (Custom)"** (course-difficulty/slope-adjusted) vs. **"Par"** (simple par-based, no course-handicap conversion) | `league_settings.diff_calculation_type` — `'par'` is fully implemented; `'whs'` (slope/rating) is a **dead dropdown option** (`admin/settings.html` offers it, but `handicap.py`'s engine always computes par-based differentials regardless of the selection) | **Directly resolves the open WP3.1 backlog question** ("implement real WHS slope/rating math, or remove the dropdown option?") — GLT's own docs confirm course-difficulty/slope-adjusted handicapping is a real, commonly-used mode (their own recommended default, in fact), not a niche USGA-only feature. This is a genuine parity gap, not just dead UI — leans toward "implement it" over "remove the option." |
| "Hole Stroke Limit" section — per-hole max score for handicap purposes, **"Net Double Bogey"** as the common preset, up to **6 different handicap-tiered ranges** (e.g., <10 hcp capped at 7, ≥10 hcp capped at 8), and an **automatic +5 adjustment when a player has no established handicap yet** | `league_settings.max_score_per_hole` (single global int, unset by default) + `max_score_action` (`'warn'`/block) | **Real gap.** BGLT has one global per-hole cap (or none) applied uniformly; GLT supports handicap-tiered ranges (different caps for different skill levels) and a "Net Double Bogey" (par + 2, adjusted for strokes received) preset rather than a raw fixed number. BGLT also has no equivalent of the +5 no-handicap penalty adjustment — a pre-eligibility player's hole-score cap isn't specially handled today. |

### Full Category Audit: General Handicap Settings (19) + Hole Stroke Limits (19) + Match Stroke Limits (7)

*(Cross-checked directly against `app/schema_postgres.sql` and `app/routes/handicap.py`/`scores.py` on 2026-07-04 — not from memory. Rows already covered above aren't repeated.)*

| GLT Setting | BGLT Equivalent | Notes |
|---|---|---|
| Calculated Handicap Decimals | *(none — hardcoded)* | **Gap.** `handicap.py` hardcodes `round(avg_diff, 1)` for the index and `int(round(...))` for playing handicap (`handicap.py:240,461,1058`) — not admin-configurable. **Note 2026-07-06: the index-rounding half of this is now moot** — `max_score_over_handicap`/precision work removed the 1-decimal rounding at storage time (see Work Packages); playing-handicap rounding is still hardcoded and intentional, unrelated to this gap. |
| Handicap Allowance Percentage | `league_settings.handicap_percent` | Likely the same concept as Setting #38 under a different label in GLT's own list (their settings list and how-to article don't perfectly agree on naming) — not a gap, just confirming it's the same setting. |
| Handicap Percent for Subs | `league_settings.temp_handicap_percent_sub` | **Partial match, narrower scope.** BGLT's version only applies during a sub's *pre-eligibility* rounds (before `min_rounds_for_handicap` real rounds); once a sub crosses that threshold they use the normal calculated index like anyone else. GLT's setting reads as a standing modifier that could apply more broadly — not fully verified without a GLT test account, flagged as needs-confirmation rather than asserted as a gap. |
| Handicap Rounding | *(none — hardcoded)* | Same as "Calculated Handicap Decimals" above — rounding behavior isn't a separate configurable option in BGLT. |
| Maximum Difference in Score from Handicap / Maximum Score Allowed for Handicap Calculation | `league_settings.max_score_over_handicap` (default `18`) | **FIXED 2026-07-06** — was dead (defined in schema, saved by the settings form, never read by the scoring/handicap engine); now wired into both handicap write paths. See Work Packages / `Handoffs/2026-07-06-max-score-over-handicap.md`. |
| Number of Most Recent Years to Use | *(none)* | **Gap.** BGLT's averaging window is rounds-based only (`rounds_to_average`); no year-based window exists. |
| Number of Rounds to Establish a Handicap for a Sub | *(none — same threshold as regular players)* | **Gap.** `min_rounds_for_handicap` (`handicap.py:141,408`) is applied uniformly regardless of `is_sub`; GLT lets this differ for subs specifically. Low-priority given BGLT already differentiates subs via the percent-based temp handicap instead. |
| Match Stroke Limits category (Maximum Course Handicap, Maximum Strokes - Best Ball / Individual Match / Par 3s / Team Totals Match, Maximum Strokes Received vs Course) | *(none)* | **Gap, whole category.** These are per-format caps on strokes *received* in a match (distinct from the hole-by-hole ESC-style cap above) — BGLT has no per-format stroke-limit settings at all. Lower priority than the ESC/Hole-Stroke-Limit gap since it's a more advanced/niche control. |

-----

## Full Settings Gap Audit (2026-07-04)

*Audit against the remaining categories of `/glthome/about/settingslist`, done via two Explore agents cross-checking BGLT's actual code (not just the settings schema) — full citations in each row. Purpose: identify gaps for @user + Fable to plan closure work from; no fixes made here.*

### ⚠️ Live bug found during this audit (not a gap — a real defect)

**Stableford scoring mode silently never activates.** `_settings_scoring_mode()` (`app/routes/scores.py:154-162`) reads `settings['scoring_type']` — the **net/gross** toggle (`schema_postgres.sql:109`, values `'net'`/`'gross'`) — when it should read `settings['scoring_mode']`, the actual **match_play/stableford** format toggle (`schema_postgres.sql:140`). Since `scoring_type` never equals `'stableford'`, every `if scoring_mode == 'stableford':` check in the scoring engine (`scores.py:1027,1351,2139,2145`) is permanently unreachable — an admin selecting "Stableford" in league settings has **zero effect on live scoring**; every round scores as match play regardless. Root cause: `_settings_scoring_mode`'s docstring literally says *"Column is 'scoring_type' in the DB"* — a leftover from an older fix (`1e0e07c`, "Fix KeyError: 'scoring_mode' — DB column is scoring_type", predates the `scoring_mode` column existing at all, added later same week). This reads as a genuine, standalone bug fix, not a feature-parity decision — flagged separately in Work Packages as higher priority than the gap items below. **FIXED 2026-07-06 (`be845e6`).**

### Dead settings (defined + saved, never read by the engine) — 5 found this session, pattern worth noting

| Setting | Where dead | Session found |
|---|---|---|
| `diff_calculation_type='whs'` | `handicap.py` always computes par-based differentials regardless | 2026-06-30 (WP3.12) |
| `max_score_over_handicap` | Never read in `scores.py`/`handicap.py`; only saved/cloned | 2026-07-04 (this audit) — **FIXED 2026-07-06** |
| `match_play_points_per_hole`, `match_play_overall_point` | `calc_match_play()` (`scores.py:78-84`) hardcodes 2.0/0.0/1.0, never reads either column | 2026-07-04 (this audit) |
| `ab_designation_method` (`'weekly'`/`'season'`) | A/B role assignment (`scores.py:573-577,2264-2265,2598`) always sorts by current playing handicap every round, regardless of setting value — appears only in the settings form, `league_info` display, and season-clone list; **never read** in `scores.py`/`handicap.py`/`standings.py` | 2026-07-04 (Point Rules deep-dive) |
| `skins_config.handicap_percent` (`schema_postgres.sql:443`, default `90.0`) | Confirmed via `grep -rn "handicap_percent" app/routes/skins.py` → zero matches. Net skins scores always use the same league-wide playing handicap as everything else; this column is saved by the settings form and cloned by the season wizard but never read in `_calculate_skins()` or anywhere else in `skins.py` | 2026-07-04 (Skins deep-dive) |

Five instances of the same failure class (a setting exists in schema + admin UI but the engine ignores it) is worth a systemic pass, not just five one-off fixes — see Work Packages. `ab_designation_method` remains the most consequential of the five since A/B role assignment directly affects match pairings and point totals every week; the others are narrower (one scoring mode, one skins sub-calculation, two match-play point values). **Status 2026-07-06: 1 of 5 fixed (`max_score_over_handicap`); the remaining 3 actionable ones (`ab_designation_method`, match-play points pair, `skins_config.handicap_percent`) are flagged in Work Packages as each needing its own planned pass given live-scoring blast radius — not safe to batch-fix. `diff_calculation_type='whs'` needs a build-vs-remove product decision first (see Tier 5, item 17).**

### Point Rules & Scoring Formats

*(Source: Explore agent code audit of `scores.py`, `standings.py`, `playoffs.py`, cross-referenced against GLT's Match Play/Stableford/Stroke Play/Best Ball/Team Totals/General Point Rules/Low Net categories, 79 settings total)*

BGLT's entire scoring model is two formats (match play, stableford — the latter currently broken, see above) inside a fixed 2-player-team A/B head-to-head structure (`scores.py:1088-1091`). GLT supports substantially more scoring formats.

| GLT Category | BGLT Status |
|---|---|
| Match Play Points (8) | Partial — point method/overall-point settings exist but are dead (see above); no front9/back9 split, no team-level "most holes won"/"both matches" aggregation. |
| Stableford Points (18) | Partial and currently non-functional (see bug above); even when fixed, only one hardcoded point curve exists (eagle=4/birdie=3/par=2/bogey=1/double+=0) — none of the 18 granular per-score settings are configurable. |
| Stroke Play Points - Individual (12) | **Not applicable — no stroke-play format exists in BGLT at all.** |
| Stroke Play Points - Team (6) | **Not applicable — same.** |
| Stroke Play Points - vs the Field (12) | **Not applicable — same; no field-wide point distribution concept exists.** |
| Best Ball Points (12) | **Not applicable — no best-ball format exists.** |
| Team Totals Points (6) | **Not applicable — no team-totals format exists.** |
| General Point Rules (11) | Partial — `ab_designation_method` is a rough analog to reflighting but not a manual per-week swap; no front9/back9 splits; `rounds_to_average`/drop-settings are handicap-calc trimming, not season-standings point rules (different purpose despite similar-sounding names). |
| Low Net (2) | **Not applicable — no low-net side-competition; closest adjacent feature is Skins, which is separate.** |

**Read on this**: adding stroke play / best ball / team totals / low net as scoring formats would be a genuinely large structural feature (a second scoring engine, not a settings tweak) — worth Fable weighing the ROI of that against smaller, more surgical gaps elsewhere before planning closure work.

**Independently corroborated 2026-07-09** via `/glthome/about/features` (GLT's own marketing page, found by @user) — confirms these aren't obscure settings-list categories but GLT's actual advertised scoring options, in plain English: Match Play (points/hole, /match, /team-match, by net score or hole points), Stableford (same structure, by net or stableford score), **Best Ball** (low score of the teammates), **Team Totals** (both teammates' scores added), **High/Low of each teammate** (a rule not previously distinguished from Best Ball in this doc — one teammate's high score plus the other's low, a genuinely separate rule), and **Stroke Play** (points by finishing position, net and gross). New detail this page adds: *"you can combine any of the scoring options for your league"* — GLT's formats are composable, not mutually exclusive, which BGLT's single-value `scoring_mode` enum architecturally isn't. Full contrast written up in `Plans/2026-07-09-best-ball-format-question.md` — **@user has since decided to build these** (Best Ball, Team Totals, High/Low, plus a new "Classical Stroke Play" preset distinct from GLT's field-position variant) as fixed, non-combinable presets over a settings surface matching GLT's own category list; see that doc for the full decision and remaining open items before implementation.

#### Deep-dive: Match Play Points, Stableford Points, General Point Rules, Low Net (39 settings, full GLT descriptions read — not just names)

*Goal for this pass: does BGLT have the underlying **feature ability**, regardless of whether the setting/UI matches GLT's — not a request for identical knobs.*

**General Point Rules (11)**

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Bonus Points for Showing Up (regular) | Extra points just for attendance, independent of match outcome | **Gap** — no BGLT concept of attendance-bonus points at all. |
| Bonus Points for Sub Showing Up | Same, for subs specifically | **Gap** — same. |
| Hole Tie Calculation (split points on a tied hole, or award none) | `calc_match_play()` (`scores.py:78-84`) hardcodes the tie case to split 1.0/1.0 — always splits, no "award nothing on tie" option | **Partial** — BGLT has the ability's default behavior (split), but not the *choice*; the "award none" branch doesn't exist. |
| Match Tie Calculation (same, at the overall-match level) | Same hardcoded split, same file | **Partial** — same as above. |
| Re-Flight Subs by Handicap (auto-reassign A/B role when a sub's handicap differs from the player they're subbing for) | Role assignment (`scores.py:573-577`) sorts by **current playing handicap every round**, unconditionally — this is functionally "always YES," permanently. No "NO — sub inherits the original player's flight" option, and no "Static Flights" concept to force it off. | **Partial** — BGLT has the *reflighting* ability, just no toggle to disable it. Genuinely useful capability already present, just not GLT's default-off / all-or-nothing framing. |
| Swap A/B Matchups (manual override to flip who plays whom) | No manual per-week override of the A-vs-A/B-vs-B pairing exists | **Gap.** |
| Team points for winning back9/front9/overall "of ALL formats" (Best Ball + Team Totals combined) | Requires Best Ball/Team Totals formats | **Not applicable** — those formats don't exist in BGLT. |
| Use "n" Best Rounds for Season Standings (drop worst rounds from the *points* total, not the handicap calc) | Verified: `standings.py` sums every round's points unconditionally — no equivalent to dropping worst-scoring rounds from a season point total exists anywhere. This is a **different axis** from `rounds_to_average`/`high_scores_to_drop` (those only ever feed the handicap index, never standings points). | **Gap**, not previously distinguished clearly from the handicap-drop settings — worth calling out since it's easy to conflate the two. |
| Use "n" Best Rounds for Segment Standings | Same distinction, scoped to a segment | **Gap**, same reasoning. |

**Low Net (2)** — **Gap, both settings.** No low-net leaderboard/summary concept exists as its own standings feature; the closest adjacent thing (Skins' gross/net toggle) is a separate side-pot, not a standings column.

**Match Play Points (8)**

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Points Per Hole, Individual Match Play, Net score | `calc_match_play()` hardcodes 2.0 win / 1.0 tie / 0.0 loss per hole; `match_play_points_per_hole` column exists but is dead (confirmed) | **BGLT has the ability (hole-by-hole match points), just not the configurability** — the underlying mechanic works, the point *value* can't be changed away from 2/1/0. |
| Match Play Point Method (whether hole points count toward the player's total, or are hole-bragging-rights only) | `match_play_overall_point` dead column; hole points always feed the total in BGLT — no "don't add to total" mode | **Gap** on the toggle; BGLT's default behavior (hole points count) is one of GLT's two options, just not switchable. |
| Individual Points for Winning Overall Match | Roughly maps to BGLT's `overall_point_won` per player (`match_results` schema) — the *ability* to award a bonus for winning overall exists and works | **Match**, different configurability (BGLT's overall-point value isn't adjustable per this setting either, but the mechanic itself is real and functioning, unlike the dead hole-point settings). |
| Individual Points for Winning Back 9 / Front 9 Match | Requires splitting a round into front/back halves for point purposes | **Gap** — confirmed no front9/back9 split anywhere in `match_results` or the points engine. |
| Team Points for Most Individual Holes Won | Team-level aggregation of individual hole-win counts | **Gap** — standings only sums individual player totals (`standings.py:1080-1084`), no team-level "most holes won" bonus exists. |
| Team Points, Players Winning Both Matches (+ Tie Option) | Team bonus when both teammates win their individual matches | **Gap** — no such team-bonus mechanic exists. |

**Stableford Points (18)**

Already covered in depth above (the live bug + hardcoded 5-tier curve). Reading GLT's full descriptions confirms the granularity gap precisely: GLT allows **9 distinct configurable point values** (double-eagle/albatross through triple-bogey-or-worse, plus separate -4/-5 tiers) versus BGLT's **5 hardcoded values** (`scores.py:133-144`) — meaning even an unusually good hole (better than eagle) or unusually bad one (worse than double bogey) can't be differentiated in BGLT; GLT's settings can. Front9/back9 individual and team Stableford point splits are the same gap as Match Play's. "Stableford Best Ball" (best-of-two-teammates scoring) has no BGLT equivalent (no best-ball concept at all, consistent with the earlier finding).

#### Priority read for Fable (this pass only)

Ranked by "how much does this affect what a league actually experiences," not by setting count:
1. **Fix the Stableford bug first** — it's not a parity gap, it's broken plumbing. **Done 2026-07-06.**
2. **`ab_designation_method` being dead** is the most consequential newly-found dead setting — it directly determines who plays whom and who scores which points, every week, for every league.
3. **Hole/Match Tie Calculation** and **Match Play Point Method** are cheap, high-value additions — small, well-scoped toggles onto logic that already exists (just needs a settings-branch, not new scoring logic).
4. **Bonus-points-for-showing-up** and **Swap A/B** are real but lower-stakes UX/admin conveniences.
5. **Stableford's 9-tier point curve** and **front9/back9 splits** (both formats) are bigger lifts — touch the core scoring loop, not just a settings branch.
6. **Stroke play / best ball / team totals / low net** as whole new formats — explicitly out of scope for "closing a gap," this is new product surface. Flag for a separate roadmap conversation, not bundled into gap-closure work.

### Skins (22 settings) — `app/routes/skins.py`, `skins_config`/`round_skins_settings`/`round_skins_participants`/`skins_results` tables

One flat, single-pot, individual-only skins system: `_get_skins_config()` reads `skins_config` (amount, gross/net choice, the now-confirmed-dead `handicap_percent`); `_calculate_skins()` (`skins.py:33-99`) is a simple per-hole low-score-wins-outright algorithm — unit value is always `pot / num_holes`, ties get no winner and roll the skin count forward via `round_skins_settings.carried_over_amount`. No multipliers, per-par caps, half-strokes, handicap-method choice, team-skins, or league-group scoping exist anywhere in the file. BGLT has no `league_groups`-equivalent concept in the schema at all (confirmed — zero matches for `league_group` anywhere in `schema_postgres.sql`), which is why several GLT skins settings that depend on League Groups or Team Skins are marked Not applicable below rather than Gap — the *prerequisite feature* doesn't exist, not just the setting.

#### Deep-dive: full 22 settings, GLT descriptions read in full

*Goal: feature ability, not identical knobs — does the underlying mechanic exist in BGLT under any name?*

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Enable Gross Skins / Enable Net Skins (2) | GLT: two independent booleans — a league can run a gross pot AND a net pot simultaneously in the same round. BGLT: `skins_config.default_gross_net` is a single exclusive value (`'gross'` or `'net'`), enforced through every read path in `skins.py` (lines 282, 313, 377, 431) | **Partial** — BGLT can run either pot, just not both at once for the same round. |
| Gross Skins Carry Over / Net Skins Carry Over (2) | Pot rolls forward to next week if nobody wins | **Match** — `round_skins_settings.carried_over_amount`, verified in `_calculate_skins`; moot that it's one shared field rather than gross/net-split, since BGLT can't run both pots in the same round anyway (see above). |
| Gross Skins Label / Net Skins Label (2) | Custom display name for the pot (e.g. rebrand "net skins" as a second gross pot) | **Gap** — no label/rename field in `skins_config` or anywhere in the skins schema. |
| Gross Skins Multiplier / Net Skins Multiplier (2) | Score-type-based skin weighting (eagle=3x, birdie=2x, par=1x) | **Gap** — `_calculate_skins`'s unit value is flat `pot/holes` regardless of score type; no albatross/eagle/birdie/par multiplier concept anywhere. |
| Gross Skins Weekly Pot / Net Skins Weekly Pot (2) | Fixed dollar pot per round, OR a per-player dollar-entry mode that sums to the pot | **Partial** — `skins_config.default_amount` + `round_skins_settings.amount_override` cover the fixed-pot mode; the alternate "sum of individual per-player entries" mode GLT describes has no BGLT equivalent. |
| Maximum Strokes - Par 3 / Par 4 / Par 5 (3) | Cap handicap strokes received on a hole by its par value, for net skins eligibility | **Gap** — `_calculate_skins` applies full handicap strokes on every hole with no par-based ceiling. |
| Net Skins Half Strokes | Player receives half their normal hole strokes for skins purposes only | **Gap** — no such adjustment anywhere in the skins calculation; full stroke allocation is used unconditionally. |
| Net Skins Handicap Method (full handicap vs. "low man in the field" — everyone's handicap reduced by the field's lowest) | Alternate handicap basis specifically for skins | **Gap** — BGLT always uses each player's full playing handicap; no field-relative reduction option exists. |
| Net Skins Handicap Percent | A skins-specific percent multiplier on top of the league's normal handicap percent | **Dead setting** — `skins_config.handicap_percent` exists and is saved/cloned but never read (see Dead Settings table, 5th entry). |
| Net Skins Maximum Strokes (total cap across the round) | Cap total strokes a player can receive for skins purposes, round-wide | **Gap** — no such cap exists; only the (dead) per-hole `handicap_percent` setting was ever intended to touch this. |
| Team Skins Tie Logic (award both teammates on a tie, vs. cancel out) | Requires a team-skins concept to begin with | **Not applicable** — BGLT has no team-skins mode at all; skins are individual-only. |
| Use Calculated Handicap for Skins Calculation (course handicap vs. a separately-calculated handicap) | Requires two distinct handicap concepts to choose between | **Not applicable** — BGLT has exactly one playing-handicap concept; there's no course-handicap-vs-calculated-handicap distinction to toggle. |
| Use League Groups for Gross Skins / Net Skins (2) | Scope skins winners to a league sub-group rather than the whole league | **Not applicable** — BGLT has no League Groups concept anywhere in the schema. |
| Worst Score Which Can Win Skin (e.g. "par or better only") | Eligibility cutoff excluding poor scores from winning a skin | **Gap** — `_calculate_skins` considers every hole/score unconditionally; no eligibility floor exists. |

**Net for Fable**: of 22, roughly **13 are real gaps** (multipliers, labels, per-par/half-stroke/round-total stroke caps, handicap method, worst-score cutoff), **3 are structurally not applicable** (team-skins tie logic, calculated-handicap distinction, league-groups × 2 — 4 settings, one bucket), **1 is the confirmed dead setting**, and **4 are partial matches** (gross/net exclusivity, weekly-pot fixed-mode-only). None of the gaps are cheap — most touch the core `_calculate_skins` per-hole loop, not just a settings branch. If Fable is looking for quick wins in skins, there aren't many; this category is either "leave as-is" or "meaningfully rebuild the calculation engine."

### Standings (8 settings) — `app/routes/standings.py`

No standings settings section exists at all in `admin/settings.html` (only Scoring/Handicap/Max-Score/Playoffs/Skins/Self-Reporting/Segments/Tiebreakers). Sort is hardcoded (`ORDER BY total_pts DESC`, `standings.py:135,160`). BGLT's **Tiebreakers** feature (head-to-head/points%/all-play/scoring-avg priority, `standings.py:171-341`) is a real but different-mechanism analog to "Standings Sort Method" (tiebreak-only, not a full sort choice).

#### Deep-dive: full 8 settings, GLT descriptions read in full

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Combine Same Dates in Standings (merge 2 same-day rounds, e.g. split tee times, into one standings row) | Requires detecting and merging multiple rounds sharing a calendar date | **Gap** — no such merge logic anywhere in `standings.py`; each round is always a separate row/entry. |
| Force Decimal Places (uniform decimal precision across all displayed point values) | Requires a formatting layer that inspects all point values in a view and normalizes precision | **Gap** — points render via Jinja's default number formatting (`{{ pts }}pt`, `{{ row['indiv_pts'] }}` in `scorecards.html`) with no normalization step. |
| Number of Scheduled Rounds Shown on Standings (a "next N rounds" preview section on the standings page) | Requires a standings-page section listing upcoming scheduled rounds | **Gap** — confirmed no "upcoming rounds" section exists anywhere in `standings.py`/`templates/standings/`. |
| Show Total Points for Season | League-wide cumulative point total, always visible | **Match** — `total_pts` computed and displayed (`standings.py:122,135,146,160`; `index.html`, `podium.html`). |
| Show Total Points for Segment | Points scoped to the current segment window, always visible on standings | **Partial** — `segment_pts` is computed and displayed, but only in the per-player `scorecards.html` detail view (gated on `has_segment`), not on the main League Standings index page GLT's setting describes. |
| Show Zero Points (marked "FOR BETA TESTERS ONLY" in GLT's own description) | Display literal `0` vs. blank for a zero point value | **Gap**, but GLT flags this one as its own internal beta/experimental setting, not a stable league feature — lower priority than the other gaps in this category for that reason. |
| Standings Sort Method (choose the default ordering shown on first load) | Requires more than one selectable sort basis | **Gap** — always `ORDER BY total_pts DESC`; no alternate sort basis is configurable. Distinct from Tiebreakers, which only resolves *ties within* that fixed sort, not the sort itself. |
| Standings Sort Order (High→Low vs. Low→High, e.g. for stroke-play-style rank scoring) | Ability to invert sort direction | **Gap** — always descending; no ascending option. Somewhat moot for BGLT today since it's a match-play/stableford points system, not stroke-play-by-rank, but the underlying capability still doesn't exist. |

**Net for Fable**: 6 of 8 are real gaps, 1 is a partial match (segment points exist but in the wrong view), 1 (Show Zero Points) is GLT's own beta-only setting and probably not worth matching at all. None of these are cheap — "Force Decimal Places" and "Standings Sort Method/Order" would touch the core standings query/rendering path, not just add a branch.

### E-mails (10 settings) — scattered across `leagues` table columns + `app/routes/email_config.py` (no unified settings page)

From-address/name implemented and matching (`smtp_from_email`/`smtp_from_name`). Player email opt-out exists (`email_opt_out*` columns) but is a *receive* opt-out, not a *send* permission — different mechanism from "Allow Players to Send E-mails."

#### Deep-dive: full 10 settings, GLT descriptions read in full

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Allow Players to Send E-mails (group email to the whole league, sent by a player rather than an admin) | Requires a player-facing "compose and send to league" feature | **Gap** — confirmed no such route exists anywhere; `send_league_email()` (`email_config.py:67`) is only ever called from admin-triggered flows (announcements, digests, reminders, round-posted notices). There's no player-initiated email feature to toggle at all. |
| Blind Copy When Sending to Players (BCC vs. To, hides recipients from each other) | Toggle between BCC and full-recipient-list send modes | **Gap** — no such toggle exists in `email_config.py`. |
| Copy League Officers for Sub Requests | Requires a sub-request-specific email flow to begin with | **Gap** — BGLT has no sub-request email notifications at all (confirmed via `subs.py`, which manages sub records/reservations in-app, not email); this and the next 3 settings all depend on a feature category that doesn't exist yet. |
| Copy Teammate on Subrequest E-mails | Same prerequisite | **Gap** — same reasoning. |
| E-mail "From" Address | Configurable sender address | **Match** — `smtp_from_email`, read and applied in `send_league_email()` (`email_config.py:98`). |
| E-mail "From" Name | Configurable sender display name | **Match** — `smtp_from_name`, with a `league_name` fallback (`email_config.py:99`). |
| E-mail Signature (one-line signature appended to outgoing emails) | Requires a stored signature string appended at send time | **Gap** — no signature field or append-at-send-time logic exists. |
| Sub Request E-mail Message (custom message included in every sub request) | Same sub-request-email prerequisite | **Gap** — same reasoning as Copy League Officers/Teammate above. |
| Sub Request E-mail Reminder Days (GLT: requires their Premium E-mail Upgrade) | Same prerequisite, plus GLT gates this behind their own paid upgrade tier | **Gap** — same reasoning; also worth noting GLT itself treats this as a premium/paid feature, not a baseline one. |
| Sub Request Verification Required (require email-link confirmation before a sub reservation is valid) | Same prerequisite | **Gap** — same reasoning. |

**Net for Fable**: 2 of 10 match (from-address/name), 8 are gaps — but 5 of those 8 (Copy League Officers, Copy Teammate, Sub Request Message, Reminder Days, Verification Required) are really **one missing prerequisite feature**: BGLT has no sub-request email notification system at all. That reframes this category from "8 small gaps" to "2 standalone gaps (Allow Players to Send E-mails, Blind Copy, E-mail Signature) + 1 bigger feature gap (sub-request email notifications) that happens to touch 5 settings at once" — closer in shape to the Absent Players/Subs finding than to a pile of unrelated small items.

### League Size and Length (6 settings)

#### Deep-dive: full 6 settings, GLT descriptions read in full

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Number of Holes (9 vs. 18) | Configurable holes-per-round | **Match** — `holes_per_round` (`schema_postgres.sql:108`). |
| Number of Players (league roster size, with even-number/ghost-player rules GLT enforces) | Configurable/enforced roster size cap | **Not applicable** — BGLT has no individual-league or scramble-league mode (see the "League Type" finding below, General category) and no explicit roster-size cap or even-number validation; teams are just created/removed directly by the admin with no size ceiling. Different enough in shape from GLT's rule-enforcing version that it's not really a partial match either. |
| Number of Rounds in 1st/2nd/3rd/4th Segment (4 settings, GLT supports up to 4 segments, 30-round season cap) | Multi-segment season structuring with per-segment round counts | **Gap** — BGLT supports exactly **one** segment window (`segment_start_week`/`segment_end_week`, confirmed earlier this audit), no per-segment round-count field, and no 30-round cap enforced anywhere. Consistent with the previously-noted single-segment-vs-4-segment gap, now confirmed at the individual-setting level rather than just the general "segments" concept level. |

**Net for Fable**: genuinely small category — 1 match, 1 not-applicable (a structural roster-model difference, not a missing setting), and the multi-segment gap (4 of the 6 settings) is really the same single known gap counted 4 times. Nothing new here beyond confirming what was already flagged in the broad pass.

### Absent Players and Subs (12 settings) — `scores.py` ghost-scoring logic, `absence_overall_point_policy`

Materially different mechanism, not just missing settings. GLT models an absence around a **"course plays for the absent player"** entity: a separately-tunable pseudo-player (`Absent Player Penalty Strokes`, default 0-handicap-minus-N making par) whose points are subject to a large family of per-category min/max clamps. BGLT instead ghost-scores an absent player using **their own playing handicap against par** (`scores.py:~1289-1304`: `gross[pid] = par + strokes_on_hole(their_own_playing_handicap, ...)`) — self-referential to the missing player, not a separately-configurable course entity. Overall-point handling is one league-wide 3-way policy (`absence_overall_point_policy`: `always`/`never`/`excused_only`, `scores.py:170-178`), not the granular numeric caps/floors GLT exposes.

#### Deep-dive: full 12 settings, GLT descriptions read in full

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Absent Player Penalty Strokes | Course-as-player entity playing better than scratch by N strokes, independent of the absent player's own handicap | **Gap** — no such entity exists; BGLT's ghost score is always the absent player's *own* handicap applied to par, which is a fundamentally different mechanism, not a missing knob on the same mechanism. |
| Maximum Absent Points - Player | Cap points a player can win against the absent-course entity, in the player-points category | **Gap** — hole/overall points against a ghost score are computed identically to a real match, with no clamping anywhere (`standings.py:803-835` confirmed unclamped). |
| Maximum Absent Points - Team | Same, team-points category | **Gap** — same reasoning. |
| Maximum Points - Opponent Absent - Player | Hard ceiling on points regardless of match outcome, when facing the course | **Gap** — `absence_overall_point_policy` is a 3-way on/off/conditional switch for the *overall* point only, not a numeric max on player points generally. |
| Maximum Points - Opponent Absent - Team | Same, team-points category | **Gap** — same. |
| Maximum Substitute Points - Player | Cap points a real sub can win for the player they're subbing for | **Gap** — no cap exists; a sub's score feeds scoring/standings exactly like a real player's, unbounded. |
| Maximum Substitute Points - Team | Same, team-points category | **Gap** — same. |
| Maximum Team Points When 1 Player Absent (in a 2-player team, no sub found — the remaining player's card is *duplicated* to play both individual matches, capped separately from the "course plays" scenario) | Requires a third absence state: "no sub, teammate plays both positions solo" | **Not applicable** — confirmed via code: BGLT has exactly two absence states (`sub_player_id` set → real sub plays; else → ghost-scored ) with no duplicated-entry/"teammate plays both matches" path anywhere in `scores.py`. The scenario this setting caps doesn't exist in BGLT at all — every position always gets a score (real or ghost), so there's nothing to duplicate. |
| Minimum Points - Opponent Absent - Player | Floor on points when facing the course (can award *more* than normally possible) | **Gap** — no minimum-point floor concept exists anywhere in the scoring engine. |
| Minimum Points - Opponent Absent - Team | Same, team-points category | **Gap** — same. |
| Minimum Points - Sub - Player | Floor on points when facing a real sub | **Gap** — same reasoning, no floor concept exists for any opponent type. |
| Minimum Points - Sub - Team | Same, team-points category | **Gap** — same. |

**Net for Fable**: **10 of 12 are real gaps**, all variations on the same missing capability — GLT's fine-grained per-category (player/team) × (min/max) × (vs-course/vs-sub) point-clamping grid has **zero BGLT equivalent**; the closest thing is the single 3-way `absence_overall_point_policy` switch, which only touches the *overall* point, not hole points, and isn't a numeric clamp. **1 setting is a structural mismatch** (Absent Player Penalty Strokes — different ghost-scoring model entirely, not a missing parameter on the same model). **1 setting is not applicable** (Maximum Team Points When 1 Player Absent — the duplicated-solo-entry scenario it caps doesn't exist in BGLT). This is the starkest gap ratio of any category audited so far — if Fable wants a single closure project with the most settings addressed per unit of work, a general "point clamp" layer (min/max, per player-or-team, applied uniformly regardless of opponent type) would cover 10 of these 12 in one pass, though it doesn't address the ghost-scoring model mismatch itself.

#### Priority read for Fable — Skins + Absent Players/Subs (this pass)

1. **`skins_config.handicap_percent` dead setting** — cheapest possible fix in this pass (read the column where it's already computed, same shape as the other 4 dead settings) but low blast radius (skins is a side pot, not standings). **Update 2026-07-06: on closer inspection this is NOT actually a one-line read** — `_calculate_skins()` reads `net_score` straight from the already-stored `hole_scores` row (computed at score-entry time using the league's *regular* handicap percent), so wiring in a skins-specific percent means computing a second, parallel net score just for skins. See Work Packages correction.
2. **Absent Players/Subs point-clamp gap (10 of 12 settings)** is the single highest-leverage closure item found in this pass — one general min/max point-clamp mechanism (player/team × min/max, applied regardless of opponent type) would close most of a whole category at once, unlike Skins where every gap requires a separate, narrow change to `_calculate_skins`.
3. **Skins gross/net exclusivity** (can't run both pots in the same round) is a real but lower-urgency partial match — most leagues run one or the other, not both.
4. **Skins per-par/half-stroke/round-total stroke caps and multipliers** are genuine gaps but narrow, cosmetic-adjacent features (skins-specific stroke tuning) — lower priority than the absence point-clamp work above.
5. **Absent Player Penalty Strokes** (structural ghost-scoring mismatch) and the **Skins Not-applicable cluster** (team-skins, league-groups, calculated-handicap distinction) all require a prerequisite feature BGLT doesn't have — flag for the same "new product surface" conversation as stroke-play/best-ball/team-totals, not bundled into ordinary gap-closure work.

### Schedule (2 settings) — `app/routes/schedule.py`, `app/templates/schedule/`

*First look — missed by both the original broad pass and every deep-dive so far.*

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Show Subs on Schedule (subs always show on scorecards regardless of this setting; this only controls the schedule page) | Toggle to display a confirmed sub's name alongside the original player on the schedule view | **Partial** — `schedule/index.html` (lines 195,197,211,213) always renders only the original roster player names (`p1_name`/`p2_name`); there is no sub-display path on the schedule page at all, and no toggle — hardcoded to GLT's "NO" behavior. Confirmed BGLT does show subs on scorecards (`scores.py` `sub_for` display, matches GLT's non-negotiable baseline), so the gap is scoped narrowly to the schedule page only. |
| Show Tee Times | Toggle to hide tee times site-wide (e.g. shotgun-start leagues where times aren't meaningful) | **Partial** — BGLT computes `has_tee_times` (`schedule.py:1195`) by checking whether *any* matchup actually has a tee_time set, and auto-hides the column when none do. This produces the right result when no times are entered, but there's no independent admin toggle to force-hide times that *are* entered (e.g. a shotgun league that logs times internally but doesn't want them public) — the auto-detect behavior is a coincidental proxy, not the same mechanism. |

**Net for Fable**: both are narrow, low-effort settings-branch additions (an explicit `show_subs_on_schedule` / `show_tee_times` boolean each, gating existing template logic that's already 90% there) — cheap wins if picked up, not required for anything structural.

### Beta Testers (1 setting)

*First look — missed by both the original broad pass and every deep-dive so far.*

**New Feature Tester** ("If on, the league has access to new features") — this is GLT's own internal early-access/beta-program flag (applicability badge is "H" only — Handicap Manager admin context, not a normal league setting). **Not applicable** — it's infrastructure for GLT's own feature-rollout process, not a league-facing capability to match; there's nothing here for BGLT to have an equivalent of.

### General / Administrative / Scorecards / Signup (small categories, 16 settings total)

#### Deep-dive: full 16 settings across General Information (5), Administrative (1), Scorecards (5), Signup and Renewal (1), General (4) — GLT descriptions read in full, verified against schema/templates (no `hide_player_detail`, `phone` formatting, `time_zone`, `custom_theme`, teebox-combine, `round_notes`, `distance_label`, `pace_of_play`, `scorecard_message`, `days_between_rounds`, `league_playfield`, `league_type`, `min_tee_times`, or `static_flights` columns/templates found anywhere)

| GLT Setting | Feature ability check | Verdict |
|---|---|---|
| Days Between Rounds (incl. same-day split-tee-time mode) | Configurable gap between rounds, or same-day double-round mode | **Gap** — no such setting; schedule dates are set per-round directly by the admin with no gap-enforcement or same-day-pairing concept. |
| League Playfield (real course vs. simulator) | Toggle affecting how the league is categorized | **Gap** — no simulator-vs-real-course concept anywhere; BGLT assumes real courses throughout (course/tee/hole data model). |
| League Type (individual vs. 2-player team, incl. scramble-as-individual) | Choice of league structural format | **Not applicable** — BGLT is hardcoded to the 2-player-team model everywhere (schema, scoring, standings); there's no individual-league mode to select, so this isn't a missing toggle so much as a structural given. |
| Minimum Number of Tee Times | Force extra tee-time slots beyond the calculated minimum | **Gap** — confirmed no tee-time-count logic in `schedule.py`'s generation path at all (`generate_round_robin` only pairs teams, doesn't allocate tee-time slots by count). |
| Static Flights (admin manually sets A/B, vs. automatic handicap-based assignment) | Toggle between manual and automatic A/B role assignment | **Related to the `ab_designation_method` dead setting already flagged in Tier 2/3** — BGLT always auto-assigns by handicap with no manual-override toggle; this is the same underlying gap, viewed from GLT's naming rather than BGLT's dead-setting name. Not a new, separate gap. |
| Custom Theme (Administrative, 1 setting) | League-specific visual theme flag | **Gap** — no per-league theming concept exists; all leagues share the same UI. |
| Combine Same Teebox Info on Scorecards | Collapse identical yardage/par/hole-handicap rows across tee boxes on printed scorecards | **Gap** — no such collapsing logic found in scorecard templates. |
| Display Round Notes | Show admin-entered per-round notes on scorecards | **Gap** — no round-notes field or display anywhere. |
| Distance Label (Yards vs. Metre, label-only) | Cosmetic unit label toggle | **Gap** — BGLT hardcodes "Yards" throughout with no label-swap setting. |
| Pace of Play Time | Display expected round duration + computed end time on scorecards | **Gap** — no such field or display exists. |
| Score Card Message (admin-set message printed on every scorecard) | Persistent admin-authored message on printed scorecards | **Gap** — no such field exists. |
| Golf League Tracker Account Type (Standard vs. Handicap Only) | GLT's own account/subscription-tier selector | **Not applicable** — BGLT has its own, differently-shaped self-registration toggles (`reg_enabled`/`public_enabled`) rather than a GLT-style account-tier setting; different concept, not a missing parameter on the same one. |
| Allow Player Score Entry | Players can self-report scores, admin approval gate | **Match** — `self_reporting_enabled` + `self_reporting_requires_approval` (`schema_postgres.sql:126-127`). |
| Hide Player Detail Information (email/phone hidden from other players) | Player-to-player contact-info visibility toggle | **Gap** — no such visibility flag exists; BGLT's `email_opt_out*` columns control *email sending*, not *profile-field visibility to other players* — a different concept, confirmed via schema, not a naming coincidence. |
| Phone Automatic Formatting | Auto-format phone number input | **Gap** — no such formatting logic found. |
| Time Zone | Per-league local time zone for displayed timestamps | **Gap** — no timezone field or conversion logic found; timestamps are presumably server-local throughout. |

**Net for Fable**: 2 of 16 match (Allow Player Score Entry, and Static Flights is really the already-known `ab_designation_method` gap rather than a new 3rd item), 2 are not applicable (League Type, Account Type — structural/conceptual mismatches, not missing settings), and the remaining 12 are real gaps, all narrow single-purpose admin/cosmetic conveniences with no shared underlying mechanism to batch-fix (unlike the Absent Players/Subs or E-mails categories) — each would be its own small, independent addition if picked up.

-----

# Part 2: Workflow Parity

*What do GLT's help articles say about actually **using** the product — as opposed to what's merely configurable? Sourced from GLT's ~50 how-to articles (narrative, screenshot-heavy content, fetched via WebFetch rather than the raw `curl` approach used for Parts 1/4 — no auth needed, all pages public). This is the natural raw material for a future site Wiki "How To" section — see Work Packages backlog, not built yet; gated on this Part being reviewed, which it now is. **Complete as of 2026-08-08** — all ~50 articles read, reviewed in 5 parallel batches by theme. See "New Gaps Found — Workflow Parity Pass" at the end of this Part for the consolidated findings list.*

## How-To Articles (grouped by theme; ✓ = reviewed and reflected in this doc)

*(Built 2026-07-04 from GLT's own how-to index — this is the reference for picking an article to review next, not a changelog. Update the Reviewed column as articles get audited; don't re-derive this list from scratch.)*

| Article | URL slug (append to `/glthome/cms/help/how-to/` unless noted) | Reviewed? |
|---|---|---|
| **Setup / Account / Admin** | | |
| Player and Admin Logins | `player-login` | ✓ *(reviewed 2026-08-08 — GLT: League ID + single shared Admin password + single shared Player password, admin resets via "Forgot Password," no invite feature, admin distributes credentials via Group E-mail. BGLT: individual `users` accounts with `password_hash` + `user_league_roles`, layered on top of the same league-level shared-secret model — a superset, not a gap.)* |
| Before You Tee Off - League Check List | `league-checklist` | ✓ *(reviewed 2026-08-08 — pre-season decision checklist: absence/forfeit policy, rainouts, prize money, subs, tiebreakers, scheduling, handicap calc, tees. Every item maps to a BGLT setting/mechanism already inventoried in Part 1 — Match, no new gaps, it's a workflow gloss on settings we already have.)* |
| Transferring League Access | `transferownership` | ✓ *(reviewed 2026-08-08 — GLT: 4 informal methods built around the single shared admin password. BGLT: individual accounts + `user_league_roles` make transfer a role grant/revoke — Match, more robust mechanism, no gap.)* |
| Purchasing Golf League Tracker | `purchasing` | ✓ *(reviewed 2026-08-08 — PayPal/card checkout + "Extended League" tier gating. BGLT has no billing/purchase flow — not applicable, BGLT isn't monetized per-league today.)* |
| System Requirements | `requirements` | ✓ *(reviewed 2026-08-08 — boilerplate browser/JS/cookie requirements, no comparison angle.)* |
| **League Year-End Maintenance** | `year-end` | ✓ *(reviewed 2026-07-04 — it's a link index to 3 other articles, not itself substantive; see Season Rollover Workflow section below)* |
| **Starting a New Season** | `renewal` | ✓ *(reviewed 2026-07-04, deep-dive vs WP3.20 — see Season Rollover Workflow section below)* |
| Typical League Settings | `system-averagees` | ✓ *(reviewed 2026-08-08 — aggregate stats across GLT's paid leagues (avg 18 rounds, 91.4% handicap %, 68% two-player-team leagues, etc.). Reference data only, not a comparison — no gap.)* |
| Tracking Only Player Handicaps | `tracking-handicaps` | ✓ *(reviewed 2026-08-08 — GLT's separate paid "Handicap Manager" product tier strips out scheduling/points entirely. Not applicable — a GLT monetization tier, not a workflow BGLT's single-product model needs.)* |
| Transferring Data / From LeagueGolfer.com | `coming-soon` / `transfer-league-golfer` | ✓ *(reviewed 2026-08-08 — `coming-soon` is an unbuilt GLT placeholder; real content lives at `transfer-league-golfer`, a manual copy-paste-to-Excel workaround. BGLT's `migration.py` (CSV/xlsx import, GLT-column-format-aware) is the more direct equivalent — Match, BGLT's importer already exceeds this. See Import Tool Comparison below for the fuller import review.)* |
| Setup is Incomplete | `incomplete-setup` | ✓ *(reviewed 2026-08-08 — GLT's 3 setup-completeness checks all key off its "Number of Players" cap/numbering system, which doesn't exist in BGLT — Not applicable, same conclusion as Part 1's League Size finding.)* |
| Seeing Ads on GLT / Payment Problems | `ads` / `payment-problems` | ✓ *(reviewed 2026-08-08 — pure support/troubleshooting content (malware ads, PayPal card/VPN issues), no BGLT comparison angle.)* |
| **League Structure & Roster** | | |
| Two Player / Individual Player League Ideas | `two-player-league-ideas` / `individual-player-league-ideas` | ✓ *(reviewed 2026-08-08 — pure format-idea articles describing scoring recipes buildable via GLT's points settings. All 3 formats already exist as BGLT scoring presets — descriptive only, no gap.)* |
| Simulator Leagues vs Standard Leagues | `simulator-standard` | ✓ *(reviewed 2026-08-08 — advisory article, not a distinct feature: incremental course entry, end-dates over fixed tee times, max-score cap for inflated sim scores, field formats over match play. BGLT already has every underlying primitive — nothing missing, just usage guidance a Wiki How-To could restate.)* |
| Setting up Your Players or Teams | `number-of-players` | ✓ *(reviewed 2026-08-08 — GLT's fixed "Number of Players" setting (confirmed N/A in Part 1) drives ghost-player padding and manual renumbering. BGLT's uncapped, active/inactive-flag roster model achieves the same outcomes via a simpler mechanism — not a gap, GLT's steps serve a constraint BGLT doesn't have.)* |
| Odd Number of Players or Teams | `setting-up-an-odd-number-player-or-teams` | ✓ *(reviewed 2026-08-08 — "ghost player" workaround for GLT's even-count requirement. Not applicable: BGLT has no player-count parity requirement; BGLT's "playing against the course" absence handling already covers the same underlying scenario.)* |
| Importing Players and Scores from Another System | `importing-data` | ✓ *(reviewed 2026-08-08 — see Import Tool Comparison subsection below)* |
| Multiple Divisions or Flights ("League Groups") | `golf-league-tracker-divisions` | ✓ *(reviewed 2026-08-08 — confirms GLT funnels divisions/flights/playoffs/skins through one "League Groups" primitive; BGLT's 2026-07-04 Decisions Log entry already documents the deliberate divergence to dedicated per-feature flows. No new finding, confirms the prior decision was made with full knowledge of GLT's approach.)* |
| Setting Up Leagues for More than 2 Players per Team | `more-than-two-player-teams` | ✓ *(reviewed 2026-08-08 — see Team-Size Constraint subsection below — real structural gap confirmed)* |
| Two Player Flighting Options | `two-player-flighting` | ✓ *(reviewed 2026-08-08 — GLT's A/B match-play flighting settings (Static Flights, Rematch Subs by Handicap, Swap A/B Matchups) have no BGLT equivalent — confirmed via grep, no `static_flight`/`swap_ab` concept in `league_settings`. 🟡 Soft gap, relevant only if BGLT ever supports skill-tiered sub-pairings; not raised by any current league, no plan needed.)* |
| **Courses / Tees** | | |
| Entering 27 Hole Courses | `entering-27-hole-courses` | ✓ *(reviewed 2026-08-08 — GLT requires 3 separate 18-hole course/tee combos for 27-hole facilities. BGLT's course model already supports arbitrary tee/hole configurations including independent 9-hole sets — the same 3-combination approach works today, no schema gap, just an undocumented admin data-entry pattern (Wiki How-To candidate).)* |
| Assigning Tee Boxes | `assign-tees` | ✓ *(reviewed 2026-08-08 — see Bulk Tee Assignment subsection below — soft gap confirmed)* |
| Adjusting Course/Hole Info | `/glthome/cms/how-to/course/hole-handicaps` (note different path) | ✓ *(reviewed 2026-08-08 — thin article, 3-step pointer to GLT's course editor. BGLT's `courses.py` has full course/tee/hole CRUD already — Match, no further detail needed.)* |
| **Scheduling** | | |
| Setting Your Tee Times / Tee-Time Priority / Split Tee Times | `set-tee-times` / `tee-time-priority` / `split-tee-times` | ✓ *(reviewed 2026-08-08 — descriptive setup guides; no specific BGLT setting was flagged as a likely-match going in, treated as informational. See Scheduling Workflow Notes below.)* |
| Shot-Gun Starts | `shotgun-start` | ✓ *(re-assessed 2026-08-13 with GLT's actual page content — 🟡 soft gap, confidently assessed. More already built than the 2026-08-08 pass assumed (per-matchup tee_time/starting_hole + a working admin editor + apply-to-all). Missing: season-level default template, default-vs-override visibility, A/B same-hole group sharing. See Scheduling Workflow Notes below.)* |
| Creating a Schedule | `creating-a-schedule` | ✓ *(reviewed 2026-08-08 — GLT's 3 methods (auto-generate, edit single round, auto-set pairings) all map cleanly to BGLT's `generate_round_robin()`, single-round edit UI, and rain-out reschedule flow — Match, no gap.)* |
| Adjusting Rounds, Dates, and Segment Breaks | `adjusting-rounds` | ✓ *(reviewed 2026-08-08 — BGLT already has segment support (`migrate_add_segments.py`) and the same completed-score safeguard on schedule edits GLT describes — Match, no gap.)* |
| How To Deal with Rainouts | `how-to-deal-with-rainouts` | ✓ *(reviewed 2026-08-08, deep-dive vs BGLT's rain-out mechanism — see Rain-Out Workflow Comparison below)* |
| Changing the Course or Side for a Round | `course-side` | ✓ *(reviewed 2026-08-08 — GLT's single-round + bulk-for-all-rounds course/side edit, plus a post-scores-entered correction path, plausibly match BGLT's existing schedule + admin score-correction tooling — not individually route-confirmed this pass, treated as likely-match.)* |
| Moving Some Groups to the Opposite Side for a Round | `moving-a-round` | ✓ *(reviewed 2026-08-08 — GLT's workaround is a manual 5-step process; no dedicated BGLT feature found/checked. 🟡 Soft gap, unconfirmed — BGLT's general insert-round + edit-matchup primitives could likely achieve the same result manually, similar to GLT's own workaround. Not worth a dedicated plan.)* |
| Rounds Types and Playoffs | `round-types` | ✓ *(reviewed 2026-08-08 — GLT's 4 round types and 3 playoff patterns are informational; BGLT's rain-out `week_type` + admin week-status values look like a plausible-equivalent typing system, not line-by-line confirmed this pass.)* |
| **Handicaps** | | |
| New Player Handicap Options | `new-player-handicap-options` | ✓ *(reviewed 2026-08-08 — padding, establish-threshold, and retroactive-apply concepts all confirmed present in BGLT (`handicap.py`, `min_rounds_for_handicap`) — Match, no new gap. See Handicap Padding + Player Health subsection below.)* |
| Adjusting Handicaps for Player Health | `handicaps-player-health` | ✓ *(reviewed 2026-08-08 — partial gap found: per-player exclusion-date field exists in the DB and handicap pipeline but has no admin UI; see Handicap Padding + Player Health subsection below.)* |
| Handicap Padding Example | `handicap-padding` | ✓ *(reviewed 2026-08-08, cross-checked against `padding_score_count` — confirmed live in `handicap.py`'s averaging pipeline, exact mechanism match, not just a name-match. See Handicap Padding + Player Health subsection below.)* |
| Limiting Strokes per Hole for Handicapping | `strokes-per-hole-limit` | ✓ *(see Handicap Settings / Calculation section in Part 1)* |
| Recommended Handicap Settings | `common-handicap-options` | ✓ *(see Handicap Settings / Calculation section in Part 1)* |
| **Scoring / Points** | | |
| Setting Up Points | `setting-up-points` | ✓ *(reviewed 2026-08-08 — orientation article; confirms GLT's "one standings format at a time" constraint matches BGLT's season-level `scoring_mode`.)* |
| Best Ball Format Options | `bestball` | ✓ *(reviewed 2026-08-08 — see Scoring/Points Workflow Review below; soft gap found on handicap-allocation method choice)* |
| Team Totals Format | `teamtotals` | ✓ *(reviewed 2026-08-08 — same handicap-allocation soft gap as Best Ball, see Scoring/Points Workflow Review below)* |
| Awarding Points for Position in a Round | `stroke-play-position-points` | ✓ *(reviewed 2026-08-08 — describes GLT's rank/field-position stroke play, already covered by Part 4 #30/#31, declined 2026-07-10 via Plan L — no new gap.)* |
| Recording and Overriding Points | `recording-points` | ✓ *(reviewed 2026-08-08 — see Scoring/Points Workflow Review below; real gap found: no points-override capability in BGLT)* |
| Entering Scores for a Random Player or Team | `entering-scores-for-a-random-team` | ✓ *(reviewed 2026-08-08 — GLT's permanent-ghost-player + handicap-override pattern matches BGLT's existing `resolve_or_create_sub_player()` + manual handicap override field — Match, different UI framing, no gap.)* |
| Incomplete Rounds | `incomplete-rounds` | ✓ *(reviewed 2026-08-08 — see Scoring/Points Workflow Review below; soft gap found: no per-round handicap-exclusion option)* |
| Entering Scramble Scores for a Non-Scramble League | `entering-scramble-scores-for-non-scramble-league` | ✓ *(reviewed 2026-08-08 — narrow GLT-specific workaround for a limitation BGLT doesn't share — not applicable.)* |
| Live Scoring for the League Manager | `league-manager-live-scoring` | ✓ *(reviewed 2026-08-08 — see Scoring/Points Workflow Review below; BGLT's Self-Report is the closest analog, different (async vs. live) mechanism, not a gap.)* |
| **Subs / Absences** | | |
| Adding Subs to Scorecards & Sub Requests | `/glthome/cms/how-to/adding-subs` (note different path) | ✓ *(reviewed 2026-08-08 — GLT's admin-assign / player-request-and-accept model matches BGLT's `subs.py` (`request_sub()`, `admin_assign()`, `admin_requests()`, `my_requests()`) — Match, different route names, same two-path model.)* |
| Sub Flighting in 2-Player Team League | `/glthome/cms/how-to/sub-flighting` (note different path) | ✓ *(reviewed 2026-08-08 — GLT's opt-in "re-flight subs by handicap" + retroactive swap-matchup control has no confirmed BGLT equivalent beyond the unrelated course/side `swap_side()`. 🟡 Partial/soft gap, not confirmed without deeper digging into matchup-assignment code — flag for follow-up if a league hits this scenario.)* |
| How to Handle Absent Players and Teams | `playing-against-the-course` | ✓ *(reviewed 2026-08-08 — GLT's per-league-type absent-player menu matches BGLT's `player_absences` + `resolve_or_create_sub_player()` + "against the Course" mechanic — Match, different UI shape, no confirmed gap.)* |
| Scoring Against the "Course" | `/glthome/cms/how-to/scoring-against-the-course` (note different path) | ✓ *(reviewed 2026-08-08 — GLT's core Course mechanic (0-handicap ghost, always scores par) exactly matches BGLT's ghost-score synthesis in `scores.py:1276-1280`. 🟡 Partial gap: no confirmed BGLT equivalent for GLT's optional "Absent Player Penalty Stroke" or point min/max caps specific to Course matchups.)* |
| **Skins / Contests** | | |
| Skins | `skins` | ✓ *(reviewed 2026-08-08 — pay-per-play/fixed-pot, gross/net, per-group pots all match BGLT's `skins_config` — Match, consistent with Part 4's existing build-out, no new gap.)* |
| Setting up Custom Contests | `custom-contests` | ✓ *(reviewed 2026-08-08 — GLT's single-contest-tracks-hole-number model matches BGLT's `contests.py` `admin_add()`/`admin_edit()` — Match.)* |
| **Reports** | | |
| Printing, Exporting, and E-Mailing Reports | `printing-reports` | ✓ *(reviewed 2026-08-08 — generic browser print/export/PDF UX advice, no code-level BGLT equivalent needed.)* |
| Creating Custom Reports | `custom-reports` | ✓ *(reviewed 2026-08-08 — GLT lets any report's filter state be saved as a named "Custom Report" and pinned to the home page as a widget. 🔴 No equivalent found in BGLT (not exhaustively grepped) — a real, distinct feature worth a closer look, not just a phrasing difference.)* |
| League Standings | `league-standings` | ✓ *(reviewed 2026-08-08 — weekly hole-by-hole color-coded report already covered by Part 4 Standings section (#27 `detailed-scoresheet`, etc.) and Part 1's settings audit — no new ground.)* |
| Printing Scorecards | `scorecards` | ✓ *(reviewed 2026-08-08 — GLT's toggle-driven customizable scorecard printouts match BGLT's `scores.py print_scorecards()` — Match.)* |
| **Communication** | | |
| Premium E-mail Optional Add-on | `premium-email` | ✓ *(reviewed 2026-08-08 — GLT monetization/business-model add-on (log retention, resend, templates), not a BGLT product gap — no comparison applicable.)* |
| Sending Text Messages | `sending-text-messages` | ✓ *(reviewed 2026-08-08 — paid SMS credits, opt-in texting. Grepped BGLT repo-wide for SMS/text-message — zero hits. 🔴 Real gap — no SMS/texting feature exists in BGLT at all. Likely intentional/out of scope, not a bug — flag for @user, don't assume it should be built.)* |
| Diagnosing E-mail Problems | `email-problems` | ✓ *(reviewed 2026-08-08 — GLT uses Mailgun with an admin-facing Email Log (delivery status per recipient). BGLT's `email_config.py` exists but no delivery-log/troubleshooting view found via grep. 🟡 Partial/soft gap — BGLT likely sends successfully, just lacks the admin-visible delivery log GLT offers.)* |

-----

## Season Rollover Workflow (How-To Comparison vs. WP3.20)

*(Reviewed 2026-07-04 — a workflow-level comparison, not a settings comparison: does GLT's actual season-rollover *process* match what BGLT's "Start Another Season" wizard does? Source: [Starting a New Season](https://www.golfleaguetracker.com/glthome/cms/help/how-to/renewal), full text read. "League Year-End Maintenance" turned out to be just a link index to 3 other articles — League Playoffs, Year-End Outing, and this one — not itself substantive, so no separate comparison needed for it.)*

**GLT's process** (verbatim structure): once a season's last scheduled date is **30+ days old**, the admin gets prompted at login to "copy" the league to a new year. The copy automatically brings over players, courses, tee times, and settings. The admin then walks a checklist of League Setup steps for the new season: update "Number of Players" (setting #69) if roster size changed; add new players / mark departing players as either a **sub** or **inactive** (inactive = admin-only visible, "equivalent to deleting"); reset tee times if changed; set the new league start date; **re-assign player/team numbers** if the roster or teammates changed (used by GLT's scheduling algorithm); generate the new schedule (auto-generator or manual).

**BGLT's wizard** (`app/routes/seasons.py:125-` `start_next()`), checked directly against each step above:

| GLT Step | BGLT Equivalent | Verdict |
|---|---|---|
| 30-day-post-season wait before renewal is offered | `season_is_over()` (`seasons.py:38-59`) offers the CTA as soon as the last matchup date (or `end_date`) is in the past — no cooldown period | **Different, deliberately not a gap to close** — BGLT is more permissive (available immediately). Worth understanding *why* GLT waits 30 days before assuming BGLT's earlier availability is strictly better — could be tied to GLT's own billing/subscription-renewal cycle (a paid-service administrative reason), not a workflow best practice. Flag for @user judgment, not an automatic fix. |
| Players, courses, tee times copied automatically | BGLT: players and courses are league-scoped already (not season-scoped), so there's nothing to "copy" — they're just already there. Matches the *outcome* GLT describes, via a different (arguably simpler) mechanism. | **Match** (different mechanism, same result). |
| Settings copied automatically | BGLT: **optional** via `bring_settings` checkbox, not automatic | **Partial** — BGLT gives the admin a choice GLT doesn't; defaults aside, this is arguably a deliberate improvement (a league changing formats mid-renewal can start clean) rather than a gap, but it does mean BGLT's wizard requires an active choice where GLT just does it. |
| Teams copied automatically | BGLT: **optional** via `bring_teams` checkbox | **Partial**, same reasoning as Settings above. |
| Update "Number of Players" setting if roster size changed | N/A — BGLT has no "Number of Players" setting/cap at all (confirmed **not applicable** in the League Size and Length deep-dive) | **Not applicable** — nothing to update because the constraint this step manages doesn't exist in BGLT. |
| Add new players / mark departing players as sub or inactive | BGLT: `players.deactivate()`/`reactivate()` routes (`players.py:499-531`), `active` column, confirmed player-facing views filter to `active = 1` | **Match** — same underlying ability (mark a departing player inactive rather than deleting, admin can still see/reactivate them), different route names. |
| Reset tee times if changed | BGLT: tee/course setup is league-scoped, not tied to season rollover as a distinct step; schedule generation assigns tee times per matchup directly | **Different mechanism, no gap identified** — BGLT doesn't need a distinct "reset tee times for the new season" step since tee-time assignment happens at schedule-generation time regardless of season boundary. |
| Set new league start date | BGLT: `start_date`/`end_date` fields on the new season, set directly in the wizard form | **Match**. |
| Re-assign player/team numbers (used by GLT's scheduling algorithm) | BGLT: `generate_round_robin(teams)` (`schedule.py:43`) works directly from the team list — no manual numbering step exists or is needed | **Not applicable** — this GLT step exists to feed their specific scheduling algorithm's numbering requirement; BGLT's scheduler doesn't have that requirement, so there's no equivalent step to be missing. Simplification, not a gap. |
| Generate schedule (auto or manual) | BGLT: `/schedule/generate` route, round-robin auto-generator + manual entry | **Match**. |

**Net for Fable**: this workflow comparison did **not** surface any new functional gaps in WP3.20 — every GLT step either has a direct BGLT equivalent, is not applicable because the underlying constraint doesn't exist in BGLT, or is a deliberate design difference (optional vs. automatic settings/teams carry-over) rather than a missing capability. The one item worth a judgment call rather than silent adoption is the **30-day cooldown before renewal is offered** — not clearly a bug or a gap, but worth @user weighing in on whether BGLT's immediate-availability behavior is intentional or worth adding a similar grace period for (e.g. to avoid an admin accidentally rolling over before playoffs/final stats are settled).

-----

## Import Tool Comparison (Importing Players and Scores from Another System)

*Reviewed 2026-08-08.* GLT's import is Excel/CSV only, two categories (player lists, historical scores), requires exact-name matching for score attribution, and walks admin upload → column-map → confirm. BGLT's actual import tool (`app/routes/migration.py`, `GET/POST /admin/migrate/*`) is more capable in every dimension checked: 4 data types (players/teams/schedule/scores, not just 2), both CSV and `.xlsx` (via `openpyxl`), a column-mapping/preview step with auto-suggested field defaults and row include/exclude checkboxes (shipped 2026-08-03 per Work Packages), and downloadable CSV templates per type.

One historical note worth preserving: the 2026-07-19/20 Buckeye League historical import found `migration.py` **couldn't** consume that source export's actual format and required a custom one-off ETL instead — a concrete, format-specific limit of the generalized tool, not a capability gap. **✅ No gap** — BGLT's import tool already exceeds GLT's own here.

-----

## Team-Size Constraint (Setting Up Leagues for More than 2 Players per Team)

*Reviewed 2026-08-08.* GLT handles 3+-players-per-team leagues via a workaround: register as Individual, use League Groups as pseudo-teams, enter matchups manually every week — no automation, no true team-level W-L-T tracking.

BGLT's `teams` table is hardcoded to `player1_id`/`player2_id` columns (confirmed via grep in `app/routes/teams.py` and `database.py`) — there is no path to a 3rd/4th team member at all, not even a manual workaround as clean as GLT's League Groups trick (BGLT has no generic grouping primitive by design — see the Divisions row above).

**🔴 Real, confirmed gap** — BGLT literally cannot represent teams larger than 2 players. Not previously caught by Part 1 (Settings) or Part 4 (Feature Parity) — this is the one place the Workflow pass found something the other two passes missed. Not raised by any current league; log as a Work Packages backlog item only if a league requests 3+/4+-player team formats (e.g. scrambles), not urgent.

-----

## Bulk Tee Assignment (Assigning Tee Boxes)

*Reviewed 2026-08-08.* GLT offers 3 bulk-assignment UIs — per-player (all courses), per-course (all players), and a full player×course grid — plus an "assign by tee-box name" shortcut across multiple courses sharing tee names.

BGLT: tee assignment exists only as a per-player field (`preferred_tee_name`, per the 2026-07-30 tee-resolution hierarchy work) plus a Default Tees mass-edit page (shipped 2026-07-29, a bulk front-end onto the same column). No course-centric bulk grid, no assign-by-name-across-courses shortcut.

**🟡 Soft gap** — BGLT's mass-edit page likely covers the single-course case adequately; the multi-course grid and assign-by-name convenience are real GLT capabilities BGLT lacks, but low priority (most leagues use one primary course). No plan needed unless raised.

-----

## Rain-Out Workflow Comparison

*Reviewed 2026-08-08.* GLT offers two explicit paths — **Cancel** (mark absent/zero-points, round doesn't get replayed, season shrinks by one round) and **Move** (push to next week / insert / append at season end, matchups unchanged).

| GLT Concept | BGLT Equivalent | Verdict |
|---|---|---|
| Mark a week as rained out | `mark_rain_out()` sets `matchups.week_type='Rain Out'` for the week (blocked if any matchup already `status='completed'`) | **Match** |
| "Cancel" (no reschedule, season shrinks, counts as absent/zero) | Leaving a week `week_type='Rain Out'` with no reschedule appears to be the same end-state, but no explicit standings/scoring logic was found treating an un-rescheduled Rain Out week as "count as absent/zero" the way GLT's Cancel does (only reference found: `admin.py:97` mapping it to a `status_label` for the admin week-tracker UI) | **🟡 Partial/unconfirmed** — worth a quick `standings.py` follow-up before calling this a full match |
| "Move" (reschedule to existing/future week) | `reschedule_rain_out()` with `action='overwrite'` | **Match** |
| "Move" (insert new week) | `reschedule_rain_out()` with `action='new_week'` | **Match** |
| Move to a non-standard date | `reschedule_rain_out()` takes a `makeup_date` field directly — looks more flexible than GLT's separate "add date to schedule setup" step | **Likely simplification, not a gap** (not fully confirmed) |
| Adjust playing side after a rainout move | Covered by the general course/side change mechanism | **Match** |

**Net**: BGLT's rain-out feature is at least as capable as GLT's, structured differently (single `week_type` flag + optional reschedule vs. GLT's binary Cancel/Move choice). No real gap found; the one open thread is confirming how an un-rescheduled Rain Out week is actually treated in standings math.

-----

## Handicap Padding + Player Health

*Reviewed 2026-08-08.* `padding_score_count` (`league_settings`, `init_db.py:148`) is used directly in the handicap-averaging pipeline (`handicap.py:206`, `:511`; also `api.py:2503`, `players.py:1766`) exactly as GLT's Handicap Padding article describes — an artificial padding-score count filling out the averaging window when a player has fewer real rounds than the window size. **Match** — confirmed live in code, not just a settings-list name-match.

GLT's "Oldest Date For Handicap" per-player field (excludes old scores after a health issue) has a direct BGLT counterpart: `players.oldest_score_date`, actively read by `handicap.py:217` and `players.py:1776`/`api.py`. **But** the player edit route's UPDATE statement (`players.py`, ~line 1114-1124) does not include it in the SET clause — no admin UI field sets this column. **🟡 Partial gap**: the mechanism and downstream logic fully exist, but there's no way for an admin to actually use it today. Worth a small WP item (add "Oldest Date For Handicap" to the player edit form) if @user wants this workflow enabled.

New Player Handicap Options' other concepts (blank-until-established handicap, retroactive application once established, 2-round-max establishment recommendation) all map to BGLT's existing `min_rounds_for_handicap` flow — **Match**, no new gap.

-----

## Scheduling Workflow Notes

*Reviewed 2026-08-08.* Lighter items not warranting individual deep-dives:

- **Creating a Schedule / Adjusting Rounds & Segments**: Match — see table rows above.
- **Tee Times / Tee-Time Priority / Split Tee Times**: purely descriptive GLT setup mechanics, not cross-checked against a specific BGLT setting this pass — treated as informational.
- **Shot-Gun Starts** (re-assessed 2026-08-13, @user supplied GLT's actual Tee Times page content): **🟡 soft gap, now confidently assessed** — more built than the 2026-08-08 pass assumed. BGLT already has per-matchup `tee_time` (free text) + `starting_hole` (1-18) columns (`schema_postgres.sql:348-349`), a real wired admin editor (`admin/edit_week.html` + `admin.py`'s `edit_week` POST handler) that sets both for every matchup in a week, and a client-side "Apply to All" shortcut (`edit_week.html:60-68`/184-190) that already covers GLT's "set the tee times the same for all players" step directly. Front/back-9 tracking also exists at the week level (`schedule.py`'s `bulk_edit` "which nine" toggle). **Genuinely missing**: (1) a season-level *default template* — GLT lets you set a group's front-hole *and* back-hole once and it auto-applies every week; BGLT requires re-entering values fresh each week, no persistent "this is Group 1's slot" concept; (2) no default-vs-override visibility (GLT's "Tee Time Summary For All Rounds" table); (3) no A/B same-hole group-sharing — a *different* concept from BGLT's already-removed `ab_designation_method` (that was individual player role assignment within a team, not hole-sharing between groups — don't conflate the two just because they share "A/B" naming); (4) no "Number of Players"-driven group-count setting, though this is probably moot since BGLT's group count is naturally however many matchups the schedule has that week. Not built — no current league has asked for shotgun format.
- **Course/Side Changes**: plausible match to BGLT's schedule-edit + admin score-correction tooling, not route-by-route confirmed.
- **Moving Groups to Opposite Side**: GLT's own solution is a manual 5-step workaround; BGLT likely has the same manual capability via its insert-round + edit-matchup primitives — **🟡 soft gap, unconfirmed**, not worth a dedicated plan.
- **Round Types and Playoffs**: GLT's 4 round types / 3 playoff patterns plausibly map to BGLT's rain-out `week_type` + admin week-status values, not line-by-line confirmed.

-----

## Scoring/Points Workflow Review

*Reviewed 2026-08-08.*

**Best Ball / Team Totals handicap-allocation method — 🟡 soft gap.** GLT offers 3-4 selectable handicap-allocation methods for these formats (default "off the low player," "full handicaps," a flight-based comparison, and for Team Totals a "Difference in Team Handicap Total" default). BGLT's `combine_team_hole_score()` (`scores.py:105`) always combines already-net scores computed from each player's own full handicap — i.e. BGLT hardcodes GLT's "Player's Full Handicaps" option only, with no admin choice of allocation method. Not previously flagged in Part 4's Best Ball/Team Totals rows (#22/#26). Real but narrow — worth a WP backlog line, not urgent.

**Recording and Overriding Points — 🔴 real gap.** GLT has a distinct two-step workflow: enter scores → separately "Record Points" (or auto via "Save and Record Points"), with an explicit "Save and Override Points" path opening a review screen where computed points can be manually typed over and locked in. `scores.py` computes points inline as part of score save (`compute_match_result`, `compute_classical_stroke_play_points`, etc.) with **no separate record-points step and no manual point-override UI/field anywhere** — BGLT has manual *handicap* override (`hcp_marker`/`is_manual_override`) but nothing equivalent for *points*. Leagues with special-rule point adjustments (the article's stated use case) have no way to hand-adjust a computed point value in BGLT today. This is the clearest actionable finding from the whole Workflow pass — worth flagging to @user as a real candidate.

**Incomplete Rounds — 🟡 soft gap.** GLT's unplayed-hole rule (par + full handicap strokes) matches BGLT's own ghost-score synthesis almost exactly (`scores.py:1618-1633`). Two things not found in BGLT: (1) a checkbox to exclude a partial/incomplete round from handicap calculation (grepped for `exclude*handicap`/`count_for_handicap`, no such option exists), and (2) a minimum-holes-played threshold — but GLT doesn't enforce that in software either, so that part isn't a gap. The handicap-exclusion checkbox is the one real soft gap here.

**Live Scoring for the League Manager.** GLT's Live Scoring is real-time, per-round-enabled, player self-entry with a live leaderboard and an admin "Confirm Scores" step. BGLT's closest analog is `self_report.py` — async, anytime submission with an admin approve/reject queue, but no live/during-round leaderboard or per-round toggle. **Partial** — same end goal, meaningfully different (async vs. live) mechanism. Not flagged as a gap since Self-Report is a deliberate existing design choice; noted for wiki-writing context later.

-----

## New Gaps Found — Workflow Parity Pass (2026-08-08)

*Consolidated from all 46 articles reviewed this pass. None of these were previously recorded in Part 1 or Part 4 (checked for overlap). Severity: 🔴 real/confirmed gap · 🟡 soft/partial or unconfirmed gap. This is inventory for @user judgment, not a build proposal — nothing here should be assumed to need building.*

| # | Item | Severity | Source Article | Notes |
|---|---|---|---|---|
| 1 | Team size capped at 2 players (`teams.player1_id`/`player2_id` hardcoded) | 🔴 | Setting Up Leagues for More than 2 Players per Team | Confirmed via code read. Only real *structural* gap found this pass. |
| 2 | ~~No points-override capability (only handicap override exists)~~ | ✅ **Built 2026-08-09** | Recording and Overriding Points | New `point_overrides` table + `apply_point_overrides()`, wired into all 9 real `match_results` write sites; override/clear routes linked from Scoring Debug. `total_points`-only v1 scope (not a per-component override) per @user's go-ahead. See `Plans/2026-08-09-points-override-technical-spec.md`, WP3.1. |
| 3 | No SMS/texting feature | 🔴 | Sending Text Messages | Confirmed zero grep hits repo-wide. Likely intentional scope choice. |
| 4 | No saved-filter "Custom Report" + home-widget mechanism | 🔴 | Creating Custom Reports | Not exhaustively grepped — worth a closer look before treating as fully confirmed. |
| 5 | ~~Shot-gun start scheduling not found~~ | 🟡 | Shot-Gun Starts | **Re-assessed 2026-08-13** — more built than assumed here (per-matchup tee_time/starting_hole + working admin editor + apply-to-all shortcut already exist). Real remaining gaps: no season-level default template, no default-vs-override visibility, no A/B same-hole group sharing. **Spec written 2026-08-13**: `Plans/2026-08-13-shotgun-tee-time-technical-spec.md`. Not built — no current league runs shotgun format. See Scheduling Workflow Notes. |
| 6 | ~~`oldest_score_date` (handicap health exclusion) has no admin UI~~ | ✅ **Built 2026-08-08** | Adjusting Handicaps for Player Health | Date field added to `players/edit.html`, wired through `players.py`'s edit route. See WP3.1. |
| 7 | Best Ball/Team Totals handicap-allocation method is hardcoded, not selectable | 🟡 | Best Ball Format Options / Team Totals Format | BGLT only supports GLT's "Player's Full Handicaps" option. |
| 8 | No handicap-exclusion checkbox for partial/incomplete rounds | 🟡 | Incomplete Rounds | |
| 9 | No two-player A/B skill-tier sub-flighting (Static Flights, Rematch by Handicap, Swap A/B) | 🟡 | Two Player Flighting Options | |
| 10 | No bulk multi-course tee assignment or assign-by-name shortcut | 🟡 | Assigning Tee Boxes | Single-course mass-edit already covers the common case. |
| 11 | Sub re-flighting by handicap / retroactive swap-matchup control unconfirmed | 🟡 | Sub Flighting in 2-Player Team League | Needs deeper matchup-assignment code review to confirm either way. |
| 12 | No Absent Player Penalty Stroke or point min/max caps for Course matchups | 🟡 | Scoring Against the "Course" | Core ghost-score mechanic itself is a confirmed Match. |
| 13 | No admin-visible email delivery log/troubleshooting view | 🟡 | Diagnosing E-mail Problems | `email_config.py` exists; unclear if delivery even fails in practice. |
| 14 | Un-rescheduled Rain Out weeks — standings treatment unconfirmed | 🟡 | How To Deal with Rainouts | Not confirmed as a gap, just a confirmed unknown; quick `standings.py` follow-up would resolve it. |
| 15 | "Moving groups to opposite side" has no dedicated feature | 🟡 | Moving Some Groups to the Opposite Side for a Round | Likely achievable manually via existing insert-round/edit-matchup primitives, GLT's own solution is also just a manual workaround. |

-----

# Part 3: UI Parity — ON HOLD

*Flagged on hold 2026-07-06 per @user: GLT's own UI/UX is widely regarded as the weakest part of the product, and BGLT already exceeds it through better design as a matter of course — a screen-by-screen audit isn't a good use of time right now. Sections below are left as placeholders in case a specific GLT page turns out to have a genuinely useful layout idea worth borrowing later; not being actively worked.*

## Stats

| GLT Page | BGLT Equivalent | Notes |
|---|---|---|
| Detailed Points Score Sheet | `/schedule/<id>/week/<n>/scorecards` | Weekly view with dropdown; linked from dashboard Recent Results |

-----

## Schedule / Results

| GLT Page | BGLT Equivalent | Notes |
|---|---|---|
| *(TBD — on hold)* | | |

-----

## Standings

| GLT Page | BGLT Equivalent | Notes |
|---|---|---|
| *(TBD — on hold)* | | |

-----

## Admin

| GLT Page | BGLT Equivalent | Notes |
|---|---|---|
| *(TBD — on hold)* | | |

-----

# Part 4: Feature Parity

*What whole **features** does GLT have that BGLT is missing (or vice versa) — a different question from Settings (what's configurable) or Workflow (how-to articles describe using it). Opened 2026-07-09 per @user, starting narrow: the Rules page and the Stats/reporting section, since that's "mostly stats and ways to display them." Deliberately left open — @user is feeding info incrementally rather than this being audited end-to-end in one pass.*

## Access

**Confirmed working 2026-07-09.** Logged into GLT's own site (not just the public settings-list/how-to pages used for Parts 1–2) using league credentials @user provided out-of-band — **not recorded here**, ask @user rather than storing a password in a committed file. GLT's login is a standard ASP.NET form (`POST /glthome/security/loginaction`, fields `UserId`/`Password` + an `__RequestVerificationToken` anti-forgery token scraped from the login page first) — worked cleanly with the same `curl` + realistic-`User-Agent` + cookie-jar approach as the rest of this doc's Access Notes. **GLT did not deny access at all once authenticated** — no additional bot-blocking encountered beyond what's already documented for the public pages.

## Rules Page

- `https://www.golfleaguetracker.com/glthome/league/leaguerules` (note: lowercase — the capitalized `/LeagueRules` link on the nav 301-redirects here) — confirmed accessible (200, ~41KB), **not yet reviewed for content**.

## Stats

**Nav structure now mirrors GLT's own statistics directory — shipped 2026-08-02, per @user.** Everything below this note is content/feature parity (does a GLT report exist in BGLT); this addresses the separate *navigation* question (how do you get to it). The hamburger drawer's "Stats & Records" was a nested dropdown; it's now a single direct link to `GET /stats/`, a flat directory page grouped into 5 categories exactly like `/glthome/statistics/`: **Individual** (6 leaves), **League** (6 leaves), **Standings** (1 hub link — Standings already has its own top-level nav entry + full subnav, not re-listed here), **Contests** (4 leaves, already also reachable from their own top-level Contests entry), **Hall of Fame** (1 leaf). Landing on any Individual or League leaf page now shows a horizontal scrollable category subnav (same `.standings-subnav` pattern already shipped on Standings) linking every sibling report in that category — so switching between, say, Season Comparison and Player Participation Summary no longer requires returning to the directory. Confirmed against a real GLT export @user supplied (`League_Standings_7_30_2026_Quarry_Golf_Club.csv`) plus the page inventory already recorded below. Full detail: `3. Work Packages.md` (WP3.1) and `Plans/2026-07-28-mobile-nav-and-theme-options.md` (nav sampler artifact, pattern G).

**Full parity assessment complete 2026-07-09 — all 36 pages reviewed (corrected count: the original "~28" estimate undercounted; the actual inventory across 4 categories is 36).** All 36 fetched live via the authenticated `curl` method (Access Notes above), then cross-referenced against BGLT's actual current stats/reporting capability (surveyed across `stats.py`, `records.py`, `standings.py`, `my_stats.py`, `reports.py`, `players.py`, `handicap.py`, `skins.py`, `contests.py`, `schedule.py`, `scores.py`). Two pages were only sanity-checked for access in a prior pass (`individual/scoring-history-summary`, `standings/standings-race`) — both are now fully assessed below along with the other 34.

**Confirmed during this pass:** BGLT already has segment-based point tracking (`migrate_add_segments.py`, `segment_start_week`/`segment_end_week`, `segment_pts` in `standings.py`) — so GLT's `ddSegmentNumber` filters are *not* automatically a gap, just a filter BGLT's equivalent pages may or may not expose yet. BGLT's `scoring_mode` supports `match_play` and `stableford` only — **no stroke-play or best-ball format exists at all**, so any GLT page describing a stroke-play or best-ball report is only a real gap *if* @user's league actually wants that scoring format supported; flagged as a clarifying question below rather than assumed.

### Legend
**Gap:** ✅ No gap (BGLT already covers this) · 🟡 Partial/soft gap (BGLT has something adjacent, not identical) · 🔴 Real gap (nothing equivalent exists) · ❓ Contingent (depends on an open question)

**Status** (last column, refreshed 2026-07-10 to reflect current decisions — see the Build Status Summary at the end of this section for the full picture): 🛠️ Built & shipped · 📋 Ready to build (technical spec linked) · ⛔ Declined · ❓ Still open/contingent · — No plan needed (✅ rows, or minor/soft items not worth a dedicated doc)

### Contests

| # | GLT Page | GLT Purpose | BGLT Equivalent | Gap | Plan |
|---|---|---|---|---|---|
| 1 | `contests/contest-winners` | Flat, filterable (Contest/Round/Player/Team) log of every contest winner across the whole season: Contest Name, Week#, Date, Course, Winner, Hole#, Distance, Amount Won, Comments | Per-contest results table (built 2026-07-09) has the same 9ish columns, but scoped to **one contest at a time** — no unified cross-contest filterable view exists | 🔴 | 🛠️ Built & shipped 2026-07-10 — `GET /contests/winners`, tab "Contest Winner Detail". `Plans/2026-07-10-contest-winners-technical-spec.md`. Validated against real dev Postgres (seeded 3 contest types incl. team + no-prize edge cases, all rendered correctly, cleaned up after). **Tab/page title renamed to "Contest Winners Detail" 2026-08-01 to match GLT verbatim.** |
| 2 | `contests/contest-winners-summary` | Per-player total $ won across all contests, filterable by Contest type/Segment | No aggregate "$ won across contests" summary exists | 🔴 | 🛠️ Built & shipped 2026-07-10 — `GET /contests/winners/summary`, tab "Summary". Validated: correctly excludes team-scoped and no-amount results. **Tab/page title renamed to "Contest Winners Summary" 2026-08-01 to match GLT verbatim.** |
| 3 | `contests/individual-low-score` | Season-long log: one row per week showing that week's Low Net and Low Gross winner (name, gender, type, group, flight) | Weekly Recap (built same day) computes weekly low gross/net on the fly for one week's email, but there's no persisted season-spanning report page listing every week's winner | 🟡 | 🛠️ Built & shipped 2026-07-10 — `GET /contests/winners/low-score`, tab "Low Score". Reuses `email_config._top_n_with_ties()`'s proven tie-handling logic. Validated against real dev Postgres, all 5 completed weeks correct. **Tab/page title renamed to "Individual Low Score" 2026-08-01 to match GLT verbatim.** |
| 4 | `contests/individual-skins-leader` | Season skins leaderboard: Pos, Name, #Skins, $Won, Owed/Paid, Net | `skins.py` shows per-round winners/payouts and a season *overview*, but no ranked per-player season leaderboard aggregating total skins won | 🔴 | 🛠️ Built & shipped 2026-07-10 — `GET /contests/winners/skins`, tab "Skins Leader". Validated against real dev Postgres, correctly excludes carried-over (no-winner) skins. **Tab/page title renamed to "Individual Skins Leader" 2026-08-01 to match GLT verbatim.** |

### Hall of Fame

| # | GLT Page | GLT Purpose | BGLT Equivalent | Gap | Plan |
|---|---|---|---|---|---|
| 5 | `halloffame/hall-of-fame-winners` | Cross-season log of admin-defined custom awards (e.g. "Rookie of the Year") and their winners, by season | `standings.py` `/awards` computes **auto-derived current-season** leaderboards (Points Leader, Birdie Machine, etc.) — not a persisted, admin-curated, cross-season awards history with custom award types | 🔴 | 🛠️ Built & shipped 2026-07-10 — fixed-slot version, new `hall_of_fame_winners` table + `hall_of_fame.py` blueprint (`GET /hall-of-fame`, admin CRUD under `/admin/season/<id>/hall-of-fame`). `Plans/2026-07-10-hall-of-fame-technical-spec.md`. Validated against real dev Postgres: add/list/delete all confirmed, incl. the "Other" custom-label slot. **Page title/nav label renamed "Hall of Fame" → "Hall of Fame Winners" 2026-08-01 to match GLT verbatim.** |

### Individual

| # | GLT Page | GLT Purpose | BGLT Equivalent | Gap | Plan |
|---|---|---|---|---|---|
| 6 | `individual/handicap-history` | Per-player round-by-round Course Handicap + Handicap Index, filterable by player/# rounds | `handicap.py` `/player/<season_id>` — round-by-round handicap table, player dropdown | ✅ | — |
| 7 | `individual/individual-scoring-detail` | Full hole-by-hole gross score for **every round** a selected player has played, in one consolidated table (OUT/IN/Gross/Hdcp/Net/Pts/Eagle-Birdie-Par-Bogey-Double-Other/Skins) | `players.py` profile has a full round-history table, but at **round-total** level, not hole-by-hole — hole detail only exists per-single-matchup (`scores.view`) | 🟡 | 🛠️ Built & shipped 2026-07-10 — extended the existing hole-by-hole table on the player profile with Out/In (18-hole), Hdcp, Net, Pts, Skins columns. `Plans/2026-07-10-scoring-history-and-by-year-technical-spec.md`. Validated against real dev Postgres, exact match on Hdcp/Net/Pts for a hand-checked round. **Section header renamed "Hole-by-Hole Scoring History" → "Individual Scoring Detail" 2026-08-01 to match GLT verbatim.** |
| 8 | `individual/league-handicap-history` | League-wide matrix: one row per player, one column per round date, cell = handicap that round | `handicap.py` `/league/<season_id>` league_matrix — same shape (rows=players, cols=rounds, cell=hcp), admin-editable | ✅ | — **Page title/h1 renamed "League Handicap Matrix" → "League Handicap History" 2026-08-01 to match GLT verbatim** (route name `league_matrix` left unchanged). |
| 9 | `individual/opponent-average` | Per-player: # rounds, own avg gross/hdcp/net vs. **average of all opponents ever faced**, and the differential | `players.py` `/compare` does head-to-head between **two specific named players** — no aggregate "vs. average opponent" stat exists | 🔴 | ⛔ Declined 2026-07-10 — @user chose hole-by-hole only, not opponent stats |
| 10 | `individual/opponent-history` | Chronological per-round log: my score/hdcp/net side-by-side with that round's specific opponent's score/hdcp/net | Underlying data (matchup + both scorecards) exists in the DB, but no dedicated page presents it in this side-by-side chronological format | 🟡 | ⛔ Declined 2026-07-10 — same call as #9 |
| 11 | `individual/player-participation` | Season attendance: Rounds Scheduled, Rounds Played, Participation %, Sub Count, Absent Count, per player | No participation/attendance report found — absences are tracked (`player_absences`) but not rolled up into a season summary report | 🔴 | 🛠️ Built & shipped 2026-07-10 — `GET /stats/participation`, `Plans/2026-07-10-player-participation-technical-spec.md`. Validated against real dev Postgres, incl. the absence+sub edge case (player 15: 23 scheduled, 2 played, 3 absent, 2 subbed — exact match). **Page title renamed "Player Participation" → "Player Participation Summary" 2026-08-01 to match GLT verbatim** (nav label left short as "Participation" for space). |
| 12 | `individual/score-aggregate-by-group` | Per league-group (flight/division): # scores, # "best" scores (best-N cut), total/avg gross+net | `standings.py` `/flight` has flight-level avg gross, but not an explicit best-N-of-M aggregate cut | 🟡 | — Soft gap, no plan; revisit if @user wants a best-N cut specifically |
| 13 | `individual/score-distribution` | Chart-only page (no static table found — likely a score histogram for a selected player) | `players.py` `/compare` already has a score-distribution **percentage** breakdown (eagle/birdie/par/bogey/double %) | — | ⛔ Closed 2026-07-10 — @user: "just a graph, ignore it." |
| 14 | `individual/scoring-average-by-year` | Per (Year × Course) for one player: rounds, front/back net averages, eagle/birdie/par/bogey/double/other counts | `players.py` profile has a per-season breakdown, but not cross-tabbed **by course within year** | 🟡 | 🛠️ Built & shipped 2026-07-10 — `GET /players/<id>/scoring-by-year`, same spec as #7. Net-based category counts, cross-tabbed by season/course/nine. Validated against real dev Postgres, exact match on avg net + all 6 category counts. **Page title/section link renamed "Scoring by Year" → "Scoring Average By Year" 2026-08-01 to match GLT verbatim** (route name `scoring_by_year` left unchanged — URL slugs weren't in scope). |
| 15 | `individual/scoring-history-summary` | Round-level log (no hole detail) across all players or one selected player/year: Date, Name, Course, Par, Rating, Slope, Score, Hdcp, Net, Pts, scoring-category counts | `players.py` profile's round-history table is very close — likely missing Course Rating/Slope columns and per-round scoring-category counts | 🟡 | ⛔ Declined 2026-07-10 — Rating/Slope columns + category-count style were part of the "smaller extensions" bucket @user chose not to build |

### League

| # | GLT Page | GLT Purpose | BGLT Equivalent | Gap | Plan |
|---|---|---|---|---|---|
| 16 | `league/course-handicap-player-list` | Full roster reference sheet: each player's course handicap at every course/tee combination (front/back/total rating, slope, par) | No course-handicap reference sheet exists — `handicap.py` tracks handicap **per round played**, not a precomputed lookup across all courses in the system | 🔴 | ⛔ Declined 2026-07-10 — @user: existing per-round computation already covers the need |
| 17 | `league/course-handicap-player-matrix` | Same course-handicap lookup, filtered to one Course/Tee/Gender/Side at a time | Same gap as #16 — shares the same underlying data/build | 🔴 | ⛔ Declined 2026-07-10 — same call as #16 |
| 18 | `league/course-list` | Course directory: name, address, all tee-box par/yardage/rating/slope detail | **Verified 2026-07-10**: `courses.index()` list→`detail.html` per-course shows Tee/Color/Gender/Format/Par/Slope-Rating/Holes; `courses.city`/`state` cover address. One real minor gap: total yardage per tee is captured (`holes.distance_yards`, editable in the holes-edit admin form with OUT/IN totals) but never displayed on the member-facing course detail page. | ✅ | — No Gap. Yardage-display gap is cosmetic/optional — revisit only if @user wants it surfaced to members, not worth a dedicated plan. |
| 19 | `league/course-summary` | Per-hole: rank, par, average, +/-par, eagle/birdie/par/bogey/double/other counts, filterable by year/course/tee | `stats.py` `/course/<course_id>` already does per-hole averages + difficulty rank at a course | ✅ | — |
| 20 | `league/league-scoring-average` | Ranked leaderboard of **all players** by scoring average, with hole-by-hole detail per player | `stats.py` hole-averages is single-player-selected or league-wide-difficulty — not a ranked "best scoring average in the league" leaderboard | 🟡 | 🛠️ Built & shipped 2026-07-10 — `GET /stats/leaderboard`, `Plans/2026-07-10-leaderboard-and-comparison-technical-spec.md`. Validated against real dev Postgres (ranking + averages hand-verified). **Page title/nav label renamed "Scoring Leaderboard"/"Leaderboard" → "League Scoring Average" 2026-08-01 to match GLT verbatim** (route name `stats.leaderboard` left unchanged). |
| 21 | `league/league-scoring-summary` | Notable-scores log broken out by course (best round per course, etc.) | `records.py` has top-5 low/high gross rounds league-wide, not broken out per-course | 🟡 | — Soft gap, no plan; revisit if @user wants per-course records specifically |
| 22 | `league/player-best-ball` | Best-ball format leaderboard (best individual score among a group, per hole, season-long) | No best-ball scoring format exists in BGLT at all (`scoring_mode` is match_play/stableford only) | 🛠️ | Built & shipped 2026-07-10 — Best Ball preset, `Plans/2026-07-10-scoring-formats-technical-spec.md` |
| 23 | `league/player-handicap-cards` | Chart/card-only page — likely a printable per-player "handicap card" | Possibly covered in spirit by `scores.py` print_scorecards | — | ⛔ Closed 2026-07-10 — @user: can be ignored |
| 24 | `league/player-scoring-summary` | Per-player Gross **and** Net eagle/birdie/par/bogey/double/other counts, side by side | `players.py` profile tracks gross scoring-category counts; net-based category counts (relative to net par) not confirmed to exist | 🟡 | ⛔ Declined 2026-07-10 — net-category counts were part of the declined "smaller extensions" bucket |
| 25 | `league/season-comparison` | Per-player season-over-season comparison (Value/Value/Change/%Change) + handicap before/after | `stats.py` `/compare` is **team**-level season comparison, not player-level | 🔴 | 🛠️ Built & shipped 2026-07-10 — `GET /stats/player-compare`, `Plans/2026-07-10-leaderboard-and-comparison-technical-spec.md`. Compares gross average, net average, handicap average. Validated against real dev Postgres (exact match on all 3 stats). **Page title/h1 renamed "Player Season Comparison" → "Season Comparison" 2026-08-01 to match GLT verbatim** (nav label already said "Season Comparison"; route name `player_compare` left unchanged). |
| 26 | `league/team-best-ball` | Same best-ball format, team-level | Same as #22 | 🛠️ | Built & shipped 2026-07-10 — same as #22 |

### Standings

| # | GLT Page | GLT Purpose | BGLT Equivalent | Gap | Plan |
|---|---|---|---|---|---|
| 27 | `standings/detailed-scoresheet` | One giant (267KB!) combined scoresheet: every round, every hole, every point, whole season. **Confirmed 2026-08-01 via @user-supplied real export** (`League_Standings_7_30_2026_Quarry_Golf_Club.csv`): GLT's own on-page title for this is literally **"League Standings"** — filterable down to a single round, showing Pos/Segment Pts/Season Pts *and* that round's full hole-by-hole detail + course hdcp/hdcp before-after, per player, grouped by division/rank. | `standings.py` `/scorecards` (season grid) + `/weekly` (per-week hole detail) together cover most of this, just split across two pages instead of one mega-page | 🟡 | — Presentation difference, not a missing capability; no dedicated-build plan. **Naming decision 2026-08-01, per @user**: GLT's "League Standings" collides with BGLT's own main Standings page title (`standings.index`'s h1 is already "League Standings") — since GLT's version is far more info-dense (full box score + cumulative points combined), call any BGLT equivalent **"League Standings - Detail"** to keep the two distinct. For now, added as a placeholder tab in the subnav scroll bar on `standings.index` only, pointing at the closest existing match (`standings.weekly`) — not a new merged page yet. |
| 28 | `standings/individual-point-summary` | Per-player Season/Segment points split into Player-earned vs. Team-earned vs. Total, plus averages | `standings.py` `/individual` has total points + rank, but the Player-vs-Team point split specifically isn't confirmed | 🟡 | — Soft gap, no plan; revisit if @user wants the player/team point split broken out |
| 29 | `standings/individual-season-point-detail` | Detailed per-round point breakdown, filterable including "Adjust for Solo Play" | Does BGLT have a solo-play point-adjustment rule at all? Unconfirmed | ❓ | ❓ Parked — page was JS-rendered (empty in the raw-HTML scrape), only the filter's *name* was ever seen, not real content; @user didn't recognize it as a real scenario. Not pursued unless it resurfaces with real content to check against |
| 30 | `standings/individual-season-stroke-play-results` | Stroke-play-format standings (best-N-of-M rounds) | Contingent on stroke-play format support (BGLT is match_play/stableford only) | ❓ | ⛔ Declined 2026-07-10 — `Plans/2026-07-10-glt-field-position-stroke-play.md`. Distinct best-N-of-M score-based ranking, orthogonal to BGLT's points engine (not the same as the already-built Classical Stroke Play). No signal of real demand across 3 review passes |
| 31 | `standings/individual-stroke-play-results` | Stroke-play leaderboard with hole detail, filterable by round/group | Same stroke-play contingency as #30 | ❓ | ⛔ Declined 2026-07-10 — same call as #30, this was the display layer on #30's ranking |
| 32 | `standings/individual-team-win-loss-tie` | **Individual player** (not team) W-L-T record, win%, points won | **Verified 2026-07-10**: `standings.individual()` derives W-L-T purely from `mr.overall_point_won` (win ≥1.0 / tie =0.5 / loss =0.0). There's no separate "forfeit" record type anywhere in the schema — an absence that forfeits the overall point per `absence_overall_point_policy` just yields `overall_point_won=0.0`, which correctly falls into the loss bucket. That's the right behavior for a W-L-T record (a forfeit *is* a loss), not a gap. | ✅ | — No Gap. No distinct "Forfeits" column needed — already folded correctly into the loss count. |
| 33 | `standings/standings-by-group` | Simple per-group (flight/division) point ranking | `standings.py` `/divisions` — direct match | ✅ | — **Page title/h1 renamed "Points by Division" → "Standings By Group" 2026-08-01 to match GLT verbatim; subnav tab label across all 6 standings pages shortened to "By Group"** (route name `divisions` and underlying "division" terminology left unchanged — not in scope). |
| 34 | `standings/standings-race` | GLT titles this page "League Standings Position" — a chart tracking each team's **rank** (1st, 2nd, 3rd...) week by week, not their point total | `standings.py` `/trend` charts cumulative **points** over time instead — a team can gain points every week and still drop in rank if others gain faster, so the two charts can tell different stories late in a season | — | ⛔ Declined 2026-07-10 — @user: "No don't need standings race" |
| 35 | `standings/team-bestball-stroke-results` | Best-ball format, team stroke results | Same best-ball contingency as #22/#26 | 🛠️ | Built & shipped 2026-07-10 — same as #22/#26 |
| 36 | `standings/team-point-summary` | Team Round/Segment/Season points, ranked | `standings.py` index already shows this; segment points already supported (see note above) | ✅ | — |

### Build Status Summary (refreshed 2026-07-10 — single source of truth for "are we building this GLT page or not")

Every plan opened from the 2026-07-09 stats-parity pass is now resolved — nothing left in `Evaluating` limbo, and every non-declined plan (A, B, C, E, F, G, H) is **built and shipped** as of 2026-07-10. Original grouping (Plan A–I) kept as the reference key, since the small per-plan docs under `Plans/` still hold the full tradeoff analysis and decision history; this table is what to check first for current status, the individual docs are for "why." A visual page-by-page scorecard of this same status is archived at `Artifacts/2026-07-29-glt-stats-parity-reference.html`.

| Plan | GLT items | Status | Spec / detail |
|---|---|---|---|
| A — Contest Winners + Leaderboards | #1, #2, #3, #4 | 🛠️ Built & shipped 2026-07-10 | `Plans/2026-07-10-contest-winners-technical-spec.md` — 4 tabs matching GLT's own names: Contest Winner Detail, Summary, Low Score, Skins Leader. Both season-scoped and all-time views. Validated against real dev Postgres. |
| B — Hall of Fame | #5 | 🛠️ Built & shipped 2026-07-10 | `Plans/2026-07-10-hall-of-fame-technical-spec.md` — fixed-slot version (Rookie of the Year, Sportsmanship, Commissioner's Choice, Other). New table + blueprint, validated against real dev Postgres. |
| C — Player Scoring Detail | #7, #14 (built) · #9, #10, #15, #24 (declined) | 🛠️ Built & shipped 2026-07-10 | `Plans/2026-07-10-scoring-history-and-by-year-technical-spec.md` — "Scoring History" (#7, extended the existing hole-by-hole table with Out/In/Hdcp/Net/Pts/Skins) and "Scoring by Year" (#14, new page). Both validated against real dev Postgres. Opponent-average/history (#9/#10) and the Rating/Slope + net-category "smaller extensions" (#15/#24) all declined. |
| D — Course Handicap Reference | #16, #17 | ⛔ Declined | Existing per-round computation already covers the need. |
| E — League Scoring Leaderboard | #20 | 🛠️ Built & shipped 2026-07-10 | `Plans/2026-07-10-leaderboard-and-comparison-technical-spec.md` — standalone page, `GET /stats/leaderboard`, validated against real dev Postgres. |
| F — Player Participation | #11 | 🛠️ Built & shipped 2026-07-10 | `Plans/2026-07-10-player-participation-technical-spec.md` — `GET /stats/participation`, visible to all members. Validated against real dev Postgres. |
| G — Player Season Comparison | #25 | 🛠️ Built & shipped 2026-07-10 | Same spec as Plan E — `GET /stats/player-compare`, compares gross average, net average, handicap average across two seasons. Validated against real dev Postgres. |
| H — Best Ball / Team Totals / Classical Stroke Play | #22, #26, #35 (Best Ball) | 🛠️ Built & shipped | `Plans/2026-07-10-scoring-formats-technical-spec.md` — Best Ball, Team Totals, and Classical Stroke Play all built, validated, in production. High/Low dropped entirely (not #22/26/35 — GLT's separate "high/low of each teammate" format). GLT's rank/field-position Stroke Play (#30, #31) stays a possible future *setting*, deliberately not built as a preset — see Plan K. |
| I — Standings Race | #34 | ⛔ Declined | "No don't need standings race." |
| J — Dead Settings Resolution | (settings-audit items, not numbered GLT pages) | 📋 Spec written 2026-07-10 | `Plans/2026-07-10-dead-settings-resolution.md` — 4 independent decisions: remove `ab_designation_method` (fully vestigial), wire Match Play points pair (changes live scoring, needs sign-off), build or remove `skins_config.handicap_percent`, implement real WHS differential math. |
| K — Classical Stroke Play Standings View | (follow-up from Plan H, not a new GLT page) | 🛠️ Built & shipped 2026-07-10 | `Plans/2026-07-10-classical-stroke-play-standings-view.md` — fixed the live W-L-T miscounting bug and gated `/standings/individual`'s Role/W-T-L columns off for this format; existing Rank/Total Pts/Pts-per-Round columns already serve as the field-wide leaderboard. Validated against real dev Postgres. |
| L — GLT Field-Position Stroke Play | #30, #31 | ⛔ Declined 2026-07-10 | `Plans/2026-07-10-glt-field-position-stroke-play.md` — resolved the naming collision with Plan H's Classical Stroke Play; @user approved the Declined recommendation. |

**Not tied to a lettered plan, still genuinely open:**
- #29 (solo-play adjustment) — parked, page was JS-rendered/empty when scraped, no real content ever confirmed, not pursued unless it resurfaces with something concrete to check against.
- #12, #21, #28 — soft/minor gaps, explicitly not worth a dedicated plan doc; revisit only if raised again.
- #13, #23 — closed outright, no action (score-distribution graph, player-handicap-cards).

Resolved this pass: #18 (`course-list`) and #32 (individual W-L-T forfeits) both verified ✅ No Gap — see their rows above. #30/#31 now tied to Plan L (spec written, decision pending). The 4 dead settings from the 2026-07-04 settings audit now tied to Plan J. Classical Stroke Play's standings-view follow-up now tied to Plan K.

Every other row not listed above is either ✅ (no gap) or was resolved inline in its own row.

## Next steps

- Rules page (`/glthome/league/leaguerules`) still not reviewed for content.
- **UI note from @user (2026-07-09):** displaying this many stat report pages efficiently will likely need a **nested/sub-menu format** in the hamburger nav (a flat "Stats & Records" list of 15-20+ items won't scale) — logged as a Work Packages backlog item, not built yet. Now more pressing: 6 plan docs are `Ready to build`, which will add real nav entries once built.
- **`/glthome/about/features`** (public marketing page, found by @user 2026-07-09) mentions two report names not clearly matched to a URL in the 36-page inventory: **"Player Head To Head"** ("if an individual player league, you can see the results of head to head matches over time") and **"Team Head to Head"** ("team number vs team number... over time"). Both likely just describe the already-inventoried `individual/opponent-average` + `individual/opponent-history` pages (both **declined**, see Build Status Summary) in marketing language rather than being genuinely separate URLs — not confirmed, since no direct link was available from this page. Worth a quick fetch-and-check next time GLT access is used, rather than assumed either way.

-----

*Add rows as new GLT pages are reviewed (Settings/Workflow/Feature tracks — UI track on hold per above).*
